# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Allied-FeedOps"

[setup]
script = '''
# ── Pull secrets from GCP at worktree creation ──
export OPENAI_API_KEY=$(gcloud secrets versions access latest --secret=feedops-openai-api-key --project=bobbys-project-346400)
export SUPABASE_URL=$(gcloud secrets versions access latest --secret=feedops-supabase-url --project=bobbys-project-346400)
export SUPABASE_SERVICE_ROLE_KEY=$(gcloud secrets versions access latest --secret=feedops-supabase-key --project=bobbys-project-346400)
export GOOGLE_ADS_DEVELOPER_TOKEN=$(gcloud secrets versions access latest --secret=feedops-google-ads-developer-token --project=bobbys-project-346400)
export GOOGLE_ADS_CLIENT_ID=$(gcloud secrets versions access latest --secret=feedops-google-ads-client-id --project=bobbys-project-346400)
export GOOGLE_ADS_CLIENT_SECRET=$(gcloud secrets versions access latest --secret=feedops-google-ads-client-secret --project=bobbys-project-346400)
export GOOGLE_ADS_REFRESH_TOKEN=$(gcloud secrets versions access latest --secret=feedops-google-ads-refresh-token --project=bobbys-project-346400)
export GOOGLE_ADS_LOGIN_CUSTOMER_ID=$(gcloud secrets versions access latest --secret=feedops-google-ads-login-customer-id --project=bobbys-project-346400)

# ── Non-secret config ──
export FEEDOPS_PROMPT_VERSION=v2
export FEEDOPS_REASONING_EFFORT=high
export PYTHONPATH=$(pwd)/src
export PORT=8080

# ── Install deps (match Dockerfile) ──
pip install -r requirements.txt
pip install .
pip install pytest pytest-asyncio

BUT — this requires gcloud to be installed and authenticated in the Codex sandbox. If that's not available, the other option is to have the script read from your local .env.vercel file:

# Source secrets from gitignored env file
set -a
source .env.vercel
set +a
'''
