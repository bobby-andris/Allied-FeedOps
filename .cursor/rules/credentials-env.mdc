---
description: Use .env for credentials
alwaysApply: true
---

# Credentials Source of Truth

- All service credentials are stored in the repository `.env` file.
- Always use `.env` values when credentials are needed; do not invent or substitute.
- The `.env` file is gitignored; do not attempt to commit or recreate it.
