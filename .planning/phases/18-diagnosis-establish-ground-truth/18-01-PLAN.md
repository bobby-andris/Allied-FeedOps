---
phase: 18-diagnosis-establish-ground-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/generation-paths.md
  - .planning/phases/18-diagnosis-establish-ground-truth/18-01-SUMMARY.md
autonomous: true
requirements:
  - DIAG-02
  - DIAG-03

must_haves:
  truths:
    - "Code path trace documents the exact function call sequence for single-SKU UI regeneration (Path A) and batch generation (Path B)"
    - "Divergence points between Path A and Path B are explicitly listed with which functions are bypassed"
    - "Feature flag audit confirms which flags are wired into which production paths with grep evidence"
    - "Cloud Run runtime env vars are checked to determine actual flag state (defaults vs explicit)"
  artifacts:
    - path: "docs/architecture/generation-paths.md"
      provides: "End-to-end call graph for both generation paths with feature flag wiring"
      contains: "regenerate_content"
    - path: ".planning/phases/18-diagnosis-establish-ground-truth/18-01-SUMMARY.md"
      provides: "Plan execution summary for downstream phases"
  key_links:
    - from: "route.ts"
      to: "main.py::regenerate_content()"
      via: "HTTP POST to Cloud Run /regenerate"
      pattern: "PIPELINE_URL.*regenerate"
    - from: "feature_flags.py"
      to: "evidence.py"
      via: "is_intent_curator_v1_enabled and is_segment_strategy_v1_enabled calls"
      pattern: "is_.*_enabled"
---

<objective>
Trace and document the production code paths for content generation, audit feature flag wiring, and verify Cloud Run runtime state.

Purpose: Answer DIAG-02 (which code path runs?) and DIAG-03 (are feature flags wired?) with grep-verified evidence before any fixes are attempted.
Output: Architecture documentation with call graphs + diagnostic report for Phase 19-20 consumption.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-diagnosis-establish-ground-truth/18-RESEARCH.md

Key source files to inspect:
@src/feedops/api/main.py
@src/feedops/pipeline/feature_flags.py
@src/feedops/api/prompt_loader.py
@src/feedops/pipeline/evidence.py
@src/feedops/pipeline/generator.py
@dashboard/src/app/api/regenerate/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace generation paths and document call graphs with feature flag audit</name>
  <files>docs/architecture/generation-paths.md</files>
  <action>
Create `docs/architecture/generation-paths.md` documenting both generation paths with grep-verified evidence.

**Path A (Single-SKU UI Regeneration):**
1. Trace from `dashboard/src/app/api/regenerate/route.ts` through to Cloud Run `main.py::regenerate_content()`
2. For each function called, record: file, function name, line number (grep to verify)
3. Explicitly list functions that are BYPASSED (generator.py::build_prompt, keyword_placement.py, verifier.py, selection.py)
4. Show which feature flags are checked and where (PROMPT_CONTRACT_V2 in prompt_loader.py, INTENT_CURATOR_V1 and SEGMENT_STRATEGY_V1 in evidence.py)

**Path B (Batch Generation):**
1. Trace from `/batch-optimize` and `/hybrid-generate` endpoints through `process_batch_job()` / `process_hybrid_batch_job()`
2. Show that the core function sequence is identical to Path A (same `_build_generation_user_prompt`, `_generate_with_metrics`, etc.)
3. Note divergences: Path B uses `run_async_in_thread()`, `_persist_generated_content_and_history()` helper, and does NOT call route.ts validation

**Feature Flag Audit (integrated into document, per user's discretion):**
1. For each flag (PROMPT_CONTRACT_V2, INTENT_CURATOR_V1, SEGMENT_STRATEGY_V1):
   - Show the definition in feature_flags.py with default value
   - List ALL call sites with file:line (grep `is_prompt_contract_v2_enabled\|is_intent_curator_v1_enabled\|is_segment_strategy_v1_enabled` across codebase)
   - Mark each call site as "IN production path" or "ONLY in legacy path"
2. Document that SEGMENT_STRATEGY_V1 in generator.py:100 is legacy-only (optimize.py is the only importer)

**Cloud Run Runtime Check:**
1. Run: `gcloud run services describe feedops-pipeline --project=bobbys-project-346400 --region=us-east1 --format='json'` and extract env vars
2. Check if PROMPT_CONTRACT_V2, INTENT_CURATOR_V1, SEGMENT_STRATEGY_V1 are explicitly set or relying on defaults (True)
3. Also check keyword_bank.json — grep .gitignore for it, and check if it's in the Docker build context

**Format:** Markdown with ASCII call graphs (similar to 18-RESEARCH.md call graph format). Include a summary table at the top:

```
| Question | Answer | Evidence |
|----------|--------|----------|
| Which code path runs for UI regen? | main.py::regenerate_content() | grep shows route.ts POSTs to /regenerate |
| Are paths divergent? | Minimal — same core functions | Side-by-side comparison below |
| Are feature flags wired? | All 3 wired to both paths | Call site table with file:line |
| Flag runtime state? | All default True (enabled) | gcloud describe output |
```

Also create a copy of findings in `.planning/phases/18-diagnosis-establish-ground-truth/` for downstream agent consumption — reference the architecture doc as canonical source.
  </action>
  <verify>
1. `cat docs/architecture/generation-paths.md` shows both path call graphs with file:line references
2. Feature flag call site table has entries for all 3 flags with "IN production path" / "ONLY in legacy" labels
3. Cloud Run env var check output is included or referenced
4. Document answers all four questions in the summary table
  </verify>
  <done>
Architecture doc exists with grep-verified call graphs for both paths, feature flag audit with call sites mapped to production vs legacy, and Cloud Run runtime state confirmed. DIAG-02 and DIAG-03 requirements are fully addressed with evidence.
  </done>
</task>

</tasks>

<verification>
- [ ] `docs/architecture/generation-paths.md` exists with both path traces
- [ ] Feature flag audit covers all 3 flags with file:line evidence
- [ ] Cloud Run runtime env vars checked
- [ ] Summary table answers DIAG-02 and DIAG-03 questions
- [ ] Findings are available in `.planning/phases/18-*/` for downstream consumption
</verification>

<success_criteria>
DIAG-02: The single-SKU UI regeneration path is documented end-to-end with function names and which are bypassed.
DIAG-03: Feature flag audit confirms with grep evidence whether each flag has active call sites in the production code path.
</success_criteria>

<output>
After completion, create `.planning/phases/18-diagnosis-establish-ground-truth/18-01-SUMMARY.md`
</output>
