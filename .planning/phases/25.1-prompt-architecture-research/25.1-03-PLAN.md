---
phase: 25.1-prompt-architecture-research
plan: 03
type: execute
wave: 3
depends_on: ["25.1-02"]
files_modified:
  - scripts/ab_prompt_test.py
  - .planning/phases/25.1-prompt-architecture-research/ab-test-results.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "3 prompt variations tested on 3 representative SKUs from the evaluation set"
    - "At least 3 random unseen SKUs tested to prevent overfitting"
    - "A/B test results show clear improvement: no keyword stuffing, varied structure, tighter writing"
    - "Content generated with new prompt would confidently win blind eval against Round 3 output"
    - "Mini human review: Bobby confirms 3-5 outputs pass gut check before declaring research complete"
  artifacts:
    - path: "scripts/ab_prompt_test.py"
      provides: "Reusable A/B prompt testing script for content generation"
    - path: ".planning/phases/25.1-prompt-architecture-research/ab-test-results.md"
      provides: "Complete A/B test results with side-by-side comparisons and analysis"
  key_links:
    - from: "scripts/ab_prompt_test.py"
      to: "src/feedops/providers/openai_provider.py"
      via: "OpenAI API call for generation"
      pattern: "openai_provider|generate"
    - from: "scripts/ab_prompt_test.py"
      to: "new-system-prompt.txt"
      via: "Loads new prompt from file"
      pattern: "new-system-prompt"
---

<objective>
A/B test the new prompt architecture against the current prompt and a minimal prompt. Test on representative SKUs from the evaluation set AND unseen SKUs. Conclude with a mini human review checkpoint.

Purpose: Validate that the new prompt architecture actually produces better content before committing to a production rewrite. Prevent overfitting by testing on unseen SKUs.

Output: A/B testing script + results doc with side-by-side comparisons + human gut-check approval.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-prompt-architecture-research/25.1-RESEARCH.md
@.planning/phases/25.1-prompt-architecture-research/25.1-CONTEXT.md
@.planning/phases/25.1-prompt-architecture-research/25.1-02-SUMMARY.md
@.planning/phases/25.1-prompt-architecture-research/new-system-prompt.txt
@.planning/phases/25.1-prompt-architecture-research/new-user-prompt-template.txt
@src/feedops/providers/openai_provider.py
@src/feedops/api/prompt_builder.py
@src/feedops/api/prompt_loader.py
@src/feedops/pipeline/prompts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build A/B prompt testing script and run tests</name>
  <files>
    scripts/ab_prompt_test.py
    .planning/phases/25.1-prompt-architecture-research/ab-test-results.md
  </files>
  <action>
Create `scripts/ab_prompt_test.py` that tests 3 prompt variations:

**Variation A (Current):** The existing production prompt — `get_system_prompt(mode="batch")` + `build_core_prompt()`. This is the 267K-char baseline.

**Variation B (Minimal):** Strip most skills from the current prompt. Use SYSTEM_PROMPT base only (no SKILL.md injection) + `build_core_prompt()` for user message. This tests whether LESS instruction actually produces BETTER output.

**Variation C (Optimized):** The new prompt from Plan 02 — load `new-system-prompt.txt` as system message, build user message from evidence data using the new user prompt template.

**SKU Selection (Claude's discretion per CONTEXT.md):**

Representative SKUs from the evaluation set (pick 3 with different failure modes):
- One that had keyword stuffing problems (e.g., a towel bar or grab bar)
- One that had filler-heavy descriptions (e.g., a shower accessory)
- One that had monotonous structure (e.g., a mirror or soap dish)

Unseen SKUs (pick 3-5 random SKUs NOT in the 10 evaluation set):
- Query Supabase `product_catalog` for random SKUs with approved content
- Ensure variety: different categories, different collections

**For each SKU x variation:**
1. Build the system prompt and user prompt
2. Call OpenAI GPT-5.2 with `reasoning_effort="medium"`, strict JSON schema mode
3. Parse the JSON response
4. Extract: google_title, google_description, bing_title, bing_description, shopify_title, shopify_description, self_score (if present)
5. Save raw outputs to `/tmp/ab_test_outputs/`

**Script output:**
- Print summary table: SKU | Variation | Google Title | Desc Length | Self Score Total
- Write detailed results to `ab-test-results.md` with:
  - Side-by-side comparison of all 3 variations for each SKU
  - Analysis: keyword stuffing presence (grep for repeated terms), structure variety (sentence count, opening words), filler detection (generic phrase count)
  - Token usage comparison (system tokens for each variation)
  - Overall recommendation with evidence

**Analysis methodology in the results doc:**
For each SKU, evaluate each variation on:
- Keyword stuffing: Count repeated non-trivial words. Flag if any word appears 4+ times.
- Structure variety: Compare opening words across variations. Flag if >50% start the same way.
- Filler detection: Count instances of banned words and generic phrases ("premium", "luxurious", "upgrade your bathroom").
- Robert's concerns: Check for competitor material references, weight capacity, excessive dimensions, "heritage bathroom fixtures".
- Tightness: Word count and information density (claims per 100 words).
  </action>
  <verify>
    <automated>python scripts/ab_prompt_test.py --dry-run 2>/dev/null | head -5</automated>
    <manual>Inspect ab-test-results.md for side-by-side comparisons and analysis</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    - Script runs and generates outputs for all 3 variations x all SKUs
    - Results doc contains side-by-side comparisons for every SKU
    - Analysis covers keyword stuffing, structure variety, filler, Robert's concerns, and tightness
    - Token usage comparison shows system prompt token reduction
    - Clear recommendation with supporting evidence
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mini human review — Bobby gut-check on new prompt outputs</name>
  <files>.planning/phases/25.1-prompt-architecture-research/ab-test-results.md</files>
  <action>
    Present the A/B test results to Bobby for gut-check review. Bobby reviews the Variation C (Optimized) outputs against Variation A (Current) across representative and unseen SKUs. This is the mini human review checkpoint required by CONTEXT.md before declaring research complete.
  </action>
  <verify>
    <automated>test -f ".planning/phases/25.1-prompt-architecture-research/ab-test-results.md" &amp;&amp; echo "OK"</automated>
    <manual>Bobby confirms outputs pass gut check or identifies specific issues</manual>
  </verify>
  <done>
    Bobby has reviewed 3-5 outputs from the new prompt and confirmed they are "obviously better" than Round 3 output, OR specific issues have been identified for iteration.
  </done>
</task>

</tasks>

<verification>
- A/B test script exists and can generate outputs for all variations
- Results doc has side-by-side comparisons for all tested SKUs
- Quantitative analysis shows improvement on keyword stuffing, filler, and structure metrics
- Bobby confirms new prompt outputs pass gut check (checkpoint)
</verification>

<success_criteria>
A/B tests demonstrate clear, measurable improvement from the new prompt architecture. The optimized prompt (Variation C) produces content that is: (1) free of keyword stuffing, (2) structurally varied, (3) tighter writing without filler, (4) addresses all of Robert's evaluation concerns, and (5) generalizes to unseen SKUs. Bobby's gut check confirms confidence before returning to Phase 25 for formal evaluation.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-prompt-architecture-research/25.1-03-SUMMARY.md`
</output>
