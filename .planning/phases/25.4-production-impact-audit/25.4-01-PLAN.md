---
phase: 25.4-production-impact-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedops/models/score.py
  - src/feedops/pipeline/generator.py
  - tests/test_pipeline.py
  - tests/test_v1_path_regression.py
  - .planning/phases/25.4-production-impact-audit/AUDIT-REPORT.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, AUDIT-04, AUDIT-05]

must_haves:
  truths:
    - "Score model fields match CANDIDATE_SCHEMA self_score fields (10 criteria)"
    - "parse_candidate_response() reads the 10 new field names from LLM response"
    - "All 3 previously-failing pipeline tests pass"
    - "All 553+ previously-passing tests still pass (no regressions)"
    - "An audit report documents every HIGH/MEDIUM risk change with disposition"
    - "The v1 code path assembles a valid prompt with system prompt, evidence, keywords, and product data fields"
  artifacts:
    - path: "src/feedops/models/score.py"
      provides: "Score model with 10 criteria matching CANDIDATE_SCHEMA"
      contains: "hook_quality"
    - path: "src/feedops/pipeline/generator.py"
      provides: "parse_candidate_response reading new field names"
      contains: "hook_quality"
    - path: "tests/test_pipeline.py"
      provides: "Fixed test assertions matching current behavior"
    - path: "tests/test_v1_path_regression.py"
      provides: "v1 code path regression test exercising build_core_prompt and get_system_prompt"
      contains: "build_core_prompt"
    - path: ".planning/phases/25.4-production-impact-audit/AUDIT-REPORT.md"
      provides: "Complete audit of all v1-path-affecting changes"
      min_lines: 50
  key_links:
    - from: "src/feedops/pipeline/prompts.py"
      to: "src/feedops/models/score.py"
      via: "CANDIDATE_SCHEMA self_score field names must match Score model fields"
      pattern: "hook_quality.*product_specificity.*competitive_diff"
    - from: "src/feedops/pipeline/generator.py"
      to: "src/feedops/models/score.py"
      via: "parse_candidate_response() constructs Score from LLM response"
      pattern: "Score\\("
    - from: "tests/test_v1_path_regression.py"
      to: "src/feedops/api/prompt_builder.py"
      via: "Tests exercise build_core_prompt with representative SKU data"
      pattern: "build_core_prompt"
---

<objective>
Align the Score model and parse_candidate_response() with the CANDIDATE_SCHEMA 10-criterion rubric, fix all 3 failing pipeline tests, write a v1 path regression test, and produce a comprehensive audit report documenting every v1-path-affecting change from Phases 23-25.3.

Purpose: Close technical debt from the CANDIDATE_SCHEMA rename (Phase 23) that left Score model and parser reading old field names. Fix tests that broke due to behavioral changes in evidence fetching and title retry logic. Verify the v1 production code path end-to-end. Document the full impact of 24+ commits already deployed to production.

Output: Updated Score model, updated parser, fixed tests, v1 regression test, AUDIT-REPORT.md
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.4-production-impact-audit/25.4-RESEARCH.md
@src/feedops/models/score.py
@src/feedops/pipeline/generator.py
@src/feedops/pipeline/prompts.py
@src/feedops/api/prompt_builder.py
@tests/test_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Score model + parse_candidate_response + fix 3 failing tests + v1 path regression test</name>
  <files>
    src/feedops/models/score.py
    src/feedops/pipeline/generator.py
    tests/test_pipeline.py
    tests/test_v1_path_regression.py
  </files>
  <action>
**Part A: Update Score model (score.py)**

Replace the 6-field Score model with 10 fields matching CANDIDATE_SCHEMA in prompts.py:

Old fields: specificity, benefit_coverage, keyword_inclusion, format_adherence, brand_voice, factual_accuracy
New fields: hook_quality, product_specificity, competitive_diff, keyword_integration, customer_scenario, emotional_resonance, factual_accuracy, platform_compliance, finish_integration, variety_score

Update:
1. Replace field definitions (all int, 0-10)
2. Update docstring: "Quality score across 10 dimensions" and composite denominator from 60 to 100
3. Update field_validator list to include all 10 new field names
4. Update composite property: sum all 10 fields, divide by 100 (not 60)
5. Keep factual_accuracy threshold logic in approval_status (unchanged)

**Part B: Update parse_candidate_response (generator.py)**

In parse_candidate_response(), update the Score construction to read new field names:

```python
self_score = Score(
    hook_quality=score_data.get("hook_quality", 5),
    product_specificity=score_data.get("product_specificity", 5),
    competitive_diff=score_data.get("competitive_diff", 5),
    keyword_integration=score_data.get("keyword_integration", 5),
    customer_scenario=score_data.get("customer_scenario", 5),
    emotional_resonance=score_data.get("emotional_resonance", 5),
    factual_accuracy=score_data.get("factual_accuracy", 5),
    platform_compliance=score_data.get("platform_compliance", 5),
    finish_integration=score_data.get("finish_integration", 5),
    variety_score=score_data.get("variety_score", 5),
)
```

**Part C: Fix 3 failing tests in test_pipeline.py**

1. `test_build_evidence_table_keyword_gaps_are_category_relevant_and_finish_excluded` (line ~238):
   - Root cause: monkeypatch lambda for `fetch_search_queries_for_master_sku` does not accept `limit` kwarg
   - Fix: Change lambda to accept `**kwargs`: `lambda master_sku, **kwargs: [...]`
   - Also update the monkeypatch for `format_search_queries_for_evidence` if its signature changed similarly

2. `test_generate_candidates_fetches_image_once_and_generates_n` (line ~963):
   - Root cause: Test provides only 3 mock responses but generate_candidates now calls generate 6 times (3 original + 3 retries because test titles are short enough to trigger _needs_title_retry)
   - Fix: Make test titles longer (>=60 chars each) to avoid triggering the title retry. The existing titles like "18-Inch Towel Bar, Skyline Collection, Solid Brass, Allied Brass" ARE 60+ chars, so the issue is likely that the mock is exhausted. Provide 6 mock responses OR make the titles clearly >60 chars. Alternatively, check if the issue is keyword alignment retry. The simplest fix: add enough mock responses (double the original 3) with identical content so retries succeed. But BETTER: ensure titles are clearly >= 60 chars so _needs_title_retry returns empty and no retry happens.
   - The assertion `assert llm.generate.call_count == 3` should stay at 3 if titles don't trigger retry. Check title lengths in the mock data carefully. "18-Inch Towel Bar, Skyline Collection, Solid Brass, Allied Brass" is 64 chars -- should NOT trigger retry. The retry might be from keyword alignment. Inspect `_needs_keyword_alignment_retry` -- if keyword_plan.enforce_alignment is True by default now, that's the cause. Fix: monkeypatch `is_segment_strategy_v1_enabled` to return False, or monkeypatch the keyword alignment check, or provide mock data that passes alignment.

3. `test_generate_candidates_skips_failed_attempts` (line ~1045):
   - Root cause: Title normalization via `trim_title_to_length` reorders components. Expected "Skyline Collection, Solid Brass" but got "Solid Brass, Skyline Collection"
   - Fix: Update the assertion to match the actual normalized title output. Read the title_normalization.py module to understand the new ordering, then update the expected string in the assertion.

After ALL changes, update any Score construction in test_pipeline.py that uses old field names to use new field names.

Search for ALL occurrences of `Score(specificity=` or `Score(` in test_pipeline.py and update them to use the 10 new fields.

**Part D: Create v1 path regression test (tests/test_v1_path_regression.py)**

Create a new test file `tests/test_v1_path_regression.py` that exercises the v1 production code path end-to-end with a mocked LLM provider. This covers success criterion 3 ("v1 path produces valid JSON output for a representative SKU").

The test must:

1. **Test build_core_prompt assembles correctly:**
   - Create a representative ParentSKU (reuse the sample_parent_sku fixture pattern from test_pipeline.py -- Towel Bar, Skyline collection, with variants)
   - Build evidence via `build_evidence_table(parent_sku)` and format via `format_evidence_markdown(evidence)`
   - Call `build_core_prompt(parent_sku, evidence, evidence_markdown, platform="google", content_type="title")`
   - Assert the returned prompt string is non-empty and contains expected sections: "Product Evidence Table", "Target platform: google", master_sku value
   - Monkeypatch any DB-calling functions (`fetch_master_sku_keywords`, `get_external_keywords`, `fetch_search_queries_for_master_sku`, `format_gold_standard_examples_bundle`, `get_category_guidance`) to return empty/stub data so no DB access is needed

2. **Test get_system_prompt returns valid prompt:**
   - Call `get_system_prompt()` (from `feedops.api.prompt_loader`)
   - Assert it returns a non-empty string
   - Assert it contains key XML tags from the rewritten SYSTEM_PROMPT (e.g., `<accuracy_guardrail>`, `<scoring_rubric>`)
   - Assert it contains skill content (at least one of: "Allied Brass", "brand", "finish")

3. **Test v1 output schema structure:**
   - The v1 path in main.py uses `build_core_prompt()` with a simple `{"content": "string"}` response schema (NOT CANDIDATE_SCHEMA)
   - Create a mock LLM provider that returns `{"content": "test content string"}`
   - Verify the response is valid JSON with a "content" key containing a string
   - This confirms the v1 path does NOT depend on CANDIDATE_SCHEMA or parse_candidate_response

4. **Test parse_candidate_response with new Score fields (defensive):**
   - Call `parse_candidate_response()` with a mock response containing the 10 new self_score field names
   - Assert the returned Candidate has a valid Score with all 10 fields populated (not defaults)
   - Call it again with OLD field names and verify it gracefully returns default scores (5 for all) -- confirming backward compatibility of the `.get()` fallback

Use `pytest` markers, `unittest.mock.patch` / `monkeypatch` for all external dependencies. No real API calls.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src .venv/bin/python -m pytest tests/test_pipeline.py tests/test_v1_path_regression.py -x --tb=short -q</automated>
    <manual>Verify all test_pipeline.py tests pass (including 3 previously-failing), and all v1 regression tests pass</manual>
  </verify>
  <done>
    - Score model has 10 fields matching CANDIDATE_SCHEMA (hook_quality, product_specificity, competitive_diff, keyword_integration, customer_scenario, emotional_resonance, factual_accuracy, platform_compliance, finish_integration, variety_score)
    - parse_candidate_response reads new field names
    - All 3 previously-failing tests pass
    - All other tests in test_pipeline.py continue to pass
    - tests/test_v1_path_regression.py exists and passes, exercising build_core_prompt, get_system_prompt, v1 output schema, and parse_candidate_response with both old and new field names
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite regression check + audit report</name>
  <files>
    .planning/phases/25.4-production-impact-audit/AUDIT-REPORT.md
  </files>
  <action>
**Part A: Run full test suite and confirm no regressions**

Run: `PYTHONPATH=./src .venv/bin/python -m pytest tests/ -v --tb=short`

Verify:
- The 3 pipeline tests now pass
- The v1 regression tests pass
- All 553+ previously-passing tests still pass
- The 8 environment/infrastructure failures (test_dashboard_approval_state_contract, test_env_parity, test_jobs, test_merchant_center) are unchanged -- these are pre-existing and NOT caused by v1.3a changes

**Part B: Write AUDIT-REPORT.md**

Create `.planning/phases/25.4-production-impact-audit/AUDIT-REPORT.md` with this structure:

```markdown
# Phase 25.4: Production Impact Audit Report

**Date:** 2026-02-24
**Auditor:** Claude (automated)
**Scope:** All source code changes from Phases 23-25.3 affecting the v1 production code path
**Key Finding:** All changes are already deployed to production on master. The audit branch (v1.3a/prompt-rewrite-validation) contains only documentation commits.

## Executive Summary

[1-2 paragraph summary: 24+ commits, all on master, feature flag correctly gates v2 code, 3 shared-layer changes affect all API calls (reasoning_effort, json_schema, max_tokens), Score model/parser mismatch was latent debt (not exercised by v1 production path), now fixed]

## Change Classification

### HIGH RISK (v1 code path modified)

| File | Change | v1 Impact | Disposition |
|------|--------|-----------|-------------|
| openai_provider.py | reasoning_effort defaults to "high" (was absent/None) | ALL API calls now use reasoning mode — changes output quality, cost, latency | KEEP — intentional bug fix (Phase 23, commit a7a285d8). Reasoning was broken before (env var unset = no reasoning sent). GPT-5.2 defaults to zero reasoning without this. |
| openai_provider.py | max_completion_tokens default 2000 → 8000 | ALL API calls allow 4x more output tokens — higher cost ceiling | KEEP — prevents truncation of long descriptions. Already deployed and operational for weeks. |
| openai_provider.py | response_format json_object → strict json_schema | ALL API calls use strict JSON schemas — changes LLM behavior fundamentally | KEEP — more reliable JSON output, eliminates retry loops from malformed responses. Already deployed. |
| openai_provider.py | temperature no longer sent when reasoning_effort set | ALL API calls drop temperature=0.7 when reasoning is active | KEEP — fixes mutual exclusion bug. temperature and reasoning_effort are mutually exclusive on GPT-5.2. |
| openai_provider.py | prompt_cache_retention: "24h" added | ALL API calls request extended cache retention | KEEP — beneficial optimization, no regression risk. Cache expires in 5-10 min without this. |
| prompts.py CANDIDATE_SCHEMA | self_score fields renamed (6 old → 10 new) | parse_candidate_response() was reading old field names, getting default 5 for all | FIXED — Score model + parser aligned to 10 new fields in this audit. Note: NOT exercised by v1 production path in main.py (which uses build_core_prompt with simple {"content": string} schema). |
| prompts.py SYSTEM_PROMPT | Completely rewritten to XML-tagged creative brief | v1 path uses this directly via get_system_prompt() — content quality characteristics changed | KEEP — intentional quality improvement (Phase 24). Replaced compliance-style prompt with creative brief structure. |
| prompts.py shopify_meta_description | maxLength 155 → 160 | v1 schema allows slightly longer Shopify meta descriptions | KEEP — minor, aligns with current SEO best practice (160 chars). |
| prompt_builder.py build_core_prompt | Added product_design_story, competitive_positioning, enhanced keyword section | v1 path user prompt structure changed — includes richer product context | KEEP — intentional improvement providing GPT-5.2 more product context for better content generation. |
| prompt_loader.py get_system_prompt | Now loads all 8 skills into system prompt | v1 system prompt is much larger (~40K+ chars with skills) — higher token cost per call | KEEP — intentional improvement. Skills provide domain knowledge (brand voice, finish expertise, category hooks) that improve content quality. Cost increase is offset by prompt caching. |

### MEDIUM RISK (v1 adjacent)

| File | Change | v1 Impact | Disposition |
|------|--------|-----------|-------------|
| evidence.py sanitize_catalog_prose | Enhanced sanitization — strips more noise from catalog prose before injection into prompt | v1 path calls this via build_core_prompt() when formatting evidence for google/bing | KEEP — stricter sanitization is an improvement. Removes metadata labels and raw query rows from customer-facing generation context. |
| evidence.py sanitize_prompt_text | Enhanced sanitization of prompt text inputs | v1 path uses this indirectly through evidence formatting | KEEP — improvement, reduces banned-word leakage from evidence into generated content. |
| evidence.py format_evidence_markdown | Added `for_customer_copy` parameter (default=False) | v1 path calls without parameter in generator.py (gets old behavior). build_core_prompt calls with for_customer_copy=True for google/bing. | KEEP — additive parameter with safe default. The for_customer_copy=True path in build_core_prompt is an intentional improvement to filter evidence for customer-facing content. |
| generator.py generate_per_platform | New function with v1 fallback path inside | v1 fallback correctly delegates to build_split_prompt() when prompt_version="v1" | KEEP — feature-flag gated. The v1 fallback inside generate_per_platform is only reached if someone explicitly calls generate_per_platform with prompt_version="v1". The main.py v1 path does NOT call this function — it calls build_core_prompt() directly. |
| prompt_builder.py build_core_prompt enhancements | Added product_design_story extraction from parent_sku data, competitive_positioning block, enhanced keyword section with placement plan | v1 path user prompt now includes richer context — product narrative copy, bullet points extracted as "design story", and competitive context | KEEP — intentional improvement. Provides GPT-5.2 with product-specific narrative data for more compelling content generation. |
| prompt_loader.py get_system_prompt skill loading | Now calls load_skills_for_prompt() which loads all 8 SKILL.md files from .claude/skills/ and appends them to SYSTEM_PROMPT | v1 system prompt token count increased significantly (~40K+ chars with all 8 skills). Higher cost per API call but better content quality. | KEEP — intentional improvement. Skills contain essential domain knowledge (28 finish descriptions, brand voice rules, category hooks, gold standard patterns). Prompt caching mitigates the cost increase across batch runs. |

### LOW RISK (additive only)

These changes add new code that is NOT imported or called by the v1 code path:

- `skill_loader.py`: New `get_platform_system_prompt(platform)` and sanitizer functions — v2 only
- `prompts.py`: New GOOGLE_SCHEMA, BING_SCHEMA, SHOPIFY_SCHEMA, FINISH_SENTENCES_SCHEMA constants — v2 only
- `prompts.py`: New GOOGLE_BRIEF, BING_BRIEF, SHOPIFY_BRIEF, FINISH_BRIEF constants — v2 only
- `prompt_builder.py`: New `build_google_prompt()`, `build_bing_prompt()`, `build_shopify_prompt()`, `build_finish_prompt()` — v2 only
- New test files for v2 harness and per-platform validation — no production impact

## Test Suite Status

### Before Fix
- 553 passing, 11 failing, 1 skipped
- 3 pipeline failures: evidence kwarg mismatch, title retry count doubled, title normalization reordering
- 8 pre-existing failures: environment/infrastructure (test_dashboard_approval_state_contract, test_env_parity, test_jobs x2, test_merchant_center x3, plus 1 other)

### After Fix
- [N] passing, [M] failing, 1 skipped (fill in actual numbers from test run)
- 3 pipeline failures: RESOLVED
- New v1 regression tests: PASSING
- 8 pre-existing failures: UNCHANGED (not v1.3a-related)

### Fix Details

1. **test_build_evidence_table_keyword_gaps_are_category_relevant_and_finish_excluded**
   - Root cause: `fetch_search_queries_for_master_sku` gained a `limit` keyword argument; monkeypatch lambda did not accept it
   - Fix: Updated lambda to accept `**kwargs`

2. **test_generate_candidates_fetches_image_once_and_generates_n**
   - Root cause: generate_candidates now triggers title-length or keyword-alignment retry, doubling LLM calls from 3 to 6
   - Fix: [describe actual fix — either longer titles, monkeypatched retry, or adjusted assertion]

3. **test_generate_candidates_skips_failed_attempts**
   - Root cause: `trim_title_to_length` reorders title components ("Solid Brass, Skyline Collection" instead of "Skyline Collection, Solid Brass")
   - Fix: Updated assertion to match actual normalized title output

## v1 Path Regression Test

New file: `tests/test_v1_path_regression.py`

Tests added:
1. `test_build_core_prompt_assembles_correctly` — Verifies build_core_prompt returns non-empty string with expected sections (evidence table, platform, master_sku)
2. `test_get_system_prompt_returns_valid_prompt` — Verifies system prompt contains XML tags and skill content
3. `test_v1_output_schema_is_simple_content_string` — Verifies v1 path uses {"content": string} schema, NOT CANDIDATE_SCHEMA
4. `test_parse_candidate_response_new_fields` — Verifies Score model accepts 10 new field names
5. `test_parse_candidate_response_old_fields_fallback` — Verifies graceful fallback to defaults when old field names provided

## Feature Flag Verification

The `FEEDOPS_PROMPT_VERSION` feature flag correctly gates all v2 code:
- Default: "v1" (when env var unset)
- Location: `main.py::_get_prompt_version()`
- v2 code paths in: `main.py` (optimize-sku, regenerate), `generator.py` (generate_per_platform), hybrid_generation.py
- v1 path uses: `build_core_prompt()` with simple {"content": string} schema — does NOT use CANDIDATE_SCHEMA or parse_candidate_response

## Recommendations

1. **Monitor production costs** — reasoning_effort="high" + max_tokens=8000 may increase OpenAI spend. Check usage dashboard for cost changes since Phase 23 deployment.
2. **The 8 pre-existing test failures** should be triaged separately (test_env_parity, test_merchant_center, etc.) — they are not caused by v1.3a changes.
3. **CANDIDATE_SCHEMA is only used by generate_candidates()** — not the v1 production path in main.py. The Score model fix is defensive (aligns code that isn't currently exercised by production).
4. **Skill loading cost** — The v1 system prompt now includes all 8 skills (~40K+ chars). This is mitigated by OpenAI prompt caching but should be monitored. If cost is a concern, the mode="single" parameter in get_system_prompt() could be used to load fewer skills for single-SKU regeneration (currently unused).
```

Fill in the `[N]` and `[M]` placeholders with actual test counts from the Part A test run. Fill in the `[describe actual fix]` placeholder in Fix Details item 2 with the specific fix applied in Task 1.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src .venv/bin/python -m pytest tests/ --tb=short -q 2>&1 | tail -5</automated>
    <manual>Review AUDIT-REPORT.md for completeness — verify every HIGH and MEDIUM risk row has a specific disposition, and all 3 test fixes are documented with root cause</manual>
  </verify>
  <done>
    - Full test suite run confirms no regressions from Task 1 changes
    - AUDIT-REPORT.md exists with complete classification of all changes
    - Every HIGH-risk change has a specific disposition with rationale (keep/fix/revert)
    - Every MEDIUM-risk change has a specific disposition with rationale
    - The 3 test fixes are documented with root cause analysis
    - Feature flag verification section confirms v2 is properly gated
    - v1 path regression test results documented
  </done>
</task>

</tasks>

<verification>
1. `PYTHONPATH=./src .venv/bin/python -m pytest tests/test_pipeline.py -x --tb=short -q` — all pipeline tests pass
2. `PYTHONPATH=./src .venv/bin/python -m pytest tests/test_v1_path_regression.py -x --tb=short -q` — all v1 regression tests pass
3. `PYTHONPATH=./src .venv/bin/python -m pytest tests/ --tb=short -q` — full suite shows same or fewer failures than before
4. `cat .planning/phases/25.4-production-impact-audit/AUDIT-REPORT.md` — audit report exists and covers all HIGH/MEDIUM risk changes with specific dispositions
5. `grep "hook_quality" src/feedops/models/score.py` — Score model uses new field names
6. `grep "hook_quality" src/feedops/pipeline/generator.py` — parser reads new field names
7. `grep "build_core_prompt" tests/test_v1_path_regression.py` — v1 path regression test exercises the production code path
</verification>

<success_criteria>
- Score model has 10 fields aligned with CANDIDATE_SCHEMA
- parse_candidate_response reads 10 new field names
- 3 previously-failing pipeline tests now pass
- 553+ previously-passing tests still pass
- tests/test_v1_path_regression.py exercises the v1 code path (build_core_prompt, get_system_prompt, output schema)
- AUDIT-REPORT.md documents all HIGH and MEDIUM changes with specific risk classification and disposition
- No v2 code paths were modified (audit scope is v1 only)
</success_criteria>

<output>
After completion, create `.planning/phases/25.4-production-impact-audit/25.4-01-SUMMARY.md`
</output>
