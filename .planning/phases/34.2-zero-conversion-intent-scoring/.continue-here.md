# Phase 34.2 — Continuation State

## Status: Plans 01-05 COMPLETE, Plan 06 checkpoint pending, UI fixes in progress

## What's Done
- Plans 34.2-01 through 34.2-05: All executed and committed
- Plan 34.2-06: Tasks 1-2 complete (calibration + threshold application), Task 3 (verification) awaiting continuation
- **UI Bug Fix 1** (commit 9299e06c): Synced wasted spend classification with calibrated $96.33 threshold (was hardcoded $5)
- **UI Bug Fix 2** (commit cf097a0e): Action Queue now filters to trigger-actionable terms only; TermScorecard uses targetTier

## What's Remaining

### 1. Complete Plan 34.2-06 checkpoint
- Task 3 (human verification) was approved but continuation agent never spawned
- Need to spawn continuation agent to create SUMMARY.md and update STATE/ROADMAP
- Or manually create the SUMMARY and run `gsd-tools roadmap update-plan-progress`

### 2. Code review agent findings
- A code review agent was spawned (background) to audit ALL old vs new scoring code
- Agent ID: a664f0fae0cc161b2
- Output: /private/tmp/claude-501/-Users-bobby-Documents-GitHub-Allied-FeedOps/tasks/a664f0fae0cc161b2.output
- CHECK THIS OUTPUT for remaining issues to fix

### 3. Known remaining issues (from browser inspection)
- **Explorer tab** still uses old `isMisplaced` + `recommendedTier` — this is OK (it's the deep-dive analysis view)
- **Revenue Leakage tab** still shows ALL classified terms (misplaced + triggered) — may need filtering too
- **`useRecommendations` batch approve** (line 248) still sends `recommendedTier` not `targetTier`
- **TermScorecard `buildFactors()`** still references `recommendedTier` for ROAS Position label
- **Intent score breakdown** not shown in TermScorecard detail view (new data exists but UI doesn't display it)
- **Behavioral signals** not shown in TermScorecard detail view

### 4. Phase verification
- After all fixes, run `/gsd:execute-phase 34.2` to resume (it will skip completed plans)
- Or manually run the verification step from the execute-phase workflow

## Key Architecture Decision
The OLD system (`isMisplaced`, `recommendedTier`, fit-score z-scores) and NEW system (`trigger`, `targetTier`, `determineAction()`) coexist:
- **Action Queue**: NEW system only (trigger-based, actionable terms)
- **Explorer**: OLD system (deep-dive, all terms, fit-score analysis)
- **Revenue Leakage**: Both (classified terms from either system)
- **Detail view (TermScorecard)**: Should prefer NEW system fields when available

## Code Review Findings (from review agent)

### CRITICAL — Fix First
1. **`useRecommendations.ts:248`** — `batchApprove` sends `recommendedTier` not `targetTier` to DB
2. **`tier-scoring.ts:458`** — Hardcoded $5M micros in `estimateImpact()` (should use calibrated $96.33)
3. **`reason-codes.ts:28,47`** — $5 fallback threshold contradicts calibrated $96.33

### IMPORTANT — Display/UX
4. **`TermScorecard.tsx`** — PARTIALLY FIXED (movement arrow + badge). Still missing: intent score breakdown section, behavioral signals section, trigger context in buildFactors()
5. **`MisplacedTermRow.tsx`** — Explorer tab component, uses only old `recommendedTier` (OK for Explorer)
6. **`tier-scoring.ts:533-547`** — `buildHeroCallout()` counts only `isMisplaced`, misses trigger-only terms
7. **`page.tsx` HeroSummary** — FIXED (now passes `actionableTerms.length`)
8. **`LeakageTermRow.tsx:298-300`** — Expanded detail shows `recommendedTier` fit score, not `targetTier`

### SUGGESTIONS — Cleanup
9. `RecommendedAction` type missing `global_block`/`funnel`
10. `trigger` typed as `string` not union type
11. New fields (`targetTier`, `trigger`, etc.) optional but always populated by `scoreTerm()`
12. `classifyAllTerms()` hybrid filter (isMisplaced OR trigger) creates noise — PARTIALLY FIXED (Action Queue filters on trigger only, but Revenue Leakage still uses hybrid)
13. Only 3 reason codes; loses trigger granularity for badges
14. `batch_approve` API doesn't persist trigger/targetTier/intentScore
15. Naming confusion: payload uses `recommendedTier` field name for what's actually `targetTier`

## Commits This Session
- 9b68ee7d: feat(34.2-01): rename constrain to demote
- b31c8eba: feat(34.2-01): update UI components
- ad6f8d89: docs(34.2-01): complete plan
- d1f97899: docs(34.2-02): audit Google Ads account
- 7ea7dc37: docs(34.2-02): complete plan
- a7ebcea6: feat(34.2-03): attribute extractor + TF-IDF
- 37dc8a93: feat(34.2-03): IntentScorer + /score-intent endpoint
- a00c1471: docs(34.2-03): complete plan
- 88bbc46d: feat(34.2-04): GAQL extension + behavioral types
- f543f506: feat(34.2-04): computeBehavioralIntent
- c855e3dd: docs(34.2-04): complete plan
- eb174ef6: feat(34.2-05): unified intent + 5-trigger determineAction
- 7ce458a0: feat(34.2-05): wire Cloud Run + intent badges
- ab90b8ae: docs(34.2-05): complete plan
- b80a87fd: docs(34.2-06): score historical terms + calibrate
- ec224df6: feat(34.2-06): apply calibrated thresholds
- 9299e06c: fix(34.2): sync wasted spend classification with calibrated threshold
- cf097a0e: fix(34.2): Action Queue shows only trigger-actionable terms
