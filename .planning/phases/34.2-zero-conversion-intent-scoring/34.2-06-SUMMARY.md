---
phase: 34.2-zero-conversion-intent-scoring
plan: 06
status: complete
started: 2026-02-26
completed: 2026-02-26
commits:
  - b80a87fd: "docs(34.2-06): score historical terms + calibrate"
  - ec224df6: "feat(34.2-06): apply calibrated thresholds"
---

# Plan 34.2-06 Summary: Calibrate Intent Score Thresholds

## What Was Built

Calibrated all intent scoring thresholds against real Google Ads account data (90-day audit) and fixed remaining bugs from code review.

### Task 1: Historical Term Scoring & Calibration (Complete)
- Scored 1000+ historical search terms against ROAS outcomes
- Validated 0.65 intent threshold, 0.55/0.45 feed/behavioral split
- Real avgCPA = $64.22 → wasted spend threshold = $96.33 (1.5x)
- Documented in `docs/analysis/intent-score-calibration.md`

### Task 2: Apply Calibrated Thresholds (Complete)
- Added `avgCPA`, `minIntentScore`, `feedAlignmentWeight`, `minRCTR`, `minQueryWords` to CalibrationConfig
- `determineAction()` uses `config.avgCPA * 1.5` instead of hardcoded $5
- All thresholds centralized in CalibrationConfig with source documentation

### Task 3: Human Verification (Approved)
- User approved complete pipeline end-to-end

### Post-Plan Bug Fixes (This Session)
From code review findings:
1. **CRITICAL**: `batchApprove` now sends `targetTier ?? recommendedTier` (was sending wrong tier to DB)
2. **CRITICAL**: `estimateImpact()` wasted spend threshold uses `config.avgCPA * 1.5` (was hardcoded $5M micros)
3. **CRITICAL**: `reason-codes.ts` fallback uses $96.33 threshold (was $5 contradicting calibration)
4. **IMPORTANT**: TermScorecard shows Feed Alignment + Behavioral Intent factors when intent data available
5. **IMPORTANT**: TermScorecard shows Behavioral Signals card (rCTR, CPC ceiling, micro-conversions, composite)
6. **IMPORTANT**: `buildHeroCallout()` counts all actionable terms (isMisplaced OR trigger-based)
7. **FIX**: TermScorecard Tier Fit Comparison uses `targetTier` over `recommendedTier`
8. **FIX**: Test CalibrationConfig objects include all required fields (3 TS errors resolved)

## Decisions
- Wasted spend threshold: $96.33 (1.5x avgCPA) replaces hardcoded $5 everywhere
- TermScorecard prefers `targetTier` (sequential trigger) over `recommendedTier` (statistical best-fit)
- Behavioral signals card only renders when data available (graceful degradation)

## Files Modified
- `dashboard/src/app/(dashboard)/tier-scoring/hooks/useRecommendations.ts` — batchApprove tier fix
- `dashboard/src/lib/optimization/tier-scoring.ts` — estimateImpact threshold + buildHeroCallout counting
- `dashboard/src/app/(dashboard)/tier-scoring/lib/reason-codes.ts` — fallback threshold alignment
- `dashboard/src/app/(dashboard)/tier-scoring/components/TermScorecard.tsx` — intent + behavioral UI
- `dashboard/src/lib/optimization/__tests__/tier-scoring.test.ts` — CalibrationConfig test fixes
