---
phase: 34.2-zero-conversion-intent-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/lib/optimization/tier-scoring.types.ts
  - dashboard/src/lib/optimization/tier-scoring.ts
  - dashboard/src/lib/optimization/__tests__/tier-scoring.test.ts
  - dashboard/src/lib/optimization/control-center.ts
  - dashboard/src/lib/optimization/__tests__/control-center.test.ts
  - dashboard/src/app/(dashboard)/tier-scoring/components/ActionQueueRow.tsx
  - dashboard/src/app/(dashboard)/tier-scoring/components/LeakageTermRow.tsx
  - dashboard/src/app/(dashboard)/tier-scoring/lib/reason-codes.ts
  - dashboard/src/app/api/monitoring/snapshot-capture/route.ts
autonomous: true
requirements:
  - INTENT-01
  - INTENT-05

must_haves:
  truths:
    - "The word 'constrain' does not appear anywhere in the codebase (types, logic, UI, tests)"
    - "TermScore has a targetTier field that determineAction() populates"
    - "TierMovementArrow uses targetTier (not recommendedTier) for arrow direction"
    - "Action buttons show 'Demote' instead of 'Constrain' with target tier labels"
  artifacts:
    - path: "dashboard/src/lib/optimization/tier-scoring.types.ts"
      provides: "RecommendedAction type with 'demote' instead of 'constrain'"
      contains: "demote"
    - path: "dashboard/src/lib/optimization/tier-scoring.ts"
      provides: "determineAction returns targetTier and uses 'demote'"
      contains: "targetTier"
    - path: "dashboard/src/app/(dashboard)/tier-scoring/components/ActionQueueRow.tsx"
      provides: "Demote button with target tier label"
      contains: "Demote"
  key_links:
    - from: "tier-scoring.ts determineAction()"
      to: "tier-scoring.types.ts RecommendedAction"
      via: "return type uses 'demote' not 'constrain'"
      pattern: "action: 'demote'"
    - from: "ActionQueueRow.tsx"
      to: "TermScore.targetTier"
      via: "button label shows target tier"
      pattern: "targetTier"
---

<objective>
Eradicate "constrain" terminology and add targetTier to TermScore type across the entire codebase. This is the foundational cleanup that unblocks all subsequent plans.

Purpose: The word "constrain" is domain-incorrect — the correct term is "demote" (pull UP the funnel to restrict bidding). This also adds the targetTier field so UI components can show where a term is going, not just what action to take.
Output: Zero occurrences of "constrain" in codebase; TermScore.targetTier populated by determineAction()
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key types the executor needs -->

From dashboard/src/lib/optimization/tier-scoring.types.ts:
```typescript
export type RecommendedAction = 'promote' | 'constrain' | 'block' | 'observe'
// CHANGE TO: 'promote' | 'demote' | 'block' | 'observe'

export interface TermScore {
  // ... existing fields ...
  recommendedAction?: RecommendedAction
  actionReason?: string
  // ADD: targetTier?: FunnelTier  (the tier determineAction wants to move to)
}
```

From dashboard/src/lib/optimization/tier-scoring.ts (determineAction at line 487):
```typescript
function determineAction(
  currentTier, currentTierDist, termRoas, totalConversions, totalCostMicros, isMisplaced
): { action: RecommendedAction; targetTier: FunnelTier }
// Already returns targetTier internally, but uses 'constrain' — change to 'demote'
```

Files with "constrain" occurrences (40 total across 9 files):
- tier-scoring.types.ts (1)
- tier-scoring.ts (10)
- tier-scoring.test.ts (21)
- control-center.ts (2)
- control-center.test.ts (1)
- ActionQueueRow.tsx (1) — handleConstrain function
- LeakageTermRow.tsx (2)
- reason-codes.ts (1) — comment only
- snapshot-capture/route.ts (1) — comment only
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename constrain to demote in types, scoring engine, and tests</name>
  <files>
    dashboard/src/lib/optimization/tier-scoring.types.ts
    dashboard/src/lib/optimization/tier-scoring.ts
    dashboard/src/lib/optimization/__tests__/tier-scoring.test.ts
    dashboard/src/lib/optimization/control-center.ts
    dashboard/src/lib/optimization/__tests__/control-center.test.ts
    dashboard/src/app/(dashboard)/tier-scoring/lib/reason-codes.ts
    dashboard/src/app/api/monitoring/snapshot-capture/route.ts
  </files>
  <action>
    1. In `tier-scoring.types.ts`:
       - Change `RecommendedAction` type from `'promote' | 'constrain' | 'block' | 'observe'` to `'promote' | 'demote' | 'block' | 'observe'`
       - Add `targetTier?: FunnelTier` field to `TermScore` interface (optional to avoid breaking consumers)

    2. In `tier-scoring.ts`:
       - Replace all `'constrain'` string literals with `'demote'` (10 occurrences)
       - In `determineAction()`: the function already returns `{ action, targetTier }` — just change `'constrain'` to `'demote'`
       - In `scoreTerm()` or wherever `determineAction()` result is consumed: assign `targetTier` to the TermScore output
       - In comments: replace "constrain" with "demote" where it refers to the action

    3. In `tier-scoring.test.ts`:
       - Replace all `'constrain'` assertions with `'demote'` (21 occurrences)
       - Verify test descriptions use "demote" language

    4. In `control-center.ts`:
       - Replace `'constrain'` with `'demote'` (2 occurrences)

    5. In `control-center.test.ts`:
       - Replace `'constrain'` with `'demote'` (1 occurrence)

    6. In `reason-codes.ts`:
       - Update comment that says "constrained tier" to "restricted tier" or "demoted tier"

    7. In `snapshot-capture/route.ts`:
       - Update comment that mentions "constrain" to use "demote"

    Do NOT change `handleConstrain` function name in ActionQueueRow yet — that's Task 2.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npx vitest run src/lib/optimization/__tests__/tier-scoring.test.ts src/lib/optimization/__tests__/control-center.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>RecommendedAction type uses 'demote' not 'constrain'. All tests pass with 'demote'. TermScore has targetTier field.</done>
</task>

<task type="auto">
  <name>Task 2: Update UI components — Demote button, targetTier in arrows</name>
  <files>
    dashboard/src/app/(dashboard)/tier-scoring/components/ActionQueueRow.tsx
    dashboard/src/app/(dashboard)/tier-scoring/components/LeakageTermRow.tsx
    dashboard/src/app/(dashboard)/tier-scoring/components/TierMovementArrow.tsx
  </files>
  <action>
    1. In `ActionQueueRow.tsx`:
       - Rename `handleConstrain` to `handleDemote`
       - Change the button label from "Constrain" to "Demote to {targetTier}" where targetTier comes from `term.targetTier`
       - If `term.targetTier` is available, use it; otherwise fall back to 'HIGH' for wasted spend demotions
       - Update the `ArrowDown` icon usage to show correct tier label

    2. In `LeakageTermRow.tsx`:
       - Replace any "Constrain" button text with "Demote"
       - Use `term.targetTier` for the target tier label on buttons
       - Ensure wasted spend terms show "Block" (HIGH tier) or "Demote to HIGH" (MEDIUM/LOW tier) buttons

    3. In `TierMovementArrow.tsx`:
       - The component currently takes `current` and `recommended` props
       - Add an optional `targetTier` prop
       - If `targetTier` is provided, use it instead of `recommended` for arrow direction
       - This ensures the arrow points correctly based on the actual target tier from determineAction()
       - Keep backward compatibility — if targetTier is undefined, use recommended

    4. In both Row components: where TierMovementArrow is rendered, pass `targetTier={term.targetTier}` prop

    IMPORTANT: The Constrain button at HIGH tier guard (FIX-05) should now be a Demote button guard — demoting from HIGH is a no-op, so hide the Demote button when currentTier === 'HIGH' (unless it's a block action).
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npx tsc --noEmit 2>&1 | tail -20 && grep -rn "constrain" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".test." || echo "No constrain found in source (good)"</automated>
  </verify>
  <done>"Constrain" does not appear in any non-test source file. UI buttons show "Demote to {tier}" with correct target. TierMovementArrow uses targetTier. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification — zero constrain occurrences, build passes</name>
  <files></files>
  <action>
    1. Run a comprehensive grep for "constrain" across the entire dashboard/src directory (all .ts and .tsx files)
    2. The ONLY acceptable occurrences are in comments explaining the old terminology (e.g., "formerly called constrain") — and even those should be reworded
    3. Run `npm run build` to verify the full Next.js build passes
    4. Run `npm run lint` and fix any issues
    5. Run all tests: `npx vitest run` to ensure nothing is broken

    If any "constrain" occurrences remain, fix them. This is a zero-tolerance task.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && grep -rn "constrain" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | wc -l && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Zero occurrences of "constrain" in dashboard/src. Build passes. Lint passes. All tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -rn "constrain" dashboard/src/ --include="*.ts" --include="*.tsx" | grep -v node_modules` returns 0 results
2. `cd dashboard && npm run build` exits 0
3. `cd dashboard && npx vitest run` — all tests pass
4. TermScore type has `targetTier?: FunnelTier` field
5. RecommendedAction type is `'promote' | 'demote' | 'block' | 'observe'`
</verification>

<success_criteria>
- The word "constrain" appears zero times in all TypeScript/TSX files under dashboard/src
- All existing tests pass with "demote" replacing "constrain"
- TermScore.targetTier is populated by determineAction()
- UI buttons show "Demote to {tier}" with correct target tier labels
- TierMovementArrow uses targetTier for arrow direction
- Full build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/34.2-zero-conversion-intent-scoring/34.2-01-SUMMARY.md`
</output>
