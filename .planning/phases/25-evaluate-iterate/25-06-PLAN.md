---
phase: 25-evaluate-iterate
plan: 06
type: execute
wave: 4
depends_on: ["25-05"]
files_modified:
  - .claude/skills/allied-brass-brand-expert/SKILL.md
  - .claude/skills/google-shopping-content/SKILL.md
  - .claude/skills/product-storytelling/SKILL.md
  - src/feedops/pipeline/prompts.py
  - src/feedops/api/prompt_builder.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
gap_closure: true

must_haves:
  truths:
    - "allied-brass-brand-expert skill contains NO references to die-cast zinc, plated alternatives, or competitor material names — only positive differentiators"
    - "google-shopping-content skill codifies Robert's title formula: finish first, collection keyword, dimensions only when varying by size, style references, solid brass not in prime position"
    - "product-storytelling skill has explicit evidence exclusion rules: no weight capacity, no detailed dimensions, no installation specifics in descriptions"
    - "SYSTEM_PROMPT accuracy_guardrail prohibits weight capacity, detailed dimensions, keyword lists, competitor materials, heritage bathroom fixtures, and 28 finishes in variant-expandable content"
    - "prompt_builder.py suppresses 28 finishes for ALL Google/Bing content (not just when finish_code is passed)"
    - "Product type data for SKUs 1024 and 1020 has been investigated and corrected if wrong"
  artifacts:
    - path: ".claude/skills/allied-brass-brand-expert/SKILL.md"
      provides: "Updated brand expert skill with competitor material prohibition"
      contains: "Never mention die-cast zinc"
    - path: ".claude/skills/google-shopping-content/SKILL.md"
      provides: "Updated shopping skill with Robert's title formula"
      contains: "Finish name FIRST"
    - path: ".claude/skills/product-storytelling/SKILL.md"
      provides: "Updated storytelling skill with evidence exclusion rules"
      contains: "weight capacity"
    - path: "src/feedops/pipeline/prompts.py"
      provides: "Updated SYSTEM_PROMPT with content prohibition rules"
      contains: "Do NOT include weight capacity"
    - path: "src/feedops/api/prompt_builder.py"
      provides: "Updated competitive positioning to suppress 28 finishes universally"
  key_links:
    - from: ".claude/skills/allied-brass-brand-expert/SKILL.md"
      to: "src/feedops/pipeline/skill_loader.py"
      via: "skill_loader reads SKILL.md and injects into system prompt"
      pattern: "brand-expert"
    - from: "src/feedops/pipeline/prompts.py"
      to: "src/feedops/api/prompt_loader.py"
      via: "prompt_loader uses SYSTEM_PROMPT as base"
      pattern: "SYSTEM_PROMPT"
---

<objective>
Apply all Round 2 evaluation fixes: update three skills to remove anti-patterns and codify expert domain knowledge, add content prohibition rules to SYSTEM_PROMPT, suppress "28 finishes" universally for Google/Bing, and investigate/fix product type data for misidentified SKUs.

Purpose: Round 2 evaluation identified specific, actionable issues — competitor material naming in skills (6/10 SKUs affected), missing title formula from Robert's expert feedback (5/10 titles lost), evidence data inclusion problems (8/10 SKUs with excessive technical detail), and product type misidentification (2 SKUs). These are content rule and data fixes, not architectural changes.

Output: Updated skills, updated SYSTEM_PROMPT, updated prompt_builder, verified product type data.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-evaluate-iterate/25-02-evaluation-results.md
@.planning/phases/25-evaluate-iterate/25-04-SUMMARY.md
@.claude/skills/allied-brass-brand-expert/SKILL.md
@.claude/skills/google-shopping-content/SKILL.md
@.claude/skills/product-storytelling/SKILL.md
@src/feedops/pipeline/prompts.py
@src/feedops/api/prompt_builder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update three skills with Round 2 evaluation feedback</name>
  <files>
    .claude/skills/allied-brass-brand-expert/SKILL.md
    .claude/skills/google-shopping-content/SKILL.md
    .claude/skills/product-storytelling/SKILL.md
  </files>
  <action>
**Skill 1: `allied-brass-brand-expert/SKILL.md` — Remove competitor material references**

The evaluation report (Issue A) shows both evaluators strongly rejected "die-cast zinc" and "plated alternatives" language. 6/10 SKUs were affected. Robert said: "I do not like the idea of 'common die-cast zinc' which I believe is not good for us."

Changes to make:
1. In `### 1. Solid Brass Construction` section:
   - Remove the sentence "Most competitors at this price tier use zinc alloy (zamak) with a plated finish."
   - Remove the paragraph starting "Zinc is brittle. It cracks under daily stress..."
   - Remove the `**Competitor contrast** (never name competitors — contrast the MATERIAL):` block entirely (it says "die-cast zinc")
   - Replace with a NEW positive-only competitor contrast section:
     ```
     **Positive differentiation (NEVER name competitor materials):**
     "Solid brass won't corrode, pit, or tarnish — even in the steamiest bathroom. It bends instead of breaking under daily stress, and develops character over decades instead of deteriorating."

     CRITICAL PROHIBITION: NEVER mention "die-cast zinc," "zinc alloy," "zamak," "plated alternatives," "chrome-plated steel," or any specific competitor material by name. Naming inferior materials risks Allied Brass appearing in searches for those cheap alternatives. Always frame solid brass POSITIVELY — what it IS and what it DOES — not what it's better than.
     ```

2. In `### 2. 28+ Designer Finishes` section:
   - Remove the competitor comparison table (Kohler, Moen, Delta, Kingston Brass)
   - Remove the `**Competitor contrast**:` block that says "Most brands offer 6-8 finishes"
   - Replace with a positive-only frame:
     ```
     **How to express finish variety positively:**
     "28 finishes — from timeless Polished Chrome to statement-making Mediterranean Blue — ensures every piece in your bathroom speaks the same design language. Choose freely; coordinate perfectly."

     Focus on what Allied Brass ENABLES (perfect coordination, self-expression, no compromises) — not on what competitors lack.
     ```

3. In `### How to express it` under Solid Brass Construction:
   - Remove the "NO" examples that reference zinc or competitors
   - Keep only the "YES" examples

4. In `## Marketing Psychology Principles` > `### Loss Aversion (Gentle)`:
   - Remove "Every day with mismatched hardware is a day your bathroom doesn't feel finished" (too negative)
   - Keep "Solid brass means never wondering if the finish will hold up in humidity" (positive)

5. In `### By Product Category` hooks:
   - In the Cabinet Hardware hook: Remove "Solid brass means the feel matches the look" and replace with something that doesn't imply other materials feel wrong
   - Review all hooks for any implicit competitor material comparisons

6. Add a new top-level section right after `## Foundation: The One-Two Punch`:
   ```
   ## CRITICAL: Competitor Material Prohibition

   NEVER mention competitor materials by name in ANY content:
   - Banned terms: "die-cast zinc," "zinc alloy," "zamak," "plated alternatives," "chrome-plated steel," "plastic cores," "hollow zinc," "plated zinc"
   - Naming cheap materials risks Allied Brass appearing in searches for those materials
   - Always frame solid brass POSITIVELY: what it is, what it does, how it performs
   - WRONG: "Unlike die-cast zinc alternatives that corrode..."
   - RIGHT: "Solid brass won't corrode, pit, or tarnish — built to outlast the renovation"
   ```

**Skill 2: `google-shopping-content/SKILL.md` — Add Robert's title formula**

The evaluation report (Issue H) documents Robert's expert title preferences. 5/10 titles lost due to missing formula.

Changes to make:
1. In `## Title Architecture` > `### Flexible Framework`, REPLACE the current priority list with Robert's formula:
   ```
   ### Robert's Title Formula (Expert Domain Knowledge)

   Robert (company founder, original description author) provided specific title structure rules based on decades of selling Allied Brass products:

   1. **Finish name FIRST** — "{FINISH_NAME}" must be the very first element in Google/Bing titles. It is the #1 search-matching and visual-differentiation signal.
   2. **Collection name + "Collection" keyword** — Always include the word "Collection" after collection names (e.g., "Skyline Collection" not just "Skyline"). This captures collection-specific searches.
   3. **Product function early** — Product type (e.g., "Towel Bar," "Robe Hook," "Multi Hook") should appear within the first 40 characters.
   4. **Dimensions ONLY when product varies by size** — Towel bars come in 18", 24", 30", 36" → include dimension. Robe hooks are one size → omit dimension. If the product doesn't vary by size, the dimension is wasted title space.
   5. **"Solid Brass" NOT in prime early position** — The early part of the title is reserved for words that convert best (finish, product type, collection). "Solid Brass" should appear after these, not before them.
   6. **Style references that VARY** — Include style descriptors (Contemporary, Traditional, Modern, etc.) but vary them across products to capture different search terms. Don't use the same style word for every product.
   7. **"Allied Brass" as final segment** — Brand name always last.

   **Formula:** `{FINISH_NAME} [Collection] Collection [Dimension if varies] [Product Type] - [Style Reference] [Solid Brass] - Allied Brass`

   **Examples applying this formula:**
   - `{FINISH_NAME} Skyline Collection 24-Inch Towel Bar - Contemporary Solid Brass - Allied Brass` (towel bar varies by size → include dimension)
   - `{FINISH_NAME} Waverly Place Collection Robe Hook - Transitional Solid Brass - Allied Brass` (robe hook is one size → omit dimension)
   - `{FINISH_NAME} Pipeline Collection Multi Hook - Industrial Solid Brass Wall Mount - Allied Brass` (multi hook is one size → omit dimension)
   - `{FINISH_NAME} Carolina Crystal Collection Toilet Paper Holder - Traditional Solid Brass - Allied Brass` (one size → omit dimension)
   ```

2. Update `### Title Rules (Hard Requirements)` to add:
   - "Finish name ({FINISH_NAME}) MUST be the first element in Google/Bing titles"
   - "Only include dimensions when the product varies by size (e.g., towel bars: 18/24/30/36 → yes; robe hooks → no)"
   - "'Solid Brass' should NOT appear in the first 30 characters — reserve that space for finish, product type, and collection"

3. Update `### Five Variations of the Same Product` to show the new formula applied:
   - Change Variation 1 (the primary example) to follow Robert's formula
   - Note that the towel bar DOES vary by size so dimension is included

4. In `### Allied Brass Differentiation (Use These — They're Real)`:
   - Remove the sentence about Kingston Brass using zinc alloy, Moen using chrome-plated steel, Delta using plastic cores
   - Replace with: "Solid brass construction — machined, not cast. Won't corrode, pit, or loosen. Say it positively without naming competitor materials."

5. In the gold standard examples (all 10):
   - Review each gold standard title and description for competitor material references ("die-cast zinc," "plated zinc," "hollow zinc," etc.)
   - Replace competitor material contrasts with positive-only brass framing:
     - "not the hollow zinc tubing" → "solid brass that won't loosen or corrode"
     - "die-cast zinc that loosens in its socket" → "solid brass that holds its threads for years"
     - "chrome-plated plastic brackets" → "solid brass brackets that won't flex or corrode"
     - "chrome-plated steel bars" → "solid brass that resists corrosion in wet environments"
     - "cheap chrome-plated steel and plastic shower caddies" → "solid brass that won't rust — shower caddies should last longer than a single season"
   - Review each gold standard title and update to follow Robert's formula (finish first, Collection keyword, etc.)

**Skill 3: `product-storytelling/SKILL.md` — Add evidence exclusion rules**

The evaluation report (Root Cause 2) shows 8/10 SKUs had excessive technical detail from evidence data.

Changes to make:
1. Add a new section after `## Feature-to-Benefit Translation`:
   ```
   ## Evidence Exclusion Rules

   Not all product evidence belongs in marketing descriptions. Some data is useful for spec sheets but harmful in descriptions because it creates doubt, clutters the narrative, or bores the reader.

   ### EXCLUDE from descriptions (always):

   1. **Weight capacity** — Robert (company founder): "I do not like the idea of a weight capacity. The weight capacity is generally not a problem and this may just have a consumer stray from the product because of a stated weight limit." Weight capacity creates doubt rather than confidence.

   2. **Detailed dimensions** (width, height, projection, depth) — Only include the PRIMARY dimension that shoppers search for:
      - Towel bars: overall length (18", 24", 30", 36")
      - Mirrors: diameter or overall dimensions
      - Glass shelves: length
      - Everything else: omit dimensions unless the product varies by size
      - Robert: "Too much dimensional information. All of that detailed information can be gotten from the spec file."

   3. **Installation specifics** — Mounting type is fine ("wall-mounted," "freestanding"). Installation procedures, screw counts, anchoring requirements, and step-by-step instructions are NOT marketing content.

   4. **Technical specifications** — Thread sizes, bore diameters, material grades, engineering tolerances. These belong in spec sheets.

   ### INCLUDE in descriptions (use from evidence):

   1. **Material** — "Solid brass" is the #1 differentiator. Always include when evidence confirms.
   2. **Mounting type** — "Wall-mounted," "freestanding," "countertop" — confirms product type match.
   3. **Primary searchable dimension** — The one dimension shoppers filter by.
   4. **Collection name** — Coordination opportunity.
   5. **Product design story** — narrative_copy, bullets that describe design intent.
   6. **Style/aesthetic** — Contemporary, traditional, transitional — design context.

   ### When in doubt: OMIT

   Omitting a technical detail is always better than including it in a marketing description. The spec sheet exists for a reason. Descriptions sell the design story; spec sheets provide the measurements.
   ```

2. In `## The Interior Designer's Perspective` section, add under item 3 ("Reference design intent"):
   - "Focus on the design DECISIONS that make the product special, not the technical measurements. A designer cares that the backplate geometry creates clean lines — not that it measures 2.75 x 2.75 inches."
  </action>
  <verify>
    <automated>grep -c "die-cast zinc" .claude/skills/allied-brass-brand-expert/SKILL.md | grep "^0$" && echo "PASS: No die-cast zinc in brand expert" || echo "FAIL: die-cast zinc still present"</automated>
    <manual>Verify: (1) brand expert has competitor material prohibition, (2) shopping content has Robert's title formula, (3) storytelling has evidence exclusion rules</manual>
  </verify>
  <done>Three skills updated: brand expert removes all competitor material references and adds prohibition, shopping content codifies Robert's title formula, storytelling adds evidence exclusion rules for weight capacity/dimensions/installation.</done>
</task>

<task type="auto">
  <name>Task 2: Update SYSTEM_PROMPT prohibitions, fix prompt_builder, investigate product types</name>
  <files>
    src/feedops/pipeline/prompts.py
    src/feedops/api/prompt_builder.py
  </files>
  <action>
**Part A: Add content prohibition rules to SYSTEM_PROMPT `accuracy_guardrail`**

In `src/feedops/pipeline/prompts.py`, update the `<accuracy_guardrail>` section. After the existing "Prohibited fabrications:" list, add a new "Content prohibitions:" list:

```
Content prohibitions (from human evaluation feedback):
- Do NOT include weight capacity in descriptions — it creates consumer doubt rather than confidence
- Do NOT include detailed dimensions (width, height, projection, depth) — only the primary searchable dimension (e.g., overall length for towel bars, diameter for mirrors)
- Do NOT use "also searched as," "also known as," or similar keyword list patterns — all keywords must be integrated naturally
- Do NOT name competitor materials: "die-cast zinc," "zinc alloy," "plated alternatives," "chrome-plated steel," "hollow zinc" — frame solid brass positively, never by contrast with cheaper materials
- Do NOT use "heritage bathroom fixtures" or any invented category terms not in the evidence
- Do NOT mention "28 finishes" or finish variety counts in Google/Bing descriptions — these descriptions will be expanded to finish-specific variants, making finish count references irrelevant and confusing
```

Also add to `<brand_voice>` section, after the banned words line:
```
Banned phrases (never use): "heritage bathroom fixtures", "common die-cast zinc", "plated alternatives", "also searched as", "also known as"
```

**Part B: Suppress "28 finishes" for ALL Google/Bing content**

In `src/feedops/api/prompt_builder.py`, in the competitive positioning section (around line 307-329), add an instruction that explicitly tells GPT-5.2 to suppress finish count mentions for Google and Bing platforms.

Find the `competitive_block` string construction and append:
```python
# Suppress "28 finishes" for Google/Bing — these descriptions get expanded to
# finish-specific variants, making finish count references irrelevant.
if platform in {"google", "bing"}:
    competitive_block += (
        "\n\nIMPORTANT: Do NOT mention '28 finishes,' '28+ finishes,' or finish variety counts "
        "in Google/Bing descriptions. These descriptions will be expanded into finish-specific "
        "variants — mentioning '28 other finishes' on an Antique Bronze listing confuses shoppers."
    )
```

**Part C: Investigate product type data for SKUs 1024 and 1020**

Use Supabase MCP to query the evidence table and product_catalog for these two SKUs:

1. SKU 1024 — Robert says it's a "2-post Toilet Paper Holder", but generated content called it "Euro Style":
   ```sql
   SELECT master_sku, current_title, current_description, category, product_type
   FROM product_catalog
   WHERE master_sku = '1024';
   ```

   Also check evidence:
   ```sql
   SELECT master_sku, field_name, field_value
   FROM evidence_table
   WHERE master_sku = '1024' AND field_name IN ('product_type', 'style', 'current_title', 'bullet_1', 'bullet_2');
   ```

   If evidence shows "Euro Style" → the evidence data is wrong. Check if the current_title or bullets contain "2-post" or "Euro" to determine the correct type. If the evidence IS correct (it IS a Euro style), then Robert's feedback takes precedence — update the category guidance or add a note to prompt_builder.

2. SKU 1020 — Robert says it's a "Robe Hook", but generated content called it "Towel Hook":
   ```sql
   SELECT master_sku, current_title, current_description, category, product_type
   FROM product_catalog
   WHERE master_sku = '1020';
   ```

   Check evidence similarly. The product_catalog.category or product_type fields likely say "robe hook" — if the model is calling it "towel hook", this is a model inference error, not a data error. The accuracy guardrail + product design story should ground the model on the correct product type.

**If data corrections are needed:** Note them in the summary. If the evidence table has incorrect product type values, note this as a data fix for a future plan (evidence table corrections require Supabase queries, not code changes).

**Part D: Deploy to Cloud Run**

After all code changes:
```bash
cd /Users/bobby/Documents/GitHub/Allied-FeedOps
git add src/feedops/pipeline/prompts.py src/feedops/api/prompt_builder.py
git commit -m "fix(25-06): add content prohibitions and suppress 28-finishes for Google/Bing"
git push origin master
```

Wait for Cloud Run build to complete (~2-3 min):
```bash
gcloud builds list --project=bobbys-project-346400 --limit=3
```

Verify deployment:
```bash
curl https://feedops-pipeline-623866089882.us-east1.run.app/health
```
  </action>
  <verify>
    <automated>grep -c "weight capacity" src/feedops/pipeline/prompts.py | grep -v "^0$" && grep -c "28 finishes" src/feedops/api/prompt_builder.py | grep -v "^0$" && echo "PASS: Prohibitions added" || echo "FAIL: Missing prohibitions"</automated>
    <manual>Verify: (1) accuracy_guardrail has all 6 prohibition rules, (2) prompt_builder suppresses 28 finishes for google/bing, (3) Cloud Run health check passes, (4) product type investigation documented</manual>
  </verify>
  <done>SYSTEM_PROMPT has 6 new content prohibition rules, prompt_builder suppresses 28-finishes for Google/Bing universally, product type data for 1024 and 1020 investigated and documented. Changes deployed to Cloud Run.</done>
</task>

</tasks>

<verification>
1. `allied-brass-brand-expert` skill contains ZERO mentions of "die-cast zinc," "zinc alloy," "plated alternatives," or competitor brand names
2. `google-shopping-content` skill has Robert's title formula with finish-first rule, dimension-only-when-varies rule
3. `product-storytelling` skill has explicit evidence exclusion rules for weight capacity, detailed dimensions, installation specifics
4. SYSTEM_PROMPT `accuracy_guardrail` has all 6 content prohibition rules
5. SYSTEM_PROMPT `brand_voice` has expanded banned phrases list
6. `prompt_builder.py` competitive block suppresses "28 finishes" for google/bing platforms
7. Gold standard examples in `google-shopping-content` skill purged of competitor material references
8. Cloud Run deployment healthy with all changes
9. Product type data for SKUs 1024 and 1020 investigated
</verification>

<success_criteria>
- All three skills updated with no competitor material references and new domain expert rules
- SYSTEM_PROMPT has comprehensive content prohibition rules from evaluator feedback
- "28 finishes" suppressed for ALL Google/Bing content generation
- Changes deployed to Cloud Run and verified healthy
- Product type misidentification for 1024 and 1020 documented and addressed
</success_criteria>

<output>
After completion, create `.planning/phases/25-evaluate-iterate/25-06-SUMMARY.md`
</output>
