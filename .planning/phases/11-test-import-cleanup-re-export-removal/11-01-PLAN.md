---
phase: 11-test-import-cleanup-re-export-removal
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/test_finish_prompt_source_contract.py
  - tests/test_generation_runtime_scope_contract.py
  - tests/test_phase7_observability_reliability.py
  - tests/api/test_main_master_sku_alias_runtime.py
  - src/feedops/api/main.py
autonomous: true
requirements: [DEAD-02, DEAD-03]

must_haves:
  truths:
    - "No test file imports any symbol from feedops.api.main for use — all imports point to canonical extracted modules"
    - "main.py backward-compat re-export block (lines 174-304) no longer exists"
    - "main.py remains importable — smoke tests pass"
    - "All 790+ tests pass after every commit in the sequence"
  artifacts:
    - path: "tests/api/test_finish_prompt_source_contract.py"
      provides: "Test imports from canonical modules (prompt_loader, finish_processing, persistence)"
      contains: "from feedops.api.prompt_loader import"
    - path: "tests/test_generation_runtime_scope_contract.py"
      provides: "Test imports from canonical modules (telemetry, observability, routes)"
      contains: "from feedops.api.telemetry import"
    - path: "tests/test_phase7_observability_reliability.py"
      provides: "Test imports from canonical modules (observability, routes, schemas)"
      contains: "from feedops.observability import"
    - path: "tests/api/test_main_master_sku_alias_runtime.py"
      provides: "Test imports from canonical modules (schemas, routes, job_management, persistence, sku_alias, runtime_controls)"
      contains: "from feedops.api.schemas import"
    - path: "src/feedops/api/main.py"
      provides: "Clean main.py with no re-export block — only FastAPI app, middleware, lifespan, routers"
  key_links:
    - from: "tests/api/test_finish_prompt_source_contract.py"
      to: "feedops.api.prompt_loader"
      via: "direct import of get_finish_list"
      pattern: "from feedops\\.api\\.prompt_loader import"
    - from: "tests/api/test_main_master_sku_alias_runtime.py"
      to: "feedops.api.routes"
      via: "direct import + monkeypatch at routes module"
      pattern: "import feedops\\.api\\.routes as api_routes"
    - from: "src/feedops/api/main.py"
      to: "feedops.api.routes"
      via: "FastAPI include_router"
      pattern: "app\\.include_router"
---

<objective>
Migrate 4 test files from main.py re-export imports to canonical module imports (DEAD-02), then delete the 131-line backward-compat re-export block from main.py (DEAD-03).

Purpose: Eliminate the re-export block that kept dead import paths alive — after this, main.py is a clean FastAPI entrypoint with no backward-compat baggage.
Output: 4 updated test files importing from canonical modules, main.py shrunk by ~131 lines.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-import-cleanup-re-export-removal/11-RESEARCH.md

<interfaces>
<!-- Canonical module map — the executor MUST use these exact import paths -->

| Symbol | Canonical Module |
|--------|----------------|
| `get_finish_list` | `feedops.api.prompt_loader` |
| `get_platform_system_prompt_hash` | `feedops.api.prompt_loader` |
| `_enforce_finish_sentence_parity` | `feedops.api.finish_processing` |
| `_assembled_prompt_hash` | `feedops.api.persistence` |
| `_enforce_write_time_finish_placeholder_contract` | `feedops.api.persistence` |
| `_emit_generation_summary` | `feedops.api.telemetry` |
| `get_request_id` | `feedops.observability` |
| `regenerate_content` | `feedops.api.routes` |
| `process_regenerate_job` | `feedops.api.routes` |
| `optimize_single_sku` | `feedops.api.routes` |
| `batch_optimize` | `feedops.api.routes` |
| `hybrid_generate` | `feedops.api.routes` |
| `RegenerateRequest` | `feedops.api.schemas` |
| `RegenerateJobResponse` | `feedops.api.schemas` |
| `HybridGenerateRequest` | `feedops.api.schemas` |
| `OptimizeRequest` | `feedops.api.schemas` |
| `BatchOptimizeRequest` | `feedops.api.schemas` |
| `_require_request_id` | `feedops.api.job_management` |
| `_regeneration_idempotency_key` | `feedops.api.job_management` |
| `_hybrid_generation_idempotency_key` | `feedops.api.job_management` |
| `ensure_generation_enabled` | `feedops.api.runtime_controls` |
| `resolve_canonical_master_sku` | `feedops.api.sku_alias` |
| `get_client` | `feedops.db.supabase_client` |
| `generate_per_platform` | `feedops.pipeline.generator` |
| `GenerationBudgetExceededError` | `feedops.pipeline.generator` |
| `HTTPException` | `fastapi` (NOT in re-export block — import directly from fastapi) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 test files to canonical imports (DEAD-02)</name>
  <files>
    tests/api/test_finish_prompt_source_contract.py
    tests/test_generation_runtime_scope_contract.py
    tests/test_phase7_observability_reliability.py
    tests/api/test_main_master_sku_alias_runtime.py
  </files>
  <action>
Migrate test files one at a time in this exact order (per user decision), committing after each with pytest + ruff verification:

**File 1: test_finish_prompt_source_contract.py (4 refs)**
- Replace `api_main.get_finish_list` with direct import from `feedops.api.prompt_loader`
- Replace `api_main._enforce_finish_sentence_parity` with direct import from `feedops.api.finish_processing`
- Replace `api_main._assembled_prompt_hash` with direct import from `feedops.api.persistence`
- Update any `monkeypatch.setattr(api_main, ...)` calls: patch at the canonical module where the symbol is actually resolved at runtime (e.g., if a route handler calls `get_finish_list`, patch `feedops.api.prompt_loader` or the importing module, not `api_main`)
- Remove the `import feedops.api.main as api_main` line if no remaining `api_main.*` references; keep it only if the test needs it for something else
- Commit: `refactor(tests): migrate test_finish_prompt_source_contract.py to canonical imports`

**File 2: test_generation_runtime_scope_contract.py (8 refs)**
- Replace `api_main._emit_generation_summary` with import from `feedops.api.telemetry`
- Replace `api_main.get_request_id` with import from `feedops.observability`
- Replace other `api_main.*` symbols with their canonical imports per the interface map above
- Update monkeypatches to target canonical modules
- Commit: `refactor(tests): migrate test_generation_runtime_scope_contract.py to canonical imports`

**File 3: test_phase7_observability_reliability.py (21 refs)**
- Same pattern: replace all `api_main.*` with canonical imports per the map
- This file has more refs, so work through them systematically
- Update monkeypatches to target the module that actually resolves the name at call time
- Commit: `refactor(tests): migrate test_phase7_observability_reliability.py to canonical imports`

**File 4: test_main_master_sku_alias_runtime.py (42 refs)**
- This is the heaviest file. Symbols span schemas, routes, job_management, persistence, sku_alias, runtime_controls, supabase_client, generator
- Replace all `api_main.RegenerateRequest` etc. with `api_schemas.RegenerateRequest` (import `feedops.api.schemas as api_schemas`)
- Replace all `api_main.regenerate_content` etc. with `api_routes.regenerate_content` (import `feedops.api.routes as api_routes`)
- Replace `api_main.HTTPException` with direct `from fastapi import HTTPException`
- Update monkeypatches: for route handler dependencies, patch at `api_routes` or the canonical module, not `api_main`
- Commit: `refactor(tests): migrate test_main_master_sku_alias_runtime.py to canonical imports`

**Critical monkeypatch rule:** `monkeypatch.setattr(module, "name", mock)` patches the attribute on the module object. After migration, if a test patches `api_main.get_request_id` but the code under test resolves `get_request_id` from `feedops.observability`, the patch is a no-op. Patch at the module where the code under test actually looks up the name. For route handlers in `routes.py`, that means patching `api_routes` or the module `routes.py` imports from. Read each route handler's actual imports to determine the correct patch target.

After each file commit, verify:
1. `PYTHONPATH=./src .venv/bin/python -m pytest {migrated_test_file} -q` passes
2. `.venv/bin/ruff check {migrated_test_file}` passes
3. `PYTHONPATH=./src .venv/bin/python -m pytest tests/ -q --tb=short` full suite green
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src .venv/bin/python -m pytest tests/api/test_finish_prompt_source_contract.py tests/test_generation_runtime_scope_contract.py tests/test_phase7_observability_reliability.py tests/api/test_main_master_sku_alias_runtime.py -q --tb=short && grep -rn "api_main\." tests/api/test_finish_prompt_source_contract.py tests/test_generation_runtime_scope_contract.py tests/test_phase7_observability_reliability.py tests/api/test_main_master_sku_alias_runtime.py | grep -v "^#" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>All 4 test files import from canonical modules. Zero `api_main.*` symbol access refs remain in these files. All tests pass. 4 atomic commits created.</done>
</task>

<task type="auto">
  <name>Task 2: Delete main.py re-export block and verify importability (DEAD-03)</name>
  <files>src/feedops/api/main.py</files>
  <action>
Delete the entire backward-compat re-export block from main.py (lines 174-304, approximately 131 lines). This block contains `# noqa: F401` annotated imports from 15+ modules that existed solely for backward compatibility with test imports.

Steps:
1. Read main.py and identify the exact line range of the re-export block (starts with comment about backward compatibility, ends before EOF or next functional code)
2. Delete the entire block in one edit
3. Run `ruff check src/feedops/api/main.py` — if the `HTTPException` import on line 39 is flagged as F401, check if it is used anywhere else in main.py's actual code. If not used, remove it too. Tests that used `api_main.HTTPException` should already import from `fastapi` directly (updated in Task 1)
4. Run full test suite to verify nothing breaks
5. Confirm the 5 smoke tests still pass (they just do `import feedops.api.main`)
6. Confirm `python -c "import feedops.api.main"` exits 0

Commit: `refactor(dead-code): remove 131-line backward-compat re-export block from main.py (DEAD-03)`

**Fallback (per user decision):** If single-commit removal breaks in unexpected ways, remove group-by-group with verification after each group. But this should not be needed since Task 1 already migrated all consumers.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src .venv/bin/python -c "import feedops.api.main; print('OK')" && PYTHONPATH=./src .venv/bin/python -m pytest tests/api/test_telemetry_smoke.py tests/api/test_persistence_smoke.py tests/api/test_schemas_smoke.py tests/api/test_job_runner_smoke.py tests/api/test_job_management_smoke.py -q && .venv/bin/ruff check src/feedops/api/main.py && PYTHONPATH=./src .venv/bin/python -m pytest tests/ -q --tb=short</automated>
  </verify>
  <done>Re-export block deleted from main.py. main.py is importable. All 5 smoke tests pass. Full test suite green. ruff clean on main.py.</done>
</task>

</tasks>

<verification>
- `grep -rn "api_main\." tests/` returns zero results for the 4 migrated files
- `wc -l src/feedops/api/main.py` shows ~173 lines (down from ~304)
- `PYTHONPATH=./src .venv/bin/python -m pytest tests/ -q --tb=short` all tests pass
- `PYTHONPATH=./src .venv/bin/python -c "import feedops.api.main"` exits 0
- `.venv/bin/ruff check src/feedops/api/main.py` clean
</verification>

<success_criteria>
- Zero `api_main.*` symbol access in the 4 migrated test files
- Re-export block completely removed from main.py
- main.py remains importable (smoke tests pass)
- Full test suite green (790+ tests passing)
- ruff clean across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-import-cleanup-re-export-removal/11-01-SUMMARY.md`
</output>
