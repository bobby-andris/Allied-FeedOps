---
phase: 34.1-fix-decision-logic
verified: 2026-02-26T04:00:00Z
status: passed
score: 13/13 requirements verified
re_verification: false
human_verification:
  - test: "Open Tier Intelligence dashboard, filter to wasted spend terms — confirm all show 'Block' or 'Constrain' action buttons, never 'Promote'"
    expected: "Zero-conversion, >$5 spend terms show 'Block' (if HIGH) or 'Constrain' (if MEDIUM/LOW)"
    why_human: "Requires live UI with actual scored terms; can't verify action button rendering from static analysis alone"
  - test: "Click Block Label button on a GroupOverview card — confirm confirmation dialog appears and the action is logged"
    expected: "window.confirm dialog appears with label name; on OK, POST to /api/shopping-funnel/recommendations with action='label_block'"
    why_human: "window.confirm interaction cannot be verified programmatically without browser automation"
  - test: "Verify label profitability table shows 'Insufficient data' badge when fewer than 7 snapshot days exist"
    expected: "Labels with days_of_data < 7 display badge with AlertTriangle icon and 'Nd data' text; spend/ROAS/conversions columns show '--'"
    why_human: "Requires live data in funnel_snapshots_daily; table may be empty until snapshots backfill"
---

# Phase 34.1: Fix Decision Logic Verification Report

**Phase Goal:** Fix the scoring engine's broken decision logic so recommendations are prescriptive (what action to take) rather than descriptive (which tier's distribution you resemble), and add custom_label_0 level blocking, search promotion candidates, and label profitability summaries.

**Verified:** 2026-02-26T04:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Wasted spend terms (0 conversions, >$5) NEVER get 'promote' — always 'block' or 'constrain' | VERIFIED | `determineAction()` in tier-scoring.ts:495-496 returns 'block'/'constrain' for zero-conversion terms; tests at lines 800-838 confirm |
| 2 | Impact for wasted spend equals monthly cost saved (not $0) | VERIFIED | `estimateImpact()` fast path at line 389-399 returns cost * multipliers (0.5/0.8/1.0); test confirms `impact.mid >= 4` for $8 spend |
| 3 | CPC fit scoring penalizes expensive terms, does not penalize cheap | VERIFIED | `cpcPenalty = Math.max(0, zCpc)` at tier-scoring.ts:225; CPC inversion tests at lines 907-968 pass |
| 4 | Verdict text is prescriptive ('Block — spent $X with zero conversions') not descriptive ('fits LOW distribution') | VERIFIED | switch-case verdict generation at lines 262-279; prescriptive verdict tests at lines 971-1022 pass |
| 5 | Under-invested detection compares avgMonthlySearches against actual impressions | VERIFIED | reason-codes.ts:50 — `keywordData.avgMonthlySearches > UNDER_INVESTED_MULTIPLIER * (term.totalImpressions ?? 0)` |
| 6 | Constrain button hidden when term already in HIGH tier | VERIFIED | LeakageTermRow.tsx:169 — `{term.currentTier !== 'HIGH' && (<Button>Constrain</Button>)}` |
| 7 | Verdict display uses prescriptive actionReason text | VERIFIED | LeakageTermRow.tsx:141 — `{term.actionReason \|\| term.verdict}` |
| 8 | ROAS recommendations guard against wasted spend | VERIFIED | control-center.ts:258-270 — wasted-spend override raises target ROAS for zero-conversion rows |
| 9 | estimateTierFromMetrics never returns LOW for zero-conversion terms | VERIFIED | query-intelligence.ts:136-137 — guard returns 'high' for zero-conversion + >$5 spend; `@deprecated` comment at line 117 |
| 10 | User can block all terms under a custom_label_0 category | VERIFIED | GroupOverview.tsx:111-127 shows Block Label button; useRecommendations.ts:270-286 exposes blockLabel; recommendations/route.ts handles 'label_block' action |
| 11 | Label blocks persisted with action_scope='label' sentinel pattern | VERIFIED | Migration 040 adds action_scope column; recommendations/route.ts inserts `search_term='__LABEL_BLOCK__'`, `action_scope='label'` |
| 12 | Search promotion candidates identified from high-ROAS terms | VERIFIED | recommendations/route.ts:identify_search_candidates case filters ROAS>3.0, impressions>100, conversions>0 and upserts to search_buildout_recommendations |
| 13 | Label profitability summary with graceful degradation for <7 days | VERIFIED | LabelProfitabilitySummary.tsx:115 — `const insufficient = label.days_of_data < 7`; shows '--' for metrics and badge with days count |

**Score:** 13/13 truths verified

---

## Required Artifacts

| Artifact | Status | Evidence |
|----------|--------|----------|
| `dashboard/src/lib/optimization/tier-scoring.types.ts` | VERIFIED | `RecommendedAction` type exported at line 9; TermScore has optional `recommendedAction?`, `actionReason?`, `totalImpressions?` at lines 90-92 |
| `dashboard/src/lib/optimization/tier-scoring.ts` | VERIFIED | `determineAction()` helper at line 484; `estimateImpact()` fast path at line 389; CPC inversion at line 225; prescriptive verdicts at lines 262-279; all exported fields populated in returned TermScore |
| `dashboard/src/lib/optimization/__tests__/tier-scoring.test.ts` | VERIFIED | 8 Phase 34.1 tests covering all 4 bug fixes; all pass (37/38 pass — 1 pre-existing TIER-06 failure unrelated to this phase) |
| `dashboard/src/app/(dashboard)/tier-scoring/lib/reason-codes.ts` | VERIFIED | `totalImpressions` used in under_invested detection at line 50 |
| `dashboard/src/app/(dashboard)/tier-scoring/components/LeakageTermRow.tsx` | VERIFIED | Constrain guard at line 169; prescriptive verdict display at line 141 |
| `dashboard/src/lib/optimization/query-intelligence.ts` | VERIFIED | Wasted-spend guard at line 136; `@deprecated` JSDoc at line 117 |
| `dashboard/src/lib/optimization/control-center.ts` | VERIFIED | Wasted-spend override in `buildRoasRecommendations()` at lines 258-270 |
| `dashboard/src/app/api/shopping-funnel/tier-scoring/route.ts` | VERIFIED | `recommendedAction`, `actionReason`, `totalImpressions` persisted in model_inputs at lines 186-188 |
| `supabase/migrations/040_routing_recommendations_label_scope.sql` | VERIFIED | `action_scope` column with CHECK('term','label'); `label_block` added to recommended_action constraint |
| `dashboard/src/app/api/shopping-funnel/recommendations/route.ts` | VERIFIED | `label_block` and `identify_search_candidates` POST action cases present |
| `dashboard/src/app/(dashboard)/tier-scoring/hooks/useRecommendations.ts` | VERIFIED | `blockLabel` function at line 270; returned from hook at line 336 |
| `dashboard/src/app/(dashboard)/tier-scoring/components/GroupOverview.tsx` | VERIFIED | `onBlockLabel` optional prop at line 16; `ShieldBan` icon imported; block button at lines 111-127 with window.confirm |
| `dashboard/src/app/(dashboard)/tier-scoring/page.tsx` | VERIFIED | `LabelProfitabilitySummary` imported at line 32; `blockLabel` wired at line 227; component rendered at line 229 |
| `dashboard/src/app/api/shopping-funnel/label-profitability/route.ts` | VERIFIED | Queries `funnel_snapshots_daily` (correct table; plan had wrong name `label_tier_daily_snapshot`); in-memory fallback if rpc fails |
| `dashboard/src/app/(dashboard)/tier-scoring/components/LabelProfitabilitySummary.tsx` | VERIFIED | `days_of_data < 7` graceful degradation at line 115; Profitable/Unprofitable/Insufficient badges |
| `dashboard/src/app/api/shopping-funnel/__tests__/recommendations.test.ts` | VERIFIED | 4 tests: label_block row shape, 400 validation, search candidate filtering (3 excluded/1 included), empty results; all pass |
| `docs/database/SCHEMA.md` | VERIFIED | `routing_recommendations` at line 1590 with all columns including `action_scope`; `query_value_scores` at line 1623; sentinel pattern documented |

---

## Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|----------|
| `tier-scoring.ts:scoreTerm()` | `tier-scoring.types.ts:RecommendedAction` | import + assignment | WIRED | `import { RecommendedAction }` at line 33; `recommendedAction` assigned at line 252, returned at line 303 |
| `LeakageTermRow.tsx` | `TermScore.recommendedAction` | prop access | WIRED | `term.actionReason \|\| term.verdict` at line 141; Constrain guard uses `term.currentTier` at line 169 |
| `reason-codes.ts:classifyLeakageReason` | `TermScore.totalImpressions` | field access | WIRED | `term.totalImpressions ?? 0` at line 50 |
| `GroupOverview.tsx:Block Label button` | `recommendations/route.ts:POST label_block` | useRecommendations hook | WIRED | `onBlockLabel={recommendations.blockLabel}` in page.tsx:227; `blockLabel` calls POST with `action: 'label_block'` at line 284 |
| `recommendations/route.ts` | `routing_recommendations table` | Supabase upsert with action_scope | WIRED | upsert includes `action_scope: 'label'` in label_block case |
| `LabelProfitabilitySummary.tsx` | `label-profitability/route.ts` | fetch | WIRED | `fetch('/api/shopping-funnel/label-profitability')` at component line 45 |

---

## Requirements Coverage

| Requirement | Plan | Description | Status | Evidence |
|-------------|------|-------------|--------|----------|
| FIX-01 | 34.1-01 | Wasted spend gets 'block'/'constrain', never 'promote' | SATISFIED | `determineAction()` logic; tests confirm; 0 conversions + >$5 → block or constrain |
| FIX-02 | 34.1-02 | Under-invested detection uses actual totalImpressions | SATISFIED | reason-codes.ts:50 — `totalImpressions ?? 0` comparison |
| FIX-03 | 34.1-01 | Wasted spend impact = monthly cost saved, not $0 | SATISFIED | estimateImpact fast path returns cost * 0.5/0.8/1.0 |
| FIX-04 | 34.1-01 | CPC fit scoring uses max(0, zCpc) | SATISFIED | tier-scoring.ts:225 — `const cpcPenalty = Math.max(0, zCpc)` |
| FIX-05 | 34.1-02 | Constrain button hidden when term in HIGH | SATISFIED | LeakageTermRow.tsx:169 — conditional render |
| FIX-06 | 34.1-01 | Verdict text prescriptive not descriptive | SATISFIED | switch-case generates 'Block — spent $X...' style verdicts |
| FIX-07 | 34.1-02 | ROAS recommendations: wasted-spend override (distribution rewrite deferred) | SATISFIED (scoped) | Override at control-center.ts:258; `@deprecated` on `estimateTierFromMetrics`; full distribution rewrite not in scope per Plan 02 task |
| FIX-08 | 34.1-02 | estimateTierFromMetrics never returns LOW for zero-conversion | SATISFIED | query-intelligence.ts:136-137 guard returns 'high' |
| FIX-09 | 34.1-02 | Scoring systems unified via deprecation and guard | SATISFIED | estimateTierFromMetrics deprecated; wasted-spend guards added in both paths |
| FEAT-01 | 34.1-03 | Label-level blocking with one click | SATISFIED | GroupOverview Block Label button + label_block API action + migration 040 |
| FEAT-02 | 34.1-03 | Search promotion candidates to search_buildout_recommendations | SATISFIED | identify_search_candidates action filters ROAS>3/impressions>100/conversions>0 |
| FEAT-03 | 34.1-03 | Label profitability summary | SATISFIED | LabelProfitabilitySummary + label-profitability API; graceful degradation at <7 days |
| DOC-01 | 34.1-03 | routing_recommendations + query_value_scores in SCHEMA.md | SATISFIED | Both tables documented including action_scope column and sentinel pattern |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `control-center.ts` | 3-5 | `BASELINE_TARGET_ROAS` has no deprecation comment despite FIX-07 intent to eventually replace with distribution-based | Info | No functional impact; plan's required deprecation note was on `estimateTierFromMetrics` (present), not on the ROAS constants |
| `tier-scoring.test.ts` | TIER-06 | Pre-existing test failure: `uses intent features when provided` expects branded alignment 0.5, gets 0.2 | Info | Pre-existing before this phase; documented in SUMMARY-01 as out of scope |
| `history.test.ts` | 20, 67 | Pre-existing TypeScript error: ImpactRange missing currency/period/direction | Info | Pre-existing; documented in SUMMARY-02; not caused by this phase |
| `trends.test.ts` | 85+ | Pre-existing TypeScript errors: wrong argument count | Info | Pre-existing; documented in SUMMARY-02; not caused by this phase |

No blockers. All anti-patterns are pre-existing issues unrelated to Phase 34.1 changes.

---

## Build Verification

TypeScript: CLEAN (excluding 3 pre-existing test files: history.test.ts, trends.test.ts, TIER-06 test case)

Tests passing:
- `tier-scoring.test.ts`: 37/38 pass (1 pre-existing TIER-06 failure)
- `recommendations.test.ts`: 4/4 pass (all new Phase 34.1-03 tests)
- All Phase 34.1-specific tests: PASS

Commits verified in git log:
- `0b83af19` — fix 4 decision logic bugs (GREEN phase)
- `31f2321c` — write failing tests (RED phase)
- `40b24d4f` — add RecommendedAction type
- `e2984834` — fix under_invested detection
- `b75b7b9f` — guard Constrain button, prescriptive verdicts, wasted-spend overrides
- `34e81065` — persist new TermScore fields in API route
- `f1a9876c` — migration 040 + schema documentation
- `1f3cdf0c` — label_block and identify_search_candidates API actions
- `9ebfd56d` — Block Label button + LabelProfitabilitySummary

---

## Human Verification Required

### 1. Wasted Spend Action Buttons in Live UI

**Test:** Open Tier Intelligence dashboard with active campaign data. Filter to wasted spend terms (reasonCode = wasted_spend). For a term currently in HIGH tier, verify only "Block" button is visible (no "Constrain"). For a term in MEDIUM or LOW tier with zero conversions, verify both "Block" and "Constrain" are visible.

**Expected:** High tier wasted spend term: Block only. MEDIUM/LOW wasted spend term: Block + Constrain.

**Why human:** Requires live data with scored terms displaying the reasonCode = wasted_spend classification. Static analysis confirms the conditional rendering code is correct; production data validates it renders as expected.

### 2. Block Label Button Confirmation Flow

**Test:** On the Tier Intelligence page, find a product group in GroupOverview. Click the "Block Label" button (shield-ban icon). Confirm a confirmation dialog appears with the label name. Click OK. Verify the group is marked as blocked (or check Supabase routing_recommendations for a row with `search_term='__LABEL_BLOCK__'` and `custom_label_0` matching the label).

**Expected:** window.confirm appears; on confirm, POST /api/shopping-funnel/recommendations with action='label_block'; response success; row persisted to routing_recommendations.

**Why human:** window.confirm() cannot be triggered or verified programmatically without browser automation.

### 3. Label Profitability Insufficient Data Badge

**Test:** Open Tier Intelligence. If funnel_snapshots_daily has data for any label with fewer than 7 distinct snapshot days, verify those labels show an AlertTriangle badge with "Nd data" text and '--' values in spend/ROAS/conversions columns.

**Expected:** Labels with days_of_data < 7 display badge; labels with >= 7 days show actual numbers with Profitable/Unprofitable badge.

**Why human:** Requires live snapshot data in funnel_snapshots_daily; table may be empty until snapshot backfill completes.

---

## Gaps Summary

No gaps found. All 13 observable truths verified. All 13 requirements satisfied.

**Notable deviation:** Plan 03 specified `label_tier_daily_snapshot` as the target table for label profitability, but the actual production table is `funnel_snapshots_daily` (same schema). The implementation correctly uses `funnel_snapshots_daily`. This was documented as an auto-fixed deviation in SUMMARY-03.

**FIX-07 scope clarification:** The requirement to "compare ROAS against tier distribution, not bid target" was implemented as a wasted-spend override (zeros always raise target) with a `@deprecated` comment on `estimateTierFromMetrics`. Full distribution-based rewrite of `buildRoasRecommendations` was explicitly deferred via plan task language ("add a deprecation comment noting these should be replaced with distribution lookups when distributions are available"). This is not a gap — it reflects intentional scoping.

---

_Verified: 2026-02-26_
_Verifier: Claude (gsd-verifier)_
