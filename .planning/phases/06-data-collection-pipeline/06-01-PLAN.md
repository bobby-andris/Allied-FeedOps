---
phase: 06-data-collection-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedops/jobs/workers.py
  - supabase/migrations/026_add_custom_labels_to_variant_index.sql
autonomous: true

must_haves:
  truths:
    - "Search terms worker collects search terms for a batch of SKUs using campaign-join pattern and saves with idempotent upserts"
    - "Performance metrics worker collects 180-day metrics for a batch of SKUs and saves to performance_baselines with idempotent upserts"
    - "Keyword Planner worker generates keyword ideas for a batch of SKUs and saves to keyword_metrics with 30-day TTL"
    - "Custom labels worker fetches custom labels 0-4 from Merchant Center and upserts JSONB to variant_index.custom_labels column (added by migration)"
    - "All workers include collection timestamps in saved data"
    - "All workers are async functions matching BatchProcessor process_fn signature"
  artifacts:
    - path: "src/feedops/jobs/workers.py"
      provides: "4 async worker functions for data collection"
      exports: ["collect_search_terms_batch", "collect_performance_batch", "collect_keyword_planner_batch", "collect_custom_labels_batch"]
    - path: "supabase/migrations/026_add_custom_labels_to_variant_index.sql"
      provides: "Migration adding custom_labels JSONB column to variant_index"
      contains: "ALTER TABLE variant_index ADD COLUMN"
  key_links:
    - from: "src/feedops/jobs/workers.py"
      to: "src/feedops/integrations/google_ads_search_terms.py"
      via: "SearchTermsClient.fetch_search_terms(), save_search_terms_to_db(), KeywordPlannerClient.get_historical_metrics()"
      pattern: "SearchTermsClient|KeywordPlannerClient"
    - from: "src/feedops/jobs/workers.py"
      to: "src/feedops/integrations/google_ads_performance.py"
      via: "fetch_batch_product_performance()"
      pattern: "fetch_batch_product_performance"
    - from: "src/feedops/jobs/workers.py"
      to: "src/feedops/integrations/merchant_center.py"
      via: "fetch_merchant_center_items()"
      pattern: "fetch_merchant_center"
---

<objective>
Implement 4 async worker functions that plug into the Phase 1 BatchProcessor for data collection.

Purpose: These workers are the core data collection logic that the job infrastructure invokes. Each receives a batch of SKU IDs and performs collection + storage with idempotent upserts.

Output: `src/feedops/jobs/workers.py` with 4 exported async functions matching the `process_fn` signature `async (batch: list[str]) -> list[dict]`.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-collection-pipeline/02-RESEARCH.md

# Phase 1 infrastructure (what workers integrate with)
@src/feedops/jobs/processor.py
@src/feedops/jobs/models.py
@src/feedops/api/backfill.py

# Existing clients (DO NOT re-implement, call these)
@src/feedops/integrations/google_ads_search_terms.py
@src/feedops/integrations/google_ads_performance.py
@src/feedops/integrations/merchant_center.py

# Database schema reference
@docs/database/SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Google Ads collection workers (search terms + performance metrics)</name>
  <files>src/feedops/jobs/workers.py</files>
  <action>
Create `src/feedops/jobs/workers.py` with two async worker functions:

**1. `collect_search_terms_batch(batch: list[str]) -> list[dict]`** (DATA-01, DATA-10)

This worker collects search terms for a batch of master SKUs using the existing SearchTermsClient:

- Import `SearchTermsClient` from `feedops.integrations.google_ads_search_terms`
- Import `compute_date_range` from `feedops.api.backfill` for explicit YYYY-MM-DD dates (DATA-07)
- Create a SearchTermsClient instance
- Call `client.fetch_search_terms(days=180)` to get search terms using the campaign-join pattern (DATA-01: 2-step query already implemented in SearchTermsClient)
- Call `client.save_search_terms_to_db(terms, period_start, period_end, sync_job_id=None)` which already uses idempotent upserts with `ON CONFLICT (query_text, gmc_offer_id, period_start, period_end)`
- The function fetches ALL search terms in one call (campaign-join pattern is batch-native, not per-SKU). The `batch` parameter filters results after collection
- Return list of dicts with `{"item_id": sku, "status": "ok"}` for each processed SKU, or `{"item_id": sku, "status": "no_data"}` if no search terms found for that SKU

IMPORTANT: The SearchTermsClient.fetch_search_terms() uses LAST_N_DAYS syntax internally but the save_search_terms_to_db() takes explicit date params. Use `compute_date_range(180)` to generate the period_start and period_end for saving.

IMPORTANT: Data flow for search terms filtering:
1. `fetch_search_terms(days=180)` returns a list of dicts. Each dict has a `"master_sku"` field (populated via `get_variant_info()` lookup from the GMC offer ID). This is how we know which SKU a search term belongs to.
2. Filter the returned list: `[t for t in all_terms if t.get("master_sku") in batch_set]` where `batch_set = set(batch)`
3. If filtered results are non-empty, call `save_search_terms_to_db(filtered_terms, period_start, period_end)` — this method handles deduplication and idempotent upserts internally
4. For each SKU in the batch, check if any terms matched. Return `"ok"` if terms found, `"no_data"` if not

**2. `collect_performance_batch(batch: list[str]) -> list[dict]`** (DATA-02, DATA-05, DATA-09, DATA-10)

This worker collects 180-day performance metrics for a batch of master SKUs:

- Import `fetch_batch_product_performance` from `feedops.integrations.google_ads_performance`
- Import `get_client` from `feedops.db.supabase_client`
- Use `compute_date_range(180)` for explicit date range (DATA-07)
- For each master_sku in the batch:
  1. Query `variant_index` to get all `gmc_offer_id` values for that master_sku
  2. Call `fetch_batch_product_performance(offer_ids, start_date, end_date)` to get metrics (impressions, clicks, CTR, conversions + competitive metrics like impression_share/click_share where available per DATA-09)
  3. Aggregate variant-level metrics to master_sku level (sum impressions/clicks/conversions, weighted avg CTR)
  4. Upsert to `performance_baselines` table with:
     - master_sku, platform='google'
     - avg_impressions (total_impressions / days), avg_clicks, avg_ctr, avg_conversions
     - Use `.upsert()` with `on_conflict="master_sku,platform"` for idempotency
     - Include `created_at` timestamp (auto-populated by DB default, DATA-10)
     - Include `baseline_start_date` and `baseline_end_date` fields (DATA-05) — these are the correct column names per SCHEMA.md (NOT `date_range_start`/`date_range_end`)
- Return list of result dicts

NOTE: The `fetch_batch_product_performance` function already handles the IN clause batching and returns `{offer_id: {impressions, clicks, ctr, conversions, conversion_value, cost, roas, daily_data}}`. Aggregate across all variants of a master_sku.

NOTE: Check `performance_baselines` schema in SCHEMA.md for exact column names before writing upsert logic. The table has `(master_sku, platform)` as the conflict target.

Add module docstring explaining the workers module purpose, the process_fn contract (idempotent upserts required), and links to Phase 1 infrastructure.
  </action>
  <verify>
Run: `PYTHONPATH=./src python -c "from feedops.jobs.workers import collect_search_terms_batch, collect_performance_batch; print('OK')"`
Verify both functions have async signatures matching `process_fn: Callable[[list[str]], Awaitable[list[dict]]]`.
  </verify>
  <done>
Two async worker functions exist that:
1. collect_search_terms_batch uses SearchTermsClient with campaign-join pattern and saves with idempotent upserts
2. collect_performance_batch queries variant_index, fetches batch performance, aggregates to master_sku, upserts to performance_baselines
Both use explicit YYYY-MM-DD date ranges and include collection timestamps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Keyword Planner and Custom Labels workers</name>
  <files>src/feedops/jobs/workers.py</files>
  <action>
Add two more async worker functions to `src/feedops/jobs/workers.py`:

**3. `collect_keyword_planner_batch(batch: list[str]) -> list[dict]`** (DATA-03, DATA-10)

This worker generates Keyword Planner ideas for a batch of master SKUs:

- Import `KeywordPlannerClient` from `feedops.integrations.google_ads_search_terms`
- Create a KeywordPlannerClient instance
- For each master_sku in the batch:
  1. Query `variant_index` for the master_sku to get product_title (used as seed keyword)
  2. Also query `search_queries` for top existing search terms for this SKU (up to 5) to use as additional seed keywords
  3. Build keyword list: [product_title] + top search terms
  4. Call `client.get_historical_metrics(keywords, use_cache=True, cache_max_age_days=30)` (DATA-03: 30-day TTL caching is built into KeywordPlannerClient)
  5. The KeywordPlannerClient already handles saving to `keyword_metrics` table with proper caching
- Return list of result dicts with `{"item_id": sku, "status": "ok", "keywords_enriched": N}`

NOTE: KeywordPlannerClient.get_historical_metrics() already checks cache and only queries the API for keywords not cached within cache_max_age_days. This means re-running for the same SKU within 30 days is essentially free.

NOTE: The keyword_planner_limiter (2 QPS) is applied at the BatchProcessor level, not inside this worker. The BatchProcessor calls `await rate_limiter.acquire()` before each batch.

**4. `collect_custom_labels_batch(batch: list[str]) -> list[dict]`** (DATA-04, DATA-10)

This worker syncs custom labels (0-4) from Google Merchant Center to the `variant_index` table.

**PREREQUISITE — Migration**: The `variant_index` table does NOT currently have a custom labels column. Before this worker can function, a migration must add a `custom_labels` JSONB column to `variant_index`:

```sql
ALTER TABLE variant_index ADD COLUMN IF NOT EXISTS custom_labels jsonb;
COMMENT ON COLUMN variant_index.custom_labels IS 'Custom labels 0-4 from GMC, stored as {"customLabel0": "value", ...}';
```

Apply this migration via `mcp__supabase__apply_migration` with name `026_add_custom_labels_to_variant_index.sql` as the FIRST step of this task.

**Worker implementation**:

- Import `fetch_merchant_center_items` from `feedops.integrations.merchant_center`
- Import `get_client` from `feedops.db.supabase_client`
- The GMC API returns all products at once (not per-SKU), so this worker should:
  1. Call `fetch_merchant_center_items()` ONCE to get all products with customLabel0-4 fields
  2. Build a lookup dict: `{offerId: {"customLabel0": val, ..., "customLabel4": val}}` from the results
  3. For each master_sku in the batch:
     a. Query `variant_index` to get all `gmc_offer_id` values for that master_sku
     b. Look up custom labels from the GMC data by offer_id (matching lowercase format)
     c. Update `variant_index` with `custom_labels` JSONB value and `updated_at` timestamp
     d. Use `.update().eq("gmc_offer_id", offer_id)` since variant_index has unique gmc_offer_id
  4. Include `updated_at` timestamp in the update (DATA-10)
- Return list of result dicts

IMPORTANT: The GMC API call is expensive (fetches ALL products). Call it ONCE at the start of the batch and reuse the lookup dict for all SKUs. Use a module-level cache with TTL (e.g., `_gmc_cache` dict with `_gmc_cache_time`) to avoid re-fetching across consecutive batches within the same job run.

Also update `src/feedops/jobs/__init__.py` to export all 4 worker functions.
  </action>
  <verify>
Run migration: Apply `026_add_custom_labels_to_variant_index.sql` via `mcp__supabase__apply_migration`.
Verify migration: `mcp__supabase__execute_sql` with `SELECT column_name FROM information_schema.columns WHERE table_name = 'variant_index' AND column_name = 'custom_labels'` — should return 1 row.
Run: `PYTHONPATH=./src python -c "from feedops.jobs.workers import collect_keyword_planner_batch, collect_custom_labels_batch; print('OK')"`
Run: `PYTHONPATH=./src python -c "from feedops.jobs import collect_search_terms_batch, collect_performance_batch, collect_keyword_planner_batch, collect_custom_labels_batch; print('All 4 workers imported')"`
  </verify>
  <done>
Migration applied: variant_index has custom_labels JSONB column.
Four async worker functions exist and are exported from feedops.jobs:
1. collect_search_terms_batch - campaign-join pattern, idempotent upserts
2. collect_performance_batch - batch performance with variant aggregation
3. collect_keyword_planner_batch - Keyword Planner with 30-day cache TTL
4. collect_custom_labels_batch - GMC custom label sync to variant_index.custom_labels
All use idempotent writes and include collection timestamps.
  </done>
</task>

</tasks>

<verification>
1. All 4 worker functions import cleanly: `from feedops.jobs.workers import collect_search_terms_batch, collect_performance_batch, collect_keyword_planner_batch, collect_custom_labels_batch`
2. All 4 functions have `async def fn(batch: list[str]) -> list[dict]` signature
3. Each worker uses existing client libraries (not custom API calls)
4. Each worker uses idempotent writes (upsert with ON CONFLICT or update with eq filter)
5. Each worker includes timestamps in saved data
6. No new external dependencies added
</verification>

<success_criteria>
- 4 async worker functions exist in src/feedops/jobs/workers.py
- All functions match BatchProcessor's process_fn signature
- Search terms worker uses SearchTermsClient (campaign-join pattern)
- Performance worker uses fetch_batch_product_performance with variant aggregation
- Keyword Planner worker uses KeywordPlannerClient with 30-day caching
- Custom labels worker uses fetch_merchant_center_items with variant_index.custom_labels JSONB upserts (column added by migration)
- All workers use idempotent database writes
- All workers include collection timestamps
- Workers are exported from feedops.jobs package
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-collection-pipeline/06-01-SUMMARY.md`
</output>
