---
phase: 06-data-collection-pipeline
plan: 03
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/test_jobs/test_workers.py
  - src/feedops/jobs/workers.py
autonomous: true

must_haves:
  truths:
    - "Tests validate search terms worker calls SearchTermsClient and saves with idempotent upserts"
    - "Tests validate performance worker queries variant_index, fetches batch metrics, and upserts to performance_baselines"
    - "Tests validate keyword planner worker builds seed keywords from product_title + search_queries and calls KeywordPlannerClient"
    - "Tests validate custom labels worker fetches from Merchant Center and updates variant_index"
    - "Tests validate all workers return list[dict] with item_id and status"
    - "Tests validate all workers handle empty batch gracefully"
  artifacts:
    - path: "tests/test_jobs/test_workers.py"
      provides: "Unit tests for all 4 collection workers"
      min_lines: 100
  key_links:
    - from: "tests/test_jobs/test_workers.py"
      to: "src/feedops/jobs/workers.py"
      via: "import and test each worker function"
      pattern: "from feedops.jobs.workers import"
---

<objective>
Write comprehensive unit tests for all 4 collection worker functions, validating they correctly call existing client libraries, use idempotent writes, and handle edge cases.

Purpose: Ensures workers follow the BatchProcessor contract (process_fn signature, idempotent upserts) and correctly integrate with existing client libraries before wiring into production endpoints.

Output: `tests/test_jobs/test_workers.py` with passing test suite.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-collection-pipeline/06-01-SUMMARY.md

# Workers under test
@src/feedops/jobs/workers.py

# Existing test patterns
@tests/test_jobs/test_processor.py
@tests/test_jobs/test_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests for Google Ads workers (search terms + performance)</name>
  <files>tests/test_jobs/test_workers.py</files>
  <action>
Create `tests/test_jobs/test_workers.py` with mock-based unit tests following the patterns established in `test_processor.py` and `test_manager.py` (mock Supabase client, mock Google Ads clients).

**Search Terms Worker Tests (3 tests):**

1. `test_collect_search_terms_batch_calls_client`:
   - Mock SearchTermsClient.fetch_search_terms() to return sample search terms
   - Mock SearchTermsClient.save_search_terms_to_db() to return row count
   - Call `collect_search_terms_batch(["SKU-1", "SKU-2"])`
   - Assert: fetch_search_terms called with days=180
   - Assert: save_search_terms_to_db called with filtered results and correct date range
   - Assert: Returns list of dicts with item_id and status for each SKU

2. `test_collect_search_terms_batch_empty_results`:
   - Mock SearchTermsClient.fetch_search_terms() to return []
   - Call `collect_search_terms_batch(["SKU-1"])`
   - Assert: Returns result with status "no_data"
   - Assert: save_search_terms_to_db NOT called (no data to save)

3. `test_collect_search_terms_batch_filters_by_sku`:
   - Mock fetch_search_terms() to return terms for SKU-1, SKU-2, SKU-3
   - Call `collect_search_terms_batch(["SKU-1", "SKU-2"])` (only requesting 2)
   - Assert: Only terms for SKU-1 and SKU-2 are saved (SKU-3 filtered out)

**Performance Worker Tests (3 tests):**

4. `test_collect_performance_batch_aggregates_variants`:
   - Mock variant_index query to return 3 variants for SKU-1
   - Mock fetch_batch_product_performance() to return metrics for each variant
   - Mock Supabase upsert
   - Call `collect_performance_batch(["SKU-1"])`
   - Assert: fetch_batch_product_performance called with all 3 offer_ids
   - Assert: Upsert called with aggregated metrics (sum of impressions/clicks across variants)
   - Assert: Upsert uses on_conflict="master_sku,platform"

5. `test_collect_performance_batch_no_variants`:
   - Mock variant_index query to return empty result
   - Call `collect_performance_batch(["SKU-1"])`
   - Assert: Returns result with status "no_variants"
   - Assert: fetch_batch_product_performance NOT called

6. `test_collect_performance_batch_includes_timestamps`:
   - Mock full pipeline
   - Call `collect_performance_batch(["SKU-1"])`
   - Assert: Upsert data includes captured_at timestamp
   - Assert: Upsert data includes date_range_start and date_range_end (DATA-05)

Use `unittest.mock.patch` to mock:
- `feedops.jobs.workers.SearchTermsClient` (patch at import location in workers module)
- `feedops.jobs.workers.fetch_batch_product_performance`
- `feedops.jobs.workers.get_client` (Supabase)

Use `pytest.mark.asyncio` for async test functions.
  </action>
  <verify>
Run: `PYTHONPATH=./src .venv/bin/python -m pytest tests/test_jobs/test_workers.py -v -k "search_terms or performance" --tb=short`
All 6 tests should pass.
  </verify>
  <done>
6 tests passing: 3 for search terms worker (client calls, empty results, SKU filtering) and 3 for performance worker (variant aggregation, no variants, timestamps).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write tests for Keyword Planner and Custom Labels workers</name>
  <files>tests/test_jobs/test_workers.py</files>
  <action>
Add tests for the remaining 2 workers to `tests/test_jobs/test_workers.py`:

**Keyword Planner Worker Tests (3 tests):**

7. `test_collect_keyword_planner_batch_builds_seeds`:
   - Mock variant_index query to return product_title for SKU
   - Mock search_queries query to return top 3 search terms
   - Mock KeywordPlannerClient.get_historical_metrics() to return metrics
   - Call `collect_keyword_planner_batch(["SKU-1"])`
   - Assert: get_historical_metrics called with product_title + top search terms as keywords
   - Assert: use_cache=True and cache_max_age_days=30 passed

8. `test_collect_keyword_planner_batch_no_product_title`:
   - Mock variant_index to return record with NULL product_title
   - Call `collect_keyword_planner_batch(["SKU-1"])`
   - Assert: Falls back to using master_sku as keyword seed
   - Assert: Still calls get_historical_metrics (with just the SKU identifier)

9. `test_collect_keyword_planner_batch_returns_enrichment_count`:
   - Mock full pipeline with 5 keywords enriched
   - Call `collect_keyword_planner_batch(["SKU-1"])`
   - Assert: Result includes keywords_enriched count

**Custom Labels Worker Tests (3 tests):**

10. `test_collect_custom_labels_batch_syncs_labels`:
    - Mock fetch_merchant_center_items() to return items with customLabel0
    - Mock variant_index query to return offer_ids for SKU
    - Mock Supabase update
    - Call `collect_custom_labels_batch(["SKU-1"])`
    - Assert: fetch_merchant_center_items called once
    - Assert: variant_index updated with custom_label_0 value

11. `test_collect_custom_labels_batch_missing_in_gmc`:
    - Mock fetch_merchant_center_items() to return items without the SKU's offer_id
    - Call `collect_custom_labels_batch(["SKU-1"])`
    - Assert: Returns result with status "not_in_gmc" (not an error, just no data)

12. `test_collect_custom_labels_batch_empty_batch`:
    - Call `collect_custom_labels_batch([])`
    - Assert: Returns empty list
    - Assert: fetch_merchant_center_items NOT called (optimization: skip API call for empty batch)

**General Pattern Tests (2 tests):**

13. `test_all_workers_return_correct_shape`:
    - For each of the 4 workers, mock all dependencies
    - Call with `["SKU-1"]`
    - Assert: Each returns `list[dict]`
    - Assert: Each dict has at minimum `item_id` and `status` keys

14. `test_all_workers_handle_empty_batch`:
    - For each of the 4 workers
    - Call with `[]`
    - Assert: Returns empty list without errors
    - Assert: No API calls made

Total: 14 tests.

Mock patterns:
- Use `@pytest.fixture` for common mock setups
- Use `unittest.mock.patch` targeting the import path in workers.py
- Use `pytest.mark.asyncio` for async tests
- Follow existing test_processor.py patterns for mock Supabase
  </action>
  <verify>
Run: `PYTHONPATH=./src .venv/bin/python -m pytest tests/test_jobs/test_workers.py -v --tb=short`
All 14 tests should pass.
  </verify>
  <done>
14 tests passing:
- 3 search terms tests (client calls, empty, filtering)
- 3 performance tests (aggregation, no variants, timestamps)
- 3 keyword planner tests (seed building, no title fallback, enrichment count)
- 3 custom labels tests (sync, missing in GMC, empty batch)
- 2 general tests (return shape, empty batch handling)
All workers validated against BatchProcessor contract.
  </done>
</task>

</tasks>

<verification>
1. `PYTHONPATH=./src .venv/bin/python -m pytest tests/test_jobs/test_workers.py -v` - all 14 tests pass
2. All tests use mocks (no real API calls or database connections)
3. Tests validate the process_fn contract (async, list[str] -> list[dict])
4. Tests validate idempotent write patterns (upsert with on_conflict)
5. Tests validate timestamp inclusion (DATA-10)
6. Tests validate edge cases (empty batches, missing data)
</verification>

<success_criteria>
- 14 tests exist in tests/test_jobs/test_workers.py
- All tests pass with `pytest -v`
- Each of the 4 workers has at least 3 dedicated tests
- Tests verify correct client library calls (not re-implementing logic)
- Tests verify idempotent write patterns
- Tests verify error/edge case handling
- No flaky tests (all use mocks, no timing dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-collection-pipeline/06-03-SUMMARY.md`
</output>
