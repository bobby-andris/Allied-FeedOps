---
phase: 06-data-collection-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/feedops/api/backfill.py
  - src/feedops/api/main.py
autonomous: true

must_haves:
  truths:
    - "Backfill endpoint routes to correct worker function based on job_type"
    - "search_terms jobs use google_ads_limiter (10 QPS)"
    - "performance_metrics jobs use google_ads_limiter (10 QPS)"
    - "keyword_planner jobs use keyword_planner_limiter (2 QPS)"
    - "custom_labels jobs use no rate limiter (GMC API has different limits)"
    - "full_backfill jobs execute all 4 collection types sequentially"
  artifacts:
    - path: "src/feedops/api/backfill.py"
      provides: "Job type routing logic replacing _noop_process"
      contains: "collect_search_terms_batch|collect_performance_batch|collect_keyword_planner_batch|collect_custom_labels_batch"
  key_links:
    - from: "src/feedops/api/backfill.py"
      to: "src/feedops/jobs/workers.py"
      via: "import and route by job_type"
      pattern: "from feedops.jobs.workers import"
    - from: "src/feedops/api/backfill.py"
      to: "src/feedops/jobs/rate_limiter.py"
      via: "select rate limiter by job_type"
      pattern: "google_ads_limiter|keyword_planner_limiter"
---

<objective>
Wire the 4 collection workers from Plan 06-01 into the backfill API endpoints, replacing the Phase 1 `_noop_process` placeholder with real job-type routing.

Purpose: Without this wiring, the backfill endpoints create jobs but process them with a no-op. This plan connects the workers to make the endpoints functional.

Output: Updated `src/feedops/api/backfill.py` with job-type routing and rate limiter selection.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-collection-pipeline/06-01-SUMMARY.md

# Current backfill API (has _noop_process placeholder)
@src/feedops/api/backfill.py

# Rate limiters
@src/feedops/jobs/rate_limiter.py

# Workers to wire in
@src/feedops/jobs/workers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _noop_process with job-type routing</name>
  <files>src/feedops/api/backfill.py</files>
  <action>
Update `src/feedops/api/backfill.py` to route backfill jobs to the correct worker function based on job_type:

**1. Add worker function map**

After the existing imports section, create a function that returns the correct process_fn and rate_limiter for a given job_type:

```python
def _get_worker_config(job_type: str) -> tuple[Callable, TokenBucket | None]:
    """Get the worker function and rate limiter for a job type.

    Returns:
        Tuple of (process_fn, rate_limiter)
    """
    from feedops.jobs.workers import (
        collect_search_terms_batch,
        collect_performance_batch,
        collect_keyword_planner_batch,
        collect_custom_labels_batch,
    )
    from feedops.jobs.rate_limiter import google_ads_limiter, keyword_planner_limiter

    config_map = {
        "search_terms": (collect_search_terms_batch, google_ads_limiter),
        "performance_metrics": (collect_performance_batch, google_ads_limiter),
        "keyword_planner": (collect_keyword_planner_batch, keyword_planner_limiter),
        "custom_labels": (collect_custom_labels_batch, None),
    }

    if job_type not in config_map:
        raise ValueError(f"Unknown job type: {job_type}")

    return config_map[job_type]
```

**2. Update `_start_background_processing` to use real workers**

Replace the `_noop_process` usage in `_start_background_processing`:

- Get worker config via `_get_worker_config(job.job_type)` using the job's type from the database
- Pass the correct `process_fn` and `rate_limiter` to the BatchProcessor
- Handle `full_backfill` type by running all 4 job types sequentially:
  1. Create 4 sub-processors (search_terms, performance_metrics, keyword_planner, custom_labels)
  2. Run each sequentially (not parallel, to avoid rate limit conflicts)
  3. Track overall progress across all 4 phases

For `full_backfill` implementation:
- Run search_terms first (gets campaign data that later workers benefit from)
- Then performance_metrics
- Then keyword_planner (uses search terms as seed keywords)
- Then custom_labels
- Update progress incrementally (each sub-job is 25% of total)

**3. Remove the `_noop_process` function**

Delete the `_noop_process` function entirely. It was only for Phase 1 testing.

**4. Add `job_type` parameter to `_start_background_processing`**

The function currently receives `skus` and `config` but not the job_type. It reads the job to get the type, which works, but for clarity add the job_type to the function signature and use it directly for routing.

IMPORTANT: Keep the `run_async_in_thread()` pattern for background processing. Do NOT switch to FastAPI BackgroundTasks.

IMPORTANT: The `start_backfill` and `resume_backfill` endpoint handlers should pass the job_type through to `_start_background_processing`.
  </action>
  <verify>
Run: `PYTHONPATH=./src python -c "from feedops.api.backfill import _get_worker_config; fn, rl = _get_worker_config('search_terms'); print(f'fn={fn.__name__}, rl={rl}')"`
Verify that `_noop_process` is no longer in the file: `grep -c '_noop_process' src/feedops/api/backfill.py` should return 0.
  </verify>
  <done>
- _noop_process replaced with _get_worker_config routing function
- _start_background_processing uses correct worker + rate limiter per job_type
- full_backfill runs all 4 job types sequentially
- Endpoints pass job_type through to background processing
  </done>
</task>

<task type="auto">
  <name>Task 2: Update endpoint docs and verify registration</name>
  <files>src/feedops/api/backfill.py, src/feedops/api/main.py</files>
  <action>
**1. Update module docstring** in `backfill.py`:

Replace "Phase 1: Uses _noop_process placeholder" references with documentation of the real job routing:
- Document which job_types map to which workers
- Document which rate limiters are used per job_type
- Document the full_backfill sequential execution order

**2. Update `_start_background_processing` docstring**:

Replace the "Phase 1" / "Phase 2" references with current documentation explaining:
- How job_type determines worker selection
- How rate limiter is selected
- How full_backfill works

**3. Verify main.py endpoint registration** is still correct:

The 4 endpoints (/backfill/start, /backfill/status/{job_id}, /backfill/resume/{job_id}, /backfill/jobs) should still be registered in main.py. No changes needed to main.py unless endpoint signatures changed. Just verify.

**4. Update the config hint in StartBackfillRequest docstring**:

Add documentation for useful config options:
- `batch_size` (default 10): Items per batch
- `checkpoint_interval` (default 100): Checkpoint save frequency
- `days_lookback` (default 180): Date range for data collection

This is documentation cleanup only -- no logic changes in this task.
  </action>
  <verify>
Run: `PYTHONPATH=./src python -c "from feedops.api.main import app; routes = [r.path for r in app.routes if 'backfill' in str(getattr(r, 'path', ''))]; print(routes); assert len(routes) >= 4"`
Verify no references to `_noop_process` or "Phase 1" remain in backfill.py: `grep -c 'noop\|Phase 1' src/feedops/api/backfill.py` should return 0.
  </verify>
  <done>
- Module docstring reflects real job routing (not placeholder)
- All endpoint registrations verified
- StartBackfillRequest documents config options
- No stale Phase 1 references remain
  </done>
</task>

</tasks>

<verification>
1. `_noop_process` no longer exists in backfill.py
2. `_get_worker_config` correctly maps all 5 job types (search_terms, performance_metrics, keyword_planner, custom_labels, full_backfill)
3. Rate limiters correctly assigned: google_ads_limiter for search_terms/performance_metrics, keyword_planner_limiter for keyword_planner, None for custom_labels
4. `full_backfill` runs all 4 types sequentially
5. All 4 existing endpoints still registered and functional
6. No "Phase 1" or "_noop" references remain
</verification>

<success_criteria>
- POST /backfill/start with job_type="search_terms" routes to collect_search_terms_batch with google_ads_limiter
- POST /backfill/start with job_type="performance_metrics" routes to collect_performance_batch with google_ads_limiter
- POST /backfill/start with job_type="keyword_planner" routes to collect_keyword_planner_batch with keyword_planner_limiter
- POST /backfill/start with job_type="custom_labels" routes to collect_custom_labels_batch with no rate limiter
- POST /backfill/start with job_type="full_backfill" runs all 4 sequentially
- Resume endpoint correctly resumes with the right worker for the job's type
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-collection-pipeline/06-02-SUMMARY.md`
</output>
