---
phase: 20-targeted-fixes-intelligence-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedops/config/shopping_intelligence.yaml
  - src/feedops/pipeline/shopping_intelligence.py
  - src/feedops/pipeline/prompts.py
  - src/feedops/providers/openai_provider.py
autonomous: true
requirements: [MODEL-03, GOOG-04]

must_haves:
  truths:
    - "Shopping intelligence YAML config loads and caches at container startup"
    - "get_universal_rules() returns formatted Shopping optimization rules for prompt injection"
    - "get_category_intelligence() returns category-specific guidance keyed by custom_label_0"
    - "GPT-5.2 accuracy guardrail is strengthened in SYSTEM_PROMPT"
  artifacts:
    - path: "src/feedops/config/shopping_intelligence.yaml"
      provides: "Three-tier Google Shopping intelligence config"
      contains: "universal_rules"
    - path: "src/feedops/pipeline/shopping_intelligence.py"
      provides: "YAML loader with lru_cache + formatting functions"
      exports: ["get_universal_rules", "get_category_intelligence", "get_shopping_intelligence_section"]
    - path: "src/feedops/pipeline/prompts.py"
      provides: "Strengthened accuracy guardrail in SYSTEM_PROMPT"
      contains: "accuracy"
  key_links:
    - from: "src/feedops/pipeline/shopping_intelligence.py"
      to: "src/feedops/config/shopping_intelligence.yaml"
      via: "yaml.safe_load with lru_cache"
      pattern: "lru_cache.*_load_shopping_intelligence"
---

<objective>
Create the Google Shopping intelligence configuration and loader module, and complete the GPT-5.2 model switch by strengthening the accuracy guardrail.

Purpose: Establishes the Shopping intelligence foundation that Plan 03 will wire into the shared prompt builder. Completes MODEL-03 (the model switch is 90% done — only the accuracy guardrail remains). These are independent foundation pieces with no upstream dependencies.

Output: `shopping_intelligence.yaml` config, `shopping_intelligence.py` loader module, strengthened SYSTEM_PROMPT accuracy guardrail.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-targeted-fixes-intelligence-application/20-RESEARCH.md

# Key source files
@src/feedops/pipeline/prompts.py
@src/feedops/pipeline/collection_descriptions.py
@src/feedops/pipeline/segment_strategy.py
@src/feedops/providers/openai_provider.py
@docs/research/google-shopping-ranking-factors.md
@docs/research/competitive-gap-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Shopping intelligence YAML config and Python loader</name>
  <files>
    src/feedops/config/shopping_intelligence.yaml
    src/feedops/pipeline/shopping_intelligence.py
  </files>
  <action>
**Step 1: Query database for custom_label_0 categories.**
Run `SELECT DISTINCT custom_label_0 FROM product_catalog WHERE custom_label_0 IS NOT NULL ORDER BY custom_label_0` via Supabase MCP to get the actual list of categories. Use this list to populate category_rules in the YAML.

**Step 2: Create `src/feedops/config/shopping_intelligence.yaml`.**
Three-tier structure per CONTEXT.md locked decision:

1. **universal_rules** — fundamental Shopping ranking factors:
   - Title structure: Lead with `[Finish Name] [Size] [Product Type]` in first 70 chars. Never lead with collection name or brand.
   - Material differentiator: Include "Solid Brass" in every title.
   - Description structure: First sentence = `[Finish] [Product Type] for bathroom installation. [Material]. [Key dimension]. [Key functional claim].`
   - Finish specificity: Integrate finish name naturally in first sentence. Never use "Available in [Finish]." pattern.
   - Front-load specs: Key specs in first 160 chars of description (visible in Shopping preview).

2. **category_rules** — keyed by lowercase custom_label_0 value. Include AT MINIMUM entries from Phase 17 research for: grab bars, towel bars, garment rods, paper towel holders, retractable hooks, soap dispensers, towel rings, robe hooks, shelves, toilet paper holders. For each category include: `intent_keywords`, `title_instruction`, and optional `evidence` or `note` from Phase 17 data.

3. **allied_brass_usp** — dual positioning (beautiful design + functional), solid brass quality, 28-finish variety.

Include `version: "1.0"` and `last_updated` date.

**Step 3: Create `src/feedops/pipeline/shopping_intelligence.py`.**
Follow the exact pattern from `collection_descriptions.py` (lru_cache, Path resolution relative to `__file__`):

```python
from functools import lru_cache
from pathlib import Path
import yaml

@lru_cache(maxsize=1)
def _load_shopping_intelligence() -> dict:
    path = Path(__file__).parent.parent / "config" / "shopping_intelligence.yaml"
    with path.open() as f:
        return yaml.safe_load(f)

def get_universal_rules() -> str:
    """Format universal Shopping rules for prompt injection."""

def get_category_intelligence(custom_label_0: str | None) -> str:
    """Format category-specific rules for prompt injection. Returns empty string if no match."""

def get_shopping_intelligence_section(custom_label_0: str | None) -> str:
    """Format the complete Shopping intelligence section for prompt injection.
    Combines universal rules + category rules + USP."""
```

The `get_shopping_intelligence_section()` function returns a formatted block starting with `=== GOOGLE SHOPPING OPTIMIZATION ===` as shown in the research examples.

CRITICAL: The YAML file MUST be inside `src/feedops/config/` (inside the `src/` tree) so it gets copied into the Cloud Run container. Placing it outside `src/` will cause a runtime FileNotFoundError in production.

Ensure `src/feedops/config/__init__.py` exists (create if missing).
  </action>
  <verify>
Run `cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src python -c "from feedops.pipeline.shopping_intelligence import get_shopping_intelligence_section; print(get_shopping_intelligence_section('grab bars')[:200])"` — should print formatted Shopping intelligence text starting with "=== GOOGLE SHOPPING OPTIMIZATION ===".

Also verify: `python -c "from feedops.pipeline.shopping_intelligence import get_category_intelligence; print(get_category_intelligence(None))"` returns empty string (graceful fallback for unknown categories).
  </verify>
  <done>Shopping intelligence YAML loads successfully, get_shopping_intelligence_section() returns formatted prompt section for known categories, returns universal rules only for unknown categories.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen GPT-5.2 accuracy guardrail in SYSTEM_PROMPT</name>
  <files>
    src/feedops/pipeline/prompts.py
    src/feedops/providers/openai_provider.py
  </files>
  <action>
**Step 1: Read `src/feedops/pipeline/prompts.py` fully** to find the SYSTEM_PROMPT string and its P0_GLOBAL_FACTUAL_RULES section.

**Step 2: Add accuracy guardrail to SYSTEM_PROMPT.**
In the P0_GLOBAL_FACTUAL_RULES section (or equivalent priority-0 section), add:

```
ACCURACY GUARDRAIL (ABSOLUTE):
- Every claim in title and description MUST be verifiable from the product evidence table.
- NEVER invent specifications, dimensions, materials, certifications, or features not in the evidence.
- If evidence is ambiguous or incomplete, use conservative language ("designed for", "suitable for") rather than specific claims.
- Solid brass construction: Only claim when evidence confirms material. Most Allied Brass products ARE solid brass, but verify per SKU.
- ADA compliance: Only include "ADA Compliant" when evidence explicitly confirms certification.
```

This goes in the SYSTEM_PROMPT (static, cacheable) because it's a universal rule that applies to ALL generations, not per-SKU.

**Step 3: Verify `openai_provider.py` defaults.**
Read the file to confirm `gpt-5.2` is already the default model and `max_completion_tokens` is already handled for gpt-5.x. Document any remaining `gpt-4o` references. If any exist, update them to `gpt-5.2` (except in comments that document history).

**Step 4: Verify no lingering gpt-4o references in factory.py.**
Read `src/feedops/providers/factory.py` and confirm the FEEDOPS_OPENAI_MODEL env var defaults to gpt-5.2. Clean up any stale gpt-4o defaults if found.
  </action>
  <verify>
Run `cd /Users/bobby/Documents/GitHub/Allied-FeedOps && grep -n "ACCURACY GUARDRAIL" src/feedops/pipeline/prompts.py` — should find the new section.

Run `grep -rn "gpt-4o" src/feedops/providers/` — should only appear in comments or historical references, not as active defaults.

Run `PYTHONPATH=./src python -c "from feedops.pipeline.prompts import SYSTEM_PROMPT; assert 'ACCURACY GUARDRAIL' in SYSTEM_PROMPT; print('OK')"` — must print OK.
  </verify>
  <done>SYSTEM_PROMPT contains strengthened accuracy guardrail. No active gpt-4o defaults remain in provider code. MODEL-03 is complete.</done>
</task>

</tasks>

<verification>
1. `shopping_intelligence.yaml` exists at `src/feedops/config/shopping_intelligence.yaml` with universal_rules, category_rules, and allied_brass_usp sections
2. `shopping_intelligence.py` exports `get_universal_rules`, `get_category_intelligence`, `get_shopping_intelligence_section`
3. SYSTEM_PROMPT in `prompts.py` contains accuracy guardrail text
4. No active `gpt-4o` defaults in `openai_provider.py` or `factory.py`
5. Python imports work: `from feedops.pipeline.shopping_intelligence import get_shopping_intelligence_section`
</verification>

<success_criteria>
- Shopping intelligence config file loadable and formatted for prompt injection
- Accuracy guardrail added to SYSTEM_PROMPT
- MODEL-03 complete (GPT-5.2 default confirmed, accuracy guardrail strengthened)
- GOOG-04 foundation ready for Plan 03 to wire into prompt builder
</success_criteria>

<output>
After completion, create `.planning/phases/20-targeted-fixes-intelligence-application/20-01-SUMMARY.md`
</output>
