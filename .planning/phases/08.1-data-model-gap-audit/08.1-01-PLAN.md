---
phase: 08.1-data-model-gap-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedops/utils/__init__.py
  - src/feedops/utils/offer_id.py
  - tests/test_offer_id_normalize.py
  - supabase/migrations/043_variant_performance_tables.sql
autonomous: true
requirements: [ENTM-01, DATA-02]

must_haves:
  truths:
    - "normalize_offer_id('shopify_US_123_456') returns 'shopify_us_123_456'"
    - "to_gmc_format('shopify_us_123_456') returns 'shopify_US_123_456'"
    - "Empty string and None inputs are handled safely by both functions"
    - "performance_snapshots_variant table exists in Supabase with correct unique constraint"
    - "performance_baselines_variant table exists in Supabase with correct PK"
  artifacts:
    - path: "src/feedops/utils/__init__.py"
      provides: "Package init for utils module"
    - path: "src/feedops/utils/offer_id.py"
      provides: "Shared offer ID normalization utility"
      exports: ["normalize_offer_id", "to_gmc_format"]
    - path: "tests/test_offer_id_normalize.py"
      provides: "Unit tests for offer ID normalization"
      min_lines: 30
    - path: "supabase/migrations/043_variant_performance_tables.sql"
      provides: "Migration creating variant-level performance tables"
      contains: "CREATE TABLE"
  key_links:
    - from: "src/feedops/utils/offer_id.py"
      to: "tests/test_offer_id_normalize.py"
      via: "import"
      pattern: "from feedops.utils.offer_id import"
---

<objective>
Create the offer ID normalization utility (ENTM-01) and variant performance tables (migration 043) that all subsequent plans depend on.

Purpose: These are the foundational artifacts -- the shared utility and new tables -- that Plans 02 and 04 build on. The utility centralizes the existing inline `.lower()` pattern from `google_ads_search_terms.py` into a single importable module. The migration creates `performance_snapshots_variant` and `performance_baselines_variant` tables per locked decision (new tables, not extending existing ones).

Output: `feedops/utils/offer_id.py` with tests, migration 043 applied to production Supabase.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-data-model-gap-audit/08.1-CONTEXT.md
@.planning/phases/08.1-data-model-gap-audit/08.1-RESEARCH.md
@.planning/phases/08-schema-hardening/08-01-SUMMARY.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/feedops/integrations/google_ads_search_terms.py (line ~424-426):
```python
# Existing inline normalization to generalize:
gmc_offer_id = gmc_offer_id.lower()
```

From supabase/migrations/042_schema_hardening.sql (constraint pattern):
```sql
-- Mirror this pattern for variant tables:
CONSTRAINT uq_performance_snapshots_daily
    UNIQUE (master_sku, platform, environment, snapshot_date)
CONSTRAINT chk_performance_snapshots_platform
    CHECK (platform IN ('google', 'bing', 'shopify'))
```

Migration apply pattern (from Phase 8 summary):
```bash
# Retrieve Supabase personal access token from macOS keychain:
security find-generic-password -s "Supabase CLI" -w | base64 -d
# Apply via management API:
curl -X POST "https://api.supabase.com/v1/projects/qezuszwufortkiutlhym/database/query" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query": "..."}'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create offer ID normalization utility with TDD</name>
  <files>src/feedops/utils/__init__.py, src/feedops/utils/offer_id.py, tests/test_offer_id_normalize.py</files>
  <behavior>
    - normalize_offer_id("shopify_US_123_456") returns "shopify_us_123_456"
    - normalize_offer_id("shopify_us_123_456") returns "shopify_us_123_456" (already lowercase, no-op)
    - normalize_offer_id("") returns "" (empty string safe)
    - normalize_offer_id(None) returns None (None safe)
    - to_gmc_format("shopify_us_123_456") returns "shopify_US_123_456"
    - to_gmc_format("shopify_US_123_456") returns "shopify_US_123_456" (already uppercase, idempotent)
    - to_gmc_format("") returns "" (empty string safe)
    - to_gmc_format(None) returns None (None safe)
  </behavior>
  <action>
    RED: Create `tests/test_offer_id_normalize.py` with all 8 behavior tests above. Create empty `src/feedops/utils/__init__.py` and `src/feedops/utils/offer_id.py` with stub functions that raise NotImplementedError. Run tests -- all must FAIL.

    GREEN: Implement `normalize_offer_id()` as `offer_id.lower()` with empty/None guards. Implement `to_gmc_format()` as `offer_id.replace("shopify_us_", "shopify_US_")` with empty/None guards. Run tests -- all must PASS.

    The utility is pure Python standard library -- no external dependencies. Follow the exact function signatures from RESEARCH.md Pattern 1.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src .venv/bin/python -m pytest tests/test_offer_id_normalize.py -x -v</automated>
  </verify>
  <done>Both functions pass all 8 behavior tests. Module importable as `from feedops.utils.offer_id import normalize_offer_id, to_gmc_format`.</done>
</task>

<task type="auto">
  <name>Task 2: Create and apply migration 043 for variant performance tables</name>
  <files>supabase/migrations/043_variant_performance_tables.sql</files>
  <action>
    Create `supabase/migrations/043_variant_performance_tables.sql` with two tables per RESEARCH.md Pattern 2:

    1. `performance_snapshots_variant`:
       - Columns: id (bigserial PK), gmc_offer_id (text NOT NULL), master_sku (text NOT NULL), platform (text NOT NULL), environment (text NOT NULL DEFAULT 'production'), snapshot_date (date NOT NULL), impressions (integer NOT NULL DEFAULT 0), clicks (integer NOT NULL DEFAULT 0), ctr (real NOT NULL DEFAULT 0.0), conversions (integer NOT NULL DEFAULT 0), conversion_value (numeric(18,6) NOT NULL DEFAULT 0.0), cost (numeric(18,6) NOT NULL DEFAULT 0.0), roas (real NOT NULL DEFAULT 0.0), fetched_at (timestamptz NOT NULL DEFAULT now())
       - UNIQUE constraint: `uq_variant_snapshots_daily (gmc_offer_id, platform, environment, snapshot_date)` -- this MUST match the on_conflict columns that will be used in Plan 02's dual-write upsert
       - CHECK constraints: platform IN ('google', 'bing', 'shopify'), environment IN ('staging', 'production')
       - Indexes: `idx_variant_snapshots_master_sku (master_sku, platform, snapshot_date)`, `idx_variant_snapshots_offer_date (gmc_offer_id, snapshot_date DESC)`

    2. `performance_baselines_variant`:
       - PK: `(gmc_offer_id, platform)` -- composite PK, same pattern as `performance_baselines` PK on `(master_sku, platform)`
       - Columns: gmc_offer_id (text NOT NULL), master_sku (text NOT NULL), platform (text NOT NULL), baseline_start_date (date NOT NULL), baseline_end_date (date NOT NULL), avg_impressions (real), avg_clicks (real), avg_ctr (real), avg_conversions (real), avg_conversion_value (numeric(18,6)), avg_cvr (real), avg_cost (numeric(18,6)), avg_roas (real), created_at (timestamptz NOT NULL DEFAULT now())
       - CHECK constraint: platform IN ('google', 'bing', 'shopify')
       - Index: `idx_baselines_variant_master_sku (master_sku, platform)`

    Use `CREATE TABLE IF NOT EXISTS` for idempotency. Use `CREATE INDEX IF NOT EXISTS` for indexes.

    Apply the migration to production Supabase using the management API pattern from Phase 8:
    1. Retrieve personal access token: `security find-generic-password -s "Supabase CLI" -w` then base64 decode
    2. POST to `https://api.supabase.com/v1/projects/qezuszwufortkiutlhym/database/query`
    3. Verify tables exist: `SELECT table_name FROM information_schema.tables WHERE table_name LIKE 'performance_%_variant'`
    4. Verify constraints: `SELECT constraint_name FROM information_schema.table_constraints WHERE table_name = 'performance_snapshots_variant'`
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && cat supabase/migrations/043_variant_performance_tables.sql | grep -c "CREATE TABLE IF NOT EXISTS"</automated>
  </verify>
  <done>Migration file committed. Both variant tables exist in production Supabase with correct unique constraints, CHECK constraints, and indexes. `SELECT COUNT(*) FROM performance_snapshots_variant` returns 0 (empty, ready for data).</done>
</task>

</tasks>

<verification>
- `PYTHONPATH=./src .venv/bin/python -m pytest tests/test_offer_id_normalize.py -x -v` -- all 8+ tests pass
- `python -c "from feedops.utils.offer_id import normalize_offer_id, to_gmc_format; print('OK')"` -- imports work
- Migration 043 applied -- variant tables exist in production Supabase
- Full test suite: `PYTHONPATH=./src .venv/bin/python -m pytest tests/ -v` -- no regressions
</verification>

<success_criteria>
- `normalize_offer_id` and `to_gmc_format` functions pass all behavior tests
- `performance_snapshots_variant` table exists with `uq_variant_snapshots_daily` unique constraint
- `performance_baselines_variant` table exists with composite PK `(gmc_offer_id, platform)`
- No test regressions in full suite
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-data-model-gap-audit/08.1-01-SUMMARY.md`
</output>
