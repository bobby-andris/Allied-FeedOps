---
phase: 08.1-data-model-gap-audit
plan: 02
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - src/feedops/monitoring/performance_impact.py
  - src/feedops/integrations/google_ads_performance.py
  - src/feedops/integrations/google_ads_search_terms.py
  - src/feedops/api/performance_baseline.py
autonomous: true
requirements: [ENTM-02]

must_haves:
  truths:
    - "All Python codepaths that handle offer IDs use normalize_offer_id() instead of inline .lower()"
    - "Google Ads API response offer IDs are normalized before any DB lookup or storage"
    - "collect_daily_performance_snapshots() writes variant-level rows to performance_snapshots_variant BEFORE aggregating to master_sku"
    - "Only rows with impressions > 0 are written to performance_snapshots_variant (skip-zero-rows decision)"
  artifacts:
    - path: "src/feedops/monitoring/performance_impact.py"
      provides: "Dual-write: variant snapshots + master_sku snapshots"
      contains: "performance_snapshots_variant"
    - path: "src/feedops/integrations/google_ads_performance.py"
      provides: "Normalized offer ID keys in fetch results"
      contains: "normalize_offer_id"
    - path: "src/feedops/integrations/google_ads_search_terms.py"
      provides: "Shared utility replacing inline .lower()"
      contains: "from feedops.utils.offer_id import normalize_offer_id"
    - path: "src/feedops/api/performance_baseline.py"
      provides: "Normalized offer IDs in baseline capture"
      contains: "normalize_offer_id"
  key_links:
    - from: "src/feedops/monitoring/performance_impact.py"
      to: "performance_snapshots_variant"
      via: "supabase upsert"
      pattern: 'supabase\.table\("performance_snapshots_variant"\)\.upsert'
    - from: "src/feedops/integrations/google_ads_performance.py"
      to: "src/feedops/utils/offer_id.py"
      via: "import normalize_offer_id"
      pattern: "from feedops.utils.offer_id import normalize_offer_id"
---

<objective>
Apply offer ID normalization across all 4 Python codepaths (ENTM-02) and add variant-level dual-write to the daily snapshot job.

Purpose: This is the core wiring plan -- connecting the utility from Plan 01 to every data ingestion point, and adding the variant snapshot dual-write so `performance_snapshots_variant` starts receiving data from the daily Cloud Scheduler job. Per locked decision: normalize on INGESTION, write variant rows BEFORE aggregating to master_sku, skip zero-impression rows.

Output: All 4 Python files updated with `normalize_offer_id()` imports; `collect_daily_performance_snapshots()` writes to both tables.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-data-model-gap-audit/08.1-CONTEXT.md
@.planning/phases/08.1-data-model-gap-audit/08.1-RESEARCH.md
@.planning/phases/08.1-data-model-gap-audit/08.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (the utility to import): -->
```python
# src/feedops/utils/offer_id.py
def normalize_offer_id(offer_id: str) -> str:
    """Normalize offer ID to canonical lowercase form (DB format)."""
    ...

def to_gmc_format(offer_id: str) -> str:
    """Convert offer ID to GMC publish format (uppercase US)."""
    ...
```

<!-- Target table schema from migration 043: -->
```sql
-- performance_snapshots_variant upsert target:
CONSTRAINT uq_variant_snapshots_daily
    UNIQUE (gmc_offer_id, platform, environment, snapshot_date)
-- on_conflict must be: "gmc_offer_id,platform,environment,snapshot_date"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply offer ID normalization to all 4 Python codepaths</name>
  <files>src/feedops/integrations/google_ads_performance.py, src/feedops/integrations/google_ads_search_terms.py, src/feedops/api/performance_baseline.py, src/feedops/monitoring/performance_impact.py</files>
  <action>
    Update 4 files to use `from feedops.utils.offer_id import normalize_offer_id` instead of inline string operations:

    **1. `google_ads_performance.py` (~line 332 in `_fetch_chunk_data()`):**
    - After reading `product_id = row.get("segments", {}).get("product_item_id", "")`, apply `product_id = normalize_offer_id(product_id)` before appending to results.
    - This ensures all returned keys from `fetch_batch_product_performance()` are lowercase, matching the `offer_to_sku` dict keys built from `variant_index`.
    - Add import at top of file.

    **2. `google_ads_search_terms.py` (~line 424-426 in `get_variant_info()`):**
    - Replace `gmc_offer_id = gmc_offer_id.lower()` with `gmc_offer_id = normalize_offer_id(gmc_offer_id)`.
    - Also in `_preload_variant_cache()` where `.lower()` is used on offer ID keys.
    - Add import at top of file.

    **3. `performance_baseline.py` (~line 196 in baseline capture):**
    - In the `performance_data.items()` loop, normalize each key: `for offer_id, data in performance_data.items(): offer_id = normalize_offer_id(offer_id)`.
    - Add import at top of file.

    **4. `performance_impact.py` (in `collect_daily_performance_snapshots`):**
    - In the `offer_to_sku` dict construction (where `variant_index` rows are iterated), normalize the offer_id key: `offer_to_sku[normalize_offer_id(offer_id)] = sku`.
    - In the `by_offer.items()` loop, normalize the offer_id key before lookup: `normalized_id = normalize_offer_id(offer_id)`.
    - Add import at top of file.

    IMPORTANT: Do NOT touch the TypeScript side (google-sheets.ts) -- that is explicitly deferred per locked decision.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src .venv/bin/python -m pytest tests/ -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files import `normalize_offer_id` from `feedops.utils.offer_id`. No inline `.lower()` calls on offer IDs remain in these files. Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add variant-level dual-write to collect_daily_performance_snapshots</name>
  <files>src/feedops/monitoring/performance_impact.py</files>
  <action>
    In `collect_daily_performance_snapshots()` in `performance_impact.py`, add variant snapshot writes BEFORE the existing `aggregated_by_sku` loop (per RESEARCH.md Pattern 3):

    1. After the `by_offer` dict is populated from `fetch_batch_product_performance()`, build a `variant_snapshot_payload` list.
    2. For each `(offer_id, metrics)` in `by_offer.items()`:
       - Skip if `impressions == 0` (locked decision: skip zero rows)
       - Normalize the offer_id: `normalized_id = normalize_offer_id(offer_id)`
       - Look up master_sku from `offer_to_sku` using normalized_id
       - Skip if no master_sku found (unmatched offer ID)
       - Build row dict with: gmc_offer_id, master_sku, platform, environment, snapshot_date, impressions, clicks, ctr (computed), conversions, conversion_value, cost, roas (computed), fetched_at
    3. If `variant_snapshot_payload` is not empty, upsert to `performance_snapshots_variant` with `on_conflict="gmc_offer_id,platform,environment,snapshot_date"`.
    4. Log the count: `logger.info(f"Wrote {len(variant_snapshot_payload)} variant snapshot rows")`
    5. This code block MUST come BEFORE the existing aggregation loop that builds `aggregated_by_sku`.

    Follow the exact payload structure from RESEARCH.md Pattern 3. Use `ignore_duplicates=False` (standard upsert, overwrites on conflict -- variant snapshots should update if re-run for same day).

    Key: The `on_conflict` column list `"gmc_offer_id,platform,environment,snapshot_date"` MUST exactly match the `uq_variant_snapshots_daily` UNIQUE constraint from migration 043. This is the same pattern that caused the 42P10 error in Phase 8 when mismatched -- do not deviate.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && grep -c "performance_snapshots_variant" src/feedops/monitoring/performance_impact.py</automated>
  </verify>
  <done>The `collect_daily_performance_snapshots()` function writes variant-level rows to `performance_snapshots_variant` before aggregating to master_sku level. The upsert uses the correct on_conflict columns matching the unique constraint. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `grep -r "from feedops.utils.offer_id import" src/feedops/` shows 4 files importing the utility
- `grep "performance_snapshots_variant" src/feedops/monitoring/performance_impact.py` confirms dual-write exists
- `PYTHONPATH=./src .venv/bin/python -m pytest tests/ -v` -- full suite passes, no regressions
- No remaining inline `.lower()` on offer IDs in the 4 target files (search: `grep -n "\.lower()" src/feedops/integrations/google_ads_performance.py src/feedops/integrations/google_ads_search_terms.py src/feedops/api/performance_baseline.py src/feedops/monitoring/performance_impact.py`)
</verification>

<success_criteria>
- All 4 Python files use `normalize_offer_id()` from the shared utility
- `collect_daily_performance_snapshots()` writes to `performance_snapshots_variant` before aggregating
- Zero-impression rows are skipped for variant snapshots
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-data-model-gap-audit/08.1-02-SUMMARY.md`
</output>
