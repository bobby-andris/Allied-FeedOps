---
phase: 33.1-scoring-calibration
verified: 2026-02-25T20:35:00Z
status: passed
score: 17/17 must-haves verified
re_verification: false
---

# Phase 33.1: Scoring Calibration Verification Report

**Phase Goal:** Fix impact estimation ($0 bug) and calibrate misplacement thresholds to reduce noise from 95% to actionable signal. Reframe UI from "misplaced" to "opportunity" language.
**Verified:** 2026-02-25T20:35:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `estimateImpact()` produces non-zero dollar values for upward tier moves | VERIFIED | ROAS-based formula `spend * (targetROAS - currentROAS)` confirmed in `tier-scoring.ts:345-373`; test asserts `impact.mid > 10` for $5 spend moving LOW→HIGH |
| 2 | `isMisplaced` is gated by three calibration thresholds (delta/confidence/impressions) | VERIFIED | Three-gate logic confirmed at `tier-scoring.ts:234-241`; all three gates must pass simultaneously |
| 3 | `CalibrationConfig` and `DEFAULT_CALIBRATION` centralize all threshold values | VERIFIED | Interface + constant in `tier-scoring.types.ts:8-24` with values `minFitScoreDelta:0.3, minConfidence:0.40, minImpressions:50` |
| 4 | `TermScore` exposes `fitScoreDelta` and `dataConfirmed` for UI and filtering | VERIFIED | Both fields on `TermScore` interface (`tier-scoring.types.ts:77-78`) and populated in `scoreTerm()` return at lines 268-269 |
| 5 | `ImpactRange` includes `direction` field for Phase 34 wasted spend analysis | VERIFIED | `direction: 'upward' | 'downward' | 'lateral'` on `ImpactRange` interface (`tier-scoring.types.ts:105`); populated in `estimateImpact()` at lines 360-363 |
| 6 | API route passes `DEFAULT_CALIBRATION` explicitly and reflects calibrated `totalMisplaced` count | VERIFIED | Explicit import at `route.ts:19`; `scoreTerm(term, ..., DEFAULT_CALIBRATION)` at line 144; `totalMisplaced: scores.filter(s => s.isMisplaced).length` at line 211 |
| 7 | All 30 tests pass including 9 new calibration-specific tests | VERIFIED | `npx vitest run` output: "30 tests passed" including `Phase 33.1: Calibration` describe block with 9 tests |
| 8 | HeroCallout shows "opportunities" (not "misplaced") with gut-assignment subtitle | VERIFIED | Line 55: `{totalMisplaced !== 1 ? 'opportunities' : 'opportunity'}`; subtitle at lines 40-43 |
| 9 | GroupOverview badges say "opportunities" in all three display locations | VERIFIED | Lines 66, 112, 158 of `GroupOverview.tsx` all use `'opportunities'/'opportunity'` |
| 10 | GroupDetail table header says "Optimization Opportunities" | VERIFIED | Line 225: `Optimization Opportunities ({misplacedTerms.length})` |
| 11 | GroupDetail empty state says "No optimization opportunities..." | VERIFIED | Line 232: `"No optimization opportunities in this group — all terms align with current tiers"` |
| 12 | TierDetail section says "Opportunities in {tier} Tier" | VERIFIED | Line 271: `Opportunities in {tier} Tier ({misplacedTerms.length})` |
| 13 | TierDetail well-placed terms show "Data-confirmed" or "Aligned" (not "Well-placed") | VERIFIED | Line 255: `{term.dataConfirmed ? 'Data-confirmed' : 'Aligned'}` consuming `dataConfirmed` from 33.1-01 |
| 14 | TermScorecard shows "Opportunity" badge (not "Misplaced") | VERIFIED | Line 141 of `TermScorecard.tsx`: badge text is `Opportunity` |
| 15 | MisplacedTermRow has clarifying comment for import stability | VERIFIED | Lines 1-4: `/** OpportunityTermRow — displays a term flagged as an optimization opportunity. (File originally named MisplacedTermRow...) */` |
| 16 | Page subtitle updated to calibrated approach language | VERIFIED | Line 114 of `page.tsx`: `"Data-driven tier analysis — surfaces high-confidence opportunities where performance disagrees with current placement"` |
| 17 | No user-facing "misplaced" text remains in component renders (only code-level variable names) | VERIFIED | grep scan found zero user-facing "misplaced" strings; only code variables (`misplacedTerms`, `misplacedCount`, `isMisplaced`) remain |

**Score:** 17/17 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dashboard/src/lib/optimization/tier-scoring.types.ts` | `CalibrationConfig`, `DEFAULT_CALIBRATION`, `fitScoreDelta`/`dataConfirmed` on `TermScore`, `direction` on `ImpactRange` | VERIFIED | All four additions confirmed, file 119 lines with substantive content |
| `dashboard/src/lib/optimization/tier-scoring.ts` | ROAS-based `estimateImpact()`, calibrated `scoreTerm()` with three-gate `isMisplaced` | VERIFIED | 588 lines, both functions fully implemented, no stubs |
| `dashboard/src/lib/optimization/__tests__/tier-scoring.test.ts` | 30 tests passing, 9 in `Phase 33.1: Calibration` block | VERIFIED | All 30 tests pass in 8ms |
| `dashboard/src/app/api/shopping-funnel/tier-scoring/route.ts` | Imports `DEFAULT_CALIBRATION`, passes to `scoreTerm()`, `direction` on inline `ImpactRange` | VERIFIED | Line 19 imports `DEFAULT_CALIBRATION`; line 144 passes to `scoreTerm()`; line 47 has `direction: 'lateral' as const` |
| `dashboard/src/app/(dashboard)/tier-scoring/components/HeroCallout.tsx` | "opportunities" pill, gut-assignment subtitle | VERIFIED | Both changes present and substantive |
| `dashboard/src/app/(dashboard)/tier-scoring/components/GroupOverview.tsx` | "opportunities" in three locations (callout, badge, footer) | VERIFIED | All three confirmed |
| `dashboard/src/app/(dashboard)/tier-scoring/components/GroupDetail.tsx` | "Optimization Opportunities" header, updated empty state and callout | VERIFIED | All three confirmed |
| `dashboard/src/app/(dashboard)/tier-scoring/components/TierDetail.tsx` | "Opportunities in {tier}" header, "Data-confirmed"/"Aligned" status, data-disagrees callout | VERIFIED | All confirmed |
| `dashboard/src/app/(dashboard)/tier-scoring/components/MisplacedTermRow.tsx` | Clarifying comment at top | VERIFIED | Comment at lines 1-4 |
| `dashboard/src/app/(dashboard)/tier-scoring/components/TermScorecard.tsx` | "Opportunity" badge (not "Misplaced") | VERIFIED | Line 141 confirmed |
| `dashboard/src/app/(dashboard)/tier-scoring/page.tsx` | Updated page subtitle | VERIFIED | Line 114 confirmed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `scoreTerm()` | `CalibrationConfig` gates | `config` parameter with `DEFAULT_CALIBRATION` default | WIRED | Parameter on line 187, three gates used at lines 235-241 |
| `estimateImpact()` | ROAS-based delta formula | `monthlySpend * ROAS_delta` | WIRED | Formula at lines 354-356, returns non-zero for upward movement |
| `scoreTerm()` | Confidence computed before `isMisplaced` | Order in function body | WIRED | Confidence at line 231, `fitScoreDelta` computation at line 234 — correct ordering |
| `API route` | `DEFAULT_CALIBRATION` | Explicit import + pass-through to `scoreTerm()` | WIRED | Import at line 19, usage at line 144 |
| `TermScore.fitScoreDelta` | UI display | `TierDetail.tsx` `dataConfirmed` conditional | WIRED | `dataConfirmed` consumed at line 255 of `TierDetail.tsx` |
| `HeroCallout` | Calibrated `totalMisplaced` | `isMisplaced` boolean from calibrated scores | WIRED | `totalMisplaced` prop flows from API route which computes `scores.filter(s => s.isMisplaced).length` |
| `API route` | `fitScoreDelta`/`dataConfirmed` in response | Persisted to `query_value_scores.model_inputs` | WIRED | Lines 183-185 of `route.ts` persist both fields |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| TIER-01 | 33.1-01, 33.1-02 | Calibration refinement: distributions produce meaningful separation | SATISFIED | ROAS-based impact estimation produces meaningful dollar ranges; 3-gate calibration eliminates noise while preserving genuine signals |
| TIER-02 | 33.1-01, 33.1-02 | Calibration refinement: boundaries realistic for gut-assigned data | SATISFIED | `CalibrationConfig` thresholds (0.3/0.40/50) calibrated for gut-assigned tier context; HeroCallout subtitle explicitly acknowledges gut-assignment |
| TIER-03 | 33.1-01, 33.1-02 | Calibration refinement: minimum delta threshold for z-score recommendations | SATISFIED | `minFitScoreDelta: 0.3` gate in `scoreTerm()` prevents low-delta z-score disagreements from being flagged |

**Note on requirements mapping:** REQUIREMENTS.md traceability table maps TIER-01/02/03 to "Phase 33" (Complete). Phase 33.1 plans claim the same IDs as "calibration refinements" — this is correct since Phase 33.1 extends Phase 33's implementation. No orphaned requirements found.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

Zero anti-patterns detected. No TODOs, FIXMEs, placeholder implementations, or console-only handlers in any phase-modified file.

---

### TypeScript Compilation

`npx tsc --noEmit` produces 8 errors, all confined to `src/app/api/funnel-snapshots/__tests__/trends.test.ts`. Verified these errors pre-date Phase 33.1 (exist in commits before `518d50a2`). Zero errors introduced by this phase.

---

### Human Verification Required

#### 1. Impact dollar range credibility

**Test:** Load the Tier Intelligence page with real data. View the HeroCallout dollar range.
**Expected:** Dollar range shows non-zero values (e.g., "$X–$Y/mo opportunity"), not "$0–$0/mo"
**Why human:** Cannot verify with real production data in static analysis; formula correctness is verified by tests but real-data sanity requires live check.

#### 2. Noise reduction in practice

**Test:** Load the Tier Intelligence page. Count the percentage of terms flagged as "opportunities" vs total terms scored.
**Expected:** Significantly less than 95% flagged; ideally 10-25% representing genuinely high-confidence disagreements
**Why human:** The calibration thresholds were set based on business judgment. Real-data validation requires human assessment of whether the flagged set feels credible.

#### 3. "Data-confirmed" vs "Aligned" badges

**Test:** Drill into a tier, find terms with high click volumes in the correct tier. Check their status badges.
**Expected:** Terms with 100+ clicks in the right tier show "Data-confirmed"; new or low-volume terms show "Aligned"
**Why human:** The `dataConfirmed` condition requires `recommendedTier === currentTier && hasMinimumData && meetsConfidenceFloor` — visual check confirms this reads correctly in context.

---

### Gaps Summary

No gaps. All 17 observable truths verified, all 11 artifacts pass all three verification levels, all 7 key links are wired, no anti-patterns found, no TypeScript errors introduced.

The one pre-existing TypeScript issue in `funnel-snapshots/__tests__/trends.test.ts` is out of scope for this phase.

---

_Verified: 2026-02-25T20:35:00Z_
_Verifier: Claude (gsd-verifier)_
