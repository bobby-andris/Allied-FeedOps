# Phase 25.3-02: Rewrite Per-Platform Prompts as GPT-5.2 Creative Briefs

## Status: COMPLETE

**Commit:** `ad68f85c`

## What Was Done

### prompts.py
Added 4 platform-specific brief constants:
- `GOOGLE_BRIEF` (~2.5K) — Robert's title formula, {FINISH_NAME} first, Collection keyword, dims-only-when-varies, Allied Brass last. Description: design-story opening, {FINISH_SENTENCE}, selling points, trust close.
- `BING_BRIEF` (~1.8K) — conditional synonym variants, softer structure, Bing-specific trust signals
- `SHOPIFY_BRIEF` (~1.2K) — HTML description, SEO meta, finish-agnostic
- `FINISH_BRIEF` (~0.8K) — 28-finish sentence generation per variant

### skill_loader.py
Rewired `get_platform_system_prompt()` to return `SYSTEM_PROMPT + platform_brief` (~8-10K total) instead of extracted skill snippets (~20-60K total). Eliminated 12 internal contradictions.

### prompt_builder.py
- Added `_build_product_design_story()` — extracts collection DNA, material story, mounting context from evidence
- Added `_build_competitive_context()` — builds competitive positioning from catalog data
- All 4 per-platform user prompts now use XML tags: `<task>`, `<evidence_table>`, `<product_design_story>`, `<competitive_positioning>`, `<keyword_enrichment_hints>`, `<category_guidance>`, `<gold_examples>`, `<output>`

### Key Design Decisions
- Encoded Bobby/Robert exclusions directly in briefs: no weight capacity, no detailed dims, no competitor materials, no "heritage bathroom fixtures", no "28 finishes", no keyword stuffing
- Platform isolation: Google brief never mentions Shopify rules; Shopify brief never mentions {FINISH_NAME}/{FINISH_SENTENCE}
- Soft length targets in brief prose, hard maxLength constraints in JSON schema only
