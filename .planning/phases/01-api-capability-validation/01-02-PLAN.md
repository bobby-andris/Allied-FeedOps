---
phase: 01-api-capability-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-api-capability-validation/01-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "We know the practical LIMIT ceiling for GAQL queries with documented test results for 10K+ rows (API-03)"
    - "We know the actual data retention window for this account with earliest available date confirmed (API-04)"
    - "We have confirmed custom_label_0 availability and retrieved sample values from Merchant API (API-05)"
  artifacts:
    - path: ".planning/phases/01-api-capability-validation/01-02-SUMMARY.md"
      provides: "Query boundary tests, retention validation, and custom label confirmation"
  key_links:
    - from: "mcp__google-ads-mcp__search"
      to: "shopping_performance_view"
      via: "GAQL with varying LIMIT values and date ranges"
      pattern: "LIMIT|BETWEEN"
---

<objective>
Test query boundaries (LIMIT values, data retention windows) and confirm custom label availability in Merchant API to complete all Phase 1 API validation requirements.

Purpose: Establish the practical constraints for the backfill strategy - how many rows per query, how far back data goes, and whether custom labels are accessible for product segmentation.
Output: Documented test results for API-03, API-04, and API-05 with actual numbers from this Google Ads account.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-capability-validation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test query LIMIT values and data retention (API-03, API-04)</name>
  <files>.planning/phases/01-api-capability-validation/01-02-SUMMARY.md</files>
  <action>
Use ToolSearch to load `mcp__google-ads-mcp__search`, then run progressive LIMIT tests and retention tests.

**API-03: LIMIT Tests**

Run three queries with increasing LIMIT values against `shopping_performance_view` (no product filter - get ALL products to generate large result sets):

Query template:
```
SELECT
  segments.product_item_id,
  segments.date,
  metrics.impressions,
  metrics.clicks
FROM shopping_performance_view
WHERE segments.date DURING LAST_30_DAYS
  AND metrics.impressions > 0
ORDER BY metrics.impressions DESC
LIMIT {N}
```

Test with LIMIT values: 10000, 50000, 100000

For each test, document:
- Whether the query succeeded or failed
- Number of rows actually returned
- If it failed, the exact error message
- Approximate response time if observable

**API-04: Data Retention Tests**

Test how far back data exists for this account by querying progressively older date ranges:

```
SELECT
  segments.product_item_id,
  segments.date,
  metrics.impressions
FROM shopping_performance_view
WHERE segments.date BETWEEN '{start}' AND '{end}'
  AND metrics.impressions > 0
LIMIT 5
```

Test date ranges (oldest first):
1. `2015-01-01` to `2015-01-31` (11 years ago - testing retention claim)
2. `2018-01-01` to `2018-01-31` (8 years ago)
3. `2020-01-01` to `2020-01-31` (6 years ago)
4. `2023-01-01` to `2023-01-31` (3 years ago)
5. `2025-01-01` to `2025-01-31` (1 year ago)

For each test, document:
- Whether data was returned
- Number of rows (even with LIMIT 5, knowing if ANY data exists is the point)
- Earliest date with actual data found

**Customer ID:** 6253381786

The goal is to find the actual earliest available date and confirm LIMIT behavior, not to collect comprehensive data.
  </action>
  <verify>
- At least 3 LIMIT tests executed with results documented
- At least 3 date range tests executed with results documented
- Earliest available date for this account is identified
- Maximum reliable LIMIT value is identified
  </verify>
  <done>API-03 confirmed with documented LIMIT ceiling (or confirmation that all tested values work). API-04 confirmed with actual earliest available date for this account.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm custom_label_0 availability via Merchant API (API-05)</name>
  <files>.planning/phases/01-api-capability-validation/01-02-SUMMARY.md</files>
  <action>
Test custom label availability through two approaches:

**Approach 1: Google Ads API custom label segments**

Use `mcp__google-ads-mcp__search` to query custom labels in shopping_performance_view:
```
SELECT
  segments.product_item_id,
  segments.product_custom_attribute_0,
  segments.product_custom_attribute_1,
  segments.product_custom_attribute_2,
  metrics.impressions
FROM shopping_performance_view
WHERE segments.date DURING LAST_30_DAYS
  AND metrics.impressions > 0
ORDER BY metrics.impressions DESC
LIMIT 20
```

**Customer ID:** 6253381786

Document: whether `product_custom_attribute_0` returns values, what the values look like (should be product categories like "Towel Bars", "Grab Bars", etc.), and how many of the 20 rows have non-empty custom labels.

**Approach 2: Merchant API product_view**

Use ToolSearch to load `mcp__merchant-api-devdocs__query_mapi_docs`, then query for the custom label field schema. Look for:
- Field name for custom_label_0 in product_view
- Whether it's filterable
- Data type and format

Then use `mcp__merchant-api-devdocs__find_mapi_code_sample` to find a code example for querying custom labels.

Document: confirmed field name, availability, and sample values from Google Ads API query.

**Also test custom label filtering** (if custom labels are populated):
```
SELECT
  segments.product_item_id,
  segments.product_custom_attribute_0,
  metrics.impressions
FROM shopping_performance_view
WHERE segments.product_custom_attribute_0 = 'Towel Bars'
  AND segments.date DURING LAST_30_DAYS
  AND metrics.impressions > 0
LIMIT 10
```

This tests whether we can use custom labels as query filters (valuable for Phase 2 data discovery).
  </action>
  <verify>
- Google Ads API query returns custom label values for at least some products
- Custom label field name and format are documented
- Merchant API field availability is confirmed via documentation
- Filtering by custom label is tested (works or documented why not)
  </verify>
  <done>API-05 confirmed: custom_label_0 is accessible via Google Ads API (segments.product_custom_attribute_0) and Merchant API (customLabel0), with sample values showing product categories.</done>
</task>

</tasks>

<verification>
- API-03: LIMIT tests show maximum reliable query size (10K/50K/100K results documented)
- API-04: Historical retention confirmed with earliest available date for this account
- API-05: Custom labels confirmed accessible with sample category values
- All three requirements have actual API response evidence, not just documentation
</verification>

<success_criteria>
All three requirements (API-03, API-04, API-05) answered with definitive numbers from this specific Google Ads account. We know: (1) how many rows we can fetch per query, (2) how far back data goes, (3) whether custom labels are populated and filterable. These numbers directly inform the Phase 2-4 backfill strategy design.
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-capability-validation/01-02-SUMMARY.md`
</output>
