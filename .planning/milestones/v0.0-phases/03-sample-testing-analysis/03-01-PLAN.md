---
phase: 03-sample-testing-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase3_select_skus.py
  - .planning/phases/03-sample-testing-analysis/sample-skus.json
  - .planning/phases/03-sample-testing-analysis/search-terms-by-sku.json
autonomous: true

must_haves:
  truths:
    - "5-10 test SKUs are selected across at least 5 product categories"
    - "Each selected SKU has verified Google Ads activity (impressions > 0 in last 30 days)"
    - "Search terms are fetched for all selected SKUs with counts documented"
  artifacts:
    - path: "scripts/phase3_select_skus.py"
      provides: "SKU selection and search term fetching script"
      min_lines: 80
    - path: ".planning/phases/03-sample-testing-analysis/sample-skus.json"
      provides: "Selected test SKUs with metadata (master_sku, category, offer_id, title)"
    - path: ".planning/phases/03-sample-testing-analysis/search-terms-by-sku.json"
      provides: "Search terms per SKU with impression/click data"
  key_links:
    - from: "scripts/phase3_select_skus.py"
      to: "product_catalog table"
      via: "Supabase query for category-based selection"
      pattern: "product_catalog.*category"
    - from: "scripts/phase3_select_skus.py"
      to: "shopping_performance_view"
      via: "Google Ads API activity validation query"
      pattern: "shopping_performance_view.*impressions"
---

<objective>
Select 5-10 representative test SKUs across product categories and fetch their Google Ads search terms.

Purpose: Establish the sample set for all Phase 3 testing (SAMP-01) and capture current search term coverage (SAMP-02) as baseline for opportunity gap analysis.
Output: sample-skus.json with selected SKUs + search-terms-by-sku.json with fetched search terms.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-capability-validation/01-01-SUMMARY.md
@.planning/phases/03-sample-testing-analysis/03-RESEARCH.md
@docs/database/SCHEMA.md
@src/feedops/integrations/google_ads_search_terms.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select Test SKUs and Fetch Search Terms</name>
  <files>
    scripts/phase3_select_skus.py
    .planning/phases/03-sample-testing-analysis/sample-skus.json
    .planning/phases/03-sample-testing-analysis/search-terms-by-sku.json
  </files>
  <action>
Create a Python script `scripts/phase3_select_skus.py` that performs both SAMP-01 (SKU selection) and SAMP-02 (search term fetching):

**Part 1 - SKU Selection (SAMP-01):**
1. Load environment variables from `.env.vercel` using `set -a && source .env.vercel && set +a` pattern (or dotenv)
2. Connect to Supabase using `SUPABASE_URL` and `SUPABASE_SERVICE_KEY` env vars
3. Query `product_catalog` table for SKUs across 5 target categories: towel bars, grab bars, mirrors, shelves, robe hooks (or similar hardware). Use `ilike` for case-insensitive category matching. Select `master_sku, gmc_id, title, category` fields. Get 2-3 candidates per category.
4. Map candidates to `variant_index` table to get `gmc_offer_id` values for each `master_sku`
5. Validate activity via Google Ads API: query `shopping_performance_view` with `segments.product_item_id IN (...)` and `segments.date DURING LAST_30_DAYS` to confirm `metrics.impressions > 0`. Use lowercase `shopify_us_` format for offer IDs (confirmed in Phase 1).
6. Select 1-2 SKUs per category that have confirmed activity. Target 8-10 total.
7. If a category has no active SKUs, log warning and skip (don't fail).

**Fallback:** If category-based selection yields fewer than 5 SKUs, supplement with known-active offer IDs from Phase 1:
- `shopify_us_4538703609988_32096241320068`
- `shopify_us_8751009038562_46118169444578`
- `shopify_us_4543465947268_32123035451524`
- `shopify_us_4538765508740_32096780222596`
- `shopify_us_4542830280836_32117943369860`

Save selected SKUs to `sample-skus.json` with structure:
```json
[
  {"master_sku": "...", "category": "...", "gmc_offer_id": "...", "title": "...", "impressions_30d": N}
]
```

**Part 2 - Search Term Fetching (SAMP-02):**
1. For each selected SKU, get ALL variant offer IDs from `variant_index` table
2. Use Google Ads API to fetch search terms via campaign-join pattern:
   - First query: Get campaign IDs where products appear via `shopping_performance_view`
   - Second query: Get search terms for those campaigns via `search_term_view`
   - Match pattern from `src/feedops/integrations/google_ads_search_terms.py`
3. Use 90-day lookback window (`LAST_90_DAYS` or date range from today - 90 days)
4. For each search term, capture: search_term text, impressions, clicks, conversions (if available)
5. Deduplicate search terms across campaigns
6. Add 1-second delay between SKU queries to respect rate limits

Save results to `search-terms-by-sku.json` with structure:
```json
{
  "metadata": {"date": "2026-02-12", "lookback_days": 90},
  "skus": {
    "MASTER-SKU-1": {
      "variant_count": N,
      "search_term_count": N,
      "total_impressions": N,
      "terms": [{"search_term": "...", "impressions": N, "clicks": N}]
    }
  }
}
```

**Google Ads client setup:** Follow the pattern from Phase 1 test scripts - load config from env vars, set `use_proto_plus: True`. Customer ID: `6253381786`.

Print summary to stdout: number of SKUs selected, categories covered, total search terms found.
  </action>
  <verify>
Run: `cd /Users/bobby/Documents/GitHub/Allied-FeedOps && source .venv/bin/activate && set -a && source .env.vercel && set +a && python scripts/phase3_select_skus.py`

Verify:
- Script completes without errors
- `sample-skus.json` contains 5-10 SKUs across multiple categories
- `search-terms-by-sku.json` contains search terms for each SKU (at least some SKUs have >0 terms)
- Both JSON files are valid JSON (parseable)
  </verify>
  <done>
- 5-10 test SKUs selected across at least 5 product categories with confirmed Google Ads activity
- Search terms fetched for all selected SKUs with impression/click data
- Both result files saved as JSON in phase directory
- Summary printed showing category coverage and search term counts
  </done>
</task>

</tasks>

<verification>
1. `sample-skus.json` exists and contains 5-10 SKUs with master_sku, category, gmc_offer_id, title, impressions_30d
2. `search-terms-by-sku.json` exists and contains search term data for selected SKUs
3. At least 5 different product categories represented in sample
4. All selected SKUs have impressions_30d > 0 (activity validated)
5. Search terms include impression and click counts
</verification>

<success_criteria>
SAMP-01 and SAMP-02 requirements are satisfied: representative SKUs selected across product categories with confirmed Google Ads activity, and current search terms fetched for all sample SKUs via campaign-join pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sample-testing-analysis/03-01-SUMMARY.md`
</output>
