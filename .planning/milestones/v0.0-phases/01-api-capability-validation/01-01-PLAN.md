---
phase: 01-api-capability-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-api-capability-validation/01-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "We have confirmed search_term_view does NOT support product_item_id filtering (API-01)"
    - "We have a working GAQL query demonstrating shopping_performance_view product-level filtering (API-02)"
    - "Both findings are documented with actual API responses or error messages as evidence"
  artifacts:
    - path: ".planning/phases/01-api-capability-validation/01-01-SUMMARY.md"
      provides: "Validated query patterns and API-01/API-02 test results"
  key_links:
    - from: "mcp__google-ads-mcp__search"
      to: "shopping_performance_view"
      via: "GAQL query execution"
      pattern: "segments.product_item_id"
---

<objective>
Validate core Google Ads API view capabilities by testing search_term_view product filtering (expected to fail) and shopping_performance_view product-level queries (expected to succeed).

Purpose: Confirm the two most critical API assumptions - that search_term_view cannot filter by product (requiring campaign-join pattern) and that shopping_performance_view fully supports product-level queries for the backfill strategy.
Output: Documented test results with actual API responses for API-01 and API-02 requirements.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-capability-validation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test search_term_view product filtering (API-01)</name>
  <files>.planning/phases/01-api-capability-validation/01-01-SUMMARY.md</files>
  <action>
Use ToolSearch to load `mcp__google-ads-mcp__search`, then execute a GAQL query against `search_term_view` that attempts to filter by `segments.product_item_id`.

**Test query:**
```
SELECT
  search_term_view.search_term,
  campaign.id,
  metrics.impressions,
  metrics.clicks
FROM search_term_view
WHERE segments.product_item_id = 'shopify_US_7721863643362_42804912849122'
  AND segments.date DURING LAST_30_DAYS
```

**Expected result:** Error message indicating `product_item_id` is not available in `search_term_view`.

**Also test:** A simple search_term_view query WITHOUT product filtering to confirm the view works:
```
SELECT
  search_term_view.search_term,
  campaign.id,
  metrics.impressions,
  metrics.clicks
FROM search_term_view
WHERE segments.date DURING LAST_7_DAYS
  AND metrics.impressions > 10
ORDER BY metrics.impressions DESC
LIMIT 10
```

**Customer ID:** 6253381786

Document both results: the error message from the product filter attempt and successful results from the basic query. This confirms search_term_view works but cannot filter by product.
  </action>
  <verify>
- The product-filtered query returns an error (document the exact error message)
- The basic query returns actual search term data (document sample rows)
- Both results are captured in the SUMMARY
  </verify>
  <done>API-01 confirmed: search_term_view does not support product_item_id filtering, with documented error message as evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Validate shopping_performance_view product queries (API-02)</name>
  <files>.planning/phases/01-api-capability-validation/01-01-SUMMARY.md</files>
  <action>
Using the already-loaded `mcp__google-ads-mcp__search` tool, execute product-level queries against `shopping_performance_view`.

**Test 1 - Single product query:**
```
SELECT
  segments.product_item_id,
  segments.date,
  campaign.advertising_channel_type,
  metrics.impressions,
  metrics.clicks,
  metrics.ctr,
  metrics.conversions,
  metrics.conversions_value,
  metrics.cost_micros
FROM shopping_performance_view
WHERE segments.product_item_id = 'shopify_US_7721863643362_42804912849122'
  AND segments.date DURING LAST_30_DAYS
ORDER BY segments.date
```

**Test 2 - Batch product query (IN clause):**
Pick 3-5 product IDs from the database. Use ToolSearch to load `mcp__supabase__execute_sql` and run:
```sql
SELECT DISTINCT gmc_offer_id FROM variant_index WHERE gmc_offer_id IS NOT NULL LIMIT 5
```

Then query shopping_performance_view with those IDs in an IN clause:
```
SELECT
  segments.product_item_id,
  segments.date,
  metrics.impressions,
  metrics.clicks
FROM shopping_performance_view
WHERE segments.product_item_id IN ('id1', 'id2', 'id3', 'id4', 'id5')
  AND segments.date DURING LAST_30_DAYS
ORDER BY segments.product_item_id, segments.date
```

**IMPORTANT:** Database stores lowercase `shopify_us_` but Google Ads uses uppercase `shopify_US_`. Transform IDs before querying: `.replace('shopify_us_', 'shopify_US_')`.

**Customer ID:** 6253381786

Document: successful responses showing product-level data with actual metrics. Include at least 3-5 sample rows showing the data structure returned.
  </action>
  <verify>
- Single product query returns rows with impressions/clicks/etc for a specific product
- Batch query returns rows for multiple products grouped by product_item_id
- At least one query returns non-zero metrics data
- Sample response data is documented in the SUMMARY
  </verify>
  <done>API-02 confirmed: shopping_performance_view fully supports product-level filtering (single and batch), with working GAQL examples and sample response data.</done>
</task>

</tasks>

<verification>
- API-01 test query failed with documented error (confirms search_term_view limitation)
- API-01 basic query succeeded (confirms search_term_view works for non-product queries)
- API-02 single product query returned product-level performance data
- API-02 batch query returned data for multiple products
- All results documented with actual API responses
</verification>

<success_criteria>
Both API-01 and API-02 requirements have definitive answers backed by actual API test results, not just documentation research. The answers match research predictions (API-01: no, API-02: yes) and are documented with evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-capability-validation/01-01-SUMMARY.md`
</output>
