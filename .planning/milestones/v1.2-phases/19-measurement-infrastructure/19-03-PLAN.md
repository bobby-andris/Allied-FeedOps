---
phase: 19-measurement-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - dashboard/src/app/(dashboard)/monitoring/page.tsx
  - dashboard/src/app/(dashboard)/monitoring/bottleneck/page.tsx
  - dashboard/src/components/gmc/GmcDisapprovalBadge.tsx
  - dashboard/src/components/bottleneck/BottleneckBadge.tsx
  - dashboard/src/components/lineage/PromptLineagePanel.tsx
autonomous: true
requirements: [MEAS-01, MEAS-03, MEAS-04]

must_haves:
  truths:
    - "SKU review page shows a collapsible prompt lineage section with hash, alias, model, flag state, and generation cost"
    - "Bottleneck classification tags appear as color-coded badges on SKU tables"
    - "A dedicated /monitoring/bottleneck page groups SKUs by bottleneck category with evidence display"
    - "Manual override of bottleneck classification is accessible from the diagnostic view"
    - "Prompt comparison (side-by-side) is opt-in, not default"
  artifacts:
    - path: "dashboard/src/components/bottleneck/BottleneckBadge.tsx"
      provides: "Color-coded bottleneck classification badge for inline use in SKU tables"
    - path: "dashboard/src/components/lineage/PromptLineagePanel.tsx"
      provides: "Collapsible prompt lineage section for SKU review page"
    - path: "dashboard/src/app/(dashboard)/monitoring/bottleneck/page.tsx"
      provides: "Dedicated diagnostic view grouping SKUs by bottleneck type with evidence"
  key_links:
    - from: "dashboard/src/components/lineage/PromptLineagePanel.tsx"
      to: "/api/prompt-lineage"
      via: "fetch on mount with master_sku prop"
      pattern: "api/prompt-lineage"
    - from: "dashboard/src/app/(dashboard)/monitoring/bottleneck/page.tsx"
      to: "/api/bottleneck/status"
      via: "fetch classifications grouped by category"
      pattern: "api/bottleneck/status"
    - from: "dashboard/src/app/(dashboard)/monitoring/bottleneck/page.tsx"
      to: "/api/bottleneck/classify"
      via: "manual reclassify button and override form"
      pattern: "api/bottleneck/classify"
---

<objective>
Build all dashboard UI for measurement visibility: prompt lineage panel on SKU review, bottleneck classification badges on SKU tables, dedicated bottleneck diagnostic page, and a reclassify/override workflow.

Purpose: MEAS-01 flag visibility, MEAS-03 lineage display, and MEAS-04 bottleneck UI. Makes the instrumentation data from Plans 01-02 visible and actionable in the dashboard.
Output: Three new components, one new page, extensions to existing monitoring page.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-measurement-infrastructure/19-RESEARCH.md
@.planning/phases/19-measurement-infrastructure/19-01-SUMMARY.md
@.planning/phases/19-measurement-infrastructure/19-02-SUMMARY.md
@dashboard/src/app/(dashboard)/monitoring/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BottleneckBadge and PromptLineagePanel components</name>
  <files>dashboard/src/components/bottleneck/BottleneckBadge.tsx, dashboard/src/components/lineage/PromptLineagePanel.tsx</files>
  <action>
**BottleneckBadge component:**

Create `dashboard/src/components/bottleneck/BottleneckBadge.tsx`:

A small inline badge showing the bottleneck classification for a SKU. Color-coded per category (per user decision: "color-coded tags inline on SKU tables"):

```typescript
const BOTTLENECK_COLORS: Record<string, string> = {
  coverage_gap: 'bg-gray-100 text-gray-800',
  code_path_gap: 'bg-purple-100 text-purple-800',
  query_relevance: 'bg-yellow-100 text-yellow-800',
  propagation_failure: 'bg-orange-100 text-orange-800',
  auction_bid: 'bg-blue-100 text-blue-800',
}

const BOTTLENECK_LABELS: Record<string, string> = {
  coverage_gap: 'Coverage Gap',
  code_path_gap: 'Code Path Gap',
  query_relevance: 'Query Relevance',
  propagation_failure: 'Propagation Failure',
  auction_bid: 'Auction/Bid',
}
```

Props: `classification: string`, `confidence?: number`, `isOverride?: boolean`. Uses `Badge` from `@/components/ui/badge`. If isOverride, show a small "Override" indicator. Use existing `lucide-react` icons already imported in the project.

**PromptLineagePanel component:**

Create `dashboard/src/components/lineage/PromptLineagePanel.tsx`:

A collapsible section (per user decision: "Claude's discretion, likely collapsible technical details to avoid clutter") that shows prompt lineage for a published SKU.

Props: `masterSku: string`, `platform: string`.

On mount, fetch `/api/prompt-lineage?master_sku={masterSku}&platform={platform}`.

Display (when expanded):
- Prompt hash (truncated to 8 chars) with full hash on hover/title
- Prompt alias if set (e.g., "v2.1")
- Model version used
- Feature flags active at generation time (3 boolean badges: green=on, gray=off)
- Generation cost: tokens used, latency
- Published date
- Quality score if available

Add a "Compare Versions" button that when clicked, shows a second prompt hash selector and fetches compare mode data. This is the opt-in side-by-side per user decision: "ability to compare two generations by prompt version, but NOT default view".

Use `Collapsible` from shadcn/ui if available, otherwise a simple `details/summary` HTML element or a custom toggle with `ChevronDown`/`ChevronUp` icons. Match the existing UI patterns in the dashboard (check how other collapsible sections are implemented).

Handle the null lineage case gracefully: show "Lineage tracking not available for this publish event" in a muted text.
  </action>
  <verify>
1. `cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npx tsc --noEmit` — zero errors
2. Files exist: `ls dashboard/src/components/bottleneck/BottleneckBadge.tsx dashboard/src/components/lineage/PromptLineagePanel.tsx`
  </verify>
  <done>BottleneckBadge renders color-coded category badge with 5 category colors. PromptLineagePanel renders collapsible lineage section with hash, alias, model, flag badges, cost data, and opt-in compare button.</done>
</task>

<task type="auto">
  <name>Task 2: Create bottleneck diagnostic page and wire components into existing pages</name>
  <files>dashboard/src/app/(dashboard)/monitoring/bottleneck/page.tsx, dashboard/src/app/(dashboard)/monitoring/page.tsx</files>
  <action>
**Create `/monitoring/bottleneck` page:**

Create `dashboard/src/app/(dashboard)/monitoring/bottleneck/page.tsx`:

A dedicated diagnostic view (per user decision: "dedicated diagnostic view grouping SKUs by bottleneck type"):

1. On mount, fetch `/api/bottleneck/status?limit=500` to get all classifications
2. Display a summary bar at top showing counts per category (5 color-coded cards with count + category name)
3. Below, group SKUs by bottleneck category in collapsible sections. Each category section shows:
   - Category name + count header with the color-coded badge
   - Table of SKUs: master_sku, confidence score, classified_at date, evidence summary
   - Each row has a "Reclassify" button (calls POST /api/bottleneck/classify?master_sku=X) and an "Override" button that shows a small form (select classification + note text field)
4. Add a "Reclassify All" button at the top that calls POST /api/bottleneck/classify?batch=true with a loading spinner
5. Link to this page from the monitoring page

Per user decision on manual override: "user can change classification with a note when they know something the system doesn't" — the Override form should include a dropdown of 5 categories + a text field for the note.

**Update monitoring page:**

In `dashboard/src/app/(dashboard)/monitoring/page.tsx`:
- Add a link/card to the new `/monitoring/bottleneck` page (NOT a new tab — the bottleneck view is complex enough for its own route, per research recommendation)
- Add a small summary section showing bottleneck category counts fetched from `/api/bottleneck/status`
- Keep the existing 2-tab layout for Performance Deltas and Search Query Changes unchanged

**Wire BottleneckBadge into SKU tables:**

The BottleneckBadge should be importable and ready for use in SKU review lists. However, DO NOT modify the SKU review page in this task — that's complex with 3 variants (per CLAUDE.md). Instead, create an example integration comment in BottleneckBadge.tsx showing how to use it, and document in the SUMMARY that the badge is ready for integration into ReviewListClient in a future task.

**Wire PromptLineagePanel:**

Similarly, PromptLineagePanel is ready for integration into the SKU review detail view. DO NOT modify SkuReviewClient in this task (3 variants). Document integration path in SUMMARY.
  </action>
  <verify>
1. `cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npm run build` — passes
2. `cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npm run lint` — no new errors
3. Files exist: `ls dashboard/src/app/(dashboard)/monitoring/bottleneck/page.tsx`
4. Monitoring page still renders (no broken tabs)
  </verify>
  <done>Bottleneck diagnostic page shows SKUs grouped by 5 categories with evidence, reclassify/override controls. Monitoring page links to bottleneck view with summary counts. BottleneckBadge and PromptLineagePanel components are built and ready for integration into SKU review pages.</done>
</task>

</tasks>

<verification>
1. `cd dashboard && npm run build` — passes with zero errors
2. `cd dashboard && npm run lint` — clean
3. All new components exist and use existing shadcn/ui + lucide-react patterns
4. Monitoring page unchanged except for bottleneck link/summary section
5. Bottleneck diagnostic page renders category-grouped SKU list
</verification>

<success_criteria>
- BottleneckBadge component renders color-coded badge for all 5 categories
- PromptLineagePanel shows collapsible lineage with hash, alias, model, flags, cost
- PromptLineagePanel has opt-in Compare Versions button (not default)
- /monitoring/bottleneck page groups SKUs by category with reclassify + override
- Monitoring page links to bottleneck diagnostic view
- Dashboard builds and lints clean
</success_criteria>

<output>
After completion, create `.planning/phases/19-measurement-infrastructure/19-03-SUMMARY.md`
</output>
