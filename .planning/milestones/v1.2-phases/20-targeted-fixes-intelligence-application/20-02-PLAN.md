---
phase: 20-targeted-fixes-intelligence-application
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedops/pipeline/lifestyle_images.py
autonomous: true
requirements: [GOOG-05]

must_haves:
  truths:
    - "Image generation prompt includes collection design DNA from collection_descriptions.py"
    - "Image generation prompt includes finish-specific lighting guidance"
    - "Image generation prompt includes category-specific scene guidance"
    - "Product fidelity section is prominent and non-negotiable in the prompt"
  artifacts:
    - path: "src/feedops/pipeline/lifestyle_images.py"
      provides: "Enhanced image generation with collection/finish/category intelligence"
      contains: "PRODUCT FIDELITY"
  key_links:
    - from: "src/feedops/pipeline/lifestyle_images.py"
      to: "src/feedops/pipeline/collection_descriptions.py"
      via: "get_collection_description import"
      pattern: "get_collection_description"
---

<objective>
Wire collection design DNA, finish lighting guidance, and category scene guidance into the lifestyle image generation prompt.

Purpose: Implements GOOG-05 — image generation guidance updated to reflect Google Shopping visual ranking factors. Product fidelity is the non-negotiable constraint per user decision. Collection descriptions already exist in the database and are loaded by `collection_descriptions.py`. This plan wires that data plus new finish/category guidance into the image prompt.

Output: Enhanced `lifestyle_images.py` with three-dimensional image intelligence (category + finish + collection).
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-targeted-fixes-intelligence-application/20-RESEARCH.md

# Key source files
@src/feedops/pipeline/lifestyle_images.py
@src/feedops/pipeline/collection_descriptions.py
@data/Collection_Descriptions_Complete_All_41_20260124.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add finish lighting and category scene lookup data</name>
  <files>
    src/feedops/pipeline/lifestyle_images.py
  </files>
  <action>
**Step 1: Read `src/feedops/pipeline/lifestyle_images.py` fully** to understand the current image generation flow, especially `generate_lifestyle_images_for_sku()` and how the prompt is currently constructed.

**Step 2: Read `src/feedops/pipeline/collection_descriptions.py`** to understand the existing collection data loader, `get_collection_description()`, and `sanitize_collection_description()` functions.

**Step 3: Read the CSV** at `data/Collection_Descriptions_Complete_All_41_20260124.csv` (first 10 rows) to understand the column structure — specifically the Design, Group, Subgroup, and keyword columns.

**Step 4: Add to `lifestyle_images.py`:**

a) Import `get_collection_description` and `sanitize_collection_description` from `collection_descriptions.py`.

b) Add `FINISH_LIGHTING` dict mapping finish names to lighting/rendering guidance. Include ALL 28 finishes. Key entries:
   - Oil Rubbed Bronze → "warm directional lighting with amber tones, shadows emphasize the aged patina texture"
   - Polished Chrome → "bright diffuse lighting, crisp mirror-like reflections, cool silver tones"
   - Matte Black → "even studio lighting, minimal reflections, high contrast against surroundings"
   - Polished Brass → "warm golden ambient light, soft lustrous reflections"
   - Satin Nickel → "soft diffuse lighting, subtle brushed sheen without harsh reflections"
   - Antique Brass → "warm amber lighting, soft shadows to highlight the antiqued patina"
   - Polished Nickel → "bright neutral lighting, polished mirror finish with crisp reflections"
   - Unlacquered Brass → "natural warm lighting, living finish that shows character"
   - Venetian Bronze → "warm moody lighting with deep bronze tones and subtle copper undertones"
   - Satin Chrome → "cool diffuse lighting, brushed texture visible at close range"
   - ... (fill remaining finishes with appropriate guidance, use `get_finish_list()` from prompts.py for the canonical list)

c) Add `CATEGORY_SCENE` dict mapping product categories (lowercase) to scene descriptions. Key entries from CONTEXT.md:
   - grab bars → "modern accessible bathroom with quality tile walls, near shower or tub surround"
   - towel bars → "clean contemporary bathroom wall, neutral tile, near vanity or shower"
   - paper towel holders → "bright kitchen with stone countertop or modern bathroom vanity"
   - garment rods → "walk-in closet or dressing room with soft ambient lighting"
   - soap dispensers → "bathroom vanity countertop or kitchen sink area, clean surfaces"
   - towel rings → "beside bathroom vanity mirror, complementary fixtures visible"
   - robe hooks → "behind bathroom door or in dressing area, robe draped naturally"
   - shelves → "bathroom wall above toilet or vanity, styled with decorative items"
   - toilet paper holders → "beside toilet in clean bathroom setting, complementary tile"
   - (add more based on custom_label_0 categories discovered in Task 1 of Plan 01)

d) Create `_build_enhanced_image_prompt()` function (or modify existing prompt construction) with this structure per CONTEXT.md locked decision:

```
Generate a professional lifestyle photograph of [product_type] in [finish_name] finish.

PRODUCT FIDELITY (NON-NEGOTIABLE):
- The [finish_name] finish must be rendered with absolute precision. [finish_lighting].
- Product dimensions and design details must be indistinguishable from the actual physical product.
- Product occupies 50-70% of frame as the clear focal point.
- Scene enhances the product, never competes with it.

SCENE ENVIRONMENT:
- Setting: [category_scene]
- Collection: [collection_name] — [collection design keywords from CSV]. Environment should evoke [style_group/subgroup] aesthetic.

COMPOSITION:
- Professional interior photography style, high resolution
- Natural lighting that complements the [finish_name] finish
- [finish_lighting guidance]
- Product is hero of the image — background supports, never distracts

DO NOT compromise product accuracy for scene aesthetics. The product must be identifiable as the exact Allied Brass [product_type].
```

**Step 5: Wire the enhanced prompt into `generate_lifestyle_images_for_sku()`.**
Find where the current prompt string is constructed and replace/enhance it to call `_build_enhanced_image_prompt()`. The function should:
- Extract product_type from the SKU data
- Extract collection_name from the SKU data
- Look up collection description via `get_collection_description(collection_name)`
- Look up finish lighting from `FINISH_LIGHTING`
- Look up category scene from `CATEGORY_SCENE`
- Gracefully fall back if any data is missing (use generic defaults)

CRITICAL per user decision: "Never compromise the ability to focus on the product to ensure that the exact product is used and matches every detail and is indistinguishable from our actual product." The PRODUCT FIDELITY section must be the FIRST and most prominent section in the prompt.
  </action>
  <verify>
Run `cd /Users/bobby/Documents/GitHub/Allied-FeedOps && PYTHONPATH=./src python -c "
from feedops.pipeline.lifestyle_images import FINISH_LIGHTING, CATEGORY_SCENE
print(f'Finishes: {len(FINISH_LIGHTING)}')
print(f'Categories: {len(CATEGORY_SCENE)}')
print(f'Oil Rubbed Bronze: {FINISH_LIGHTING.get(\"Oil Rubbed Bronze\", \"MISSING\")[:50]}')
print(f'grab bars: {CATEGORY_SCENE.get(\"grab bars\", \"MISSING\")[:50]}')
"` — should show counts for both dicts and sample entries.

Also grep to verify the prompt structure: `grep -n "PRODUCT FIDELITY" src/feedops/pipeline/lifestyle_images.py` — must find the non-negotiable section.
  </verify>
  <done>lifestyle_images.py has FINISH_LIGHTING dict (all 28 finishes), CATEGORY_SCENE dict (all major categories), enhanced image prompt with collection design DNA wiring, and PRODUCT FIDELITY section is prominent and first in the prompt.</done>
</task>

</tasks>

<verification>
1. `lifestyle_images.py` imports from `collection_descriptions.py`
2. FINISH_LIGHTING dict covers all 28 finishes
3. CATEGORY_SCENE dict covers major product categories
4. Image generation prompt includes product fidelity, scene environment, and composition sections
5. Collection design DNA (from CSV) flows into prompt via `get_collection_description()`
6. Graceful fallback when collection/finish/category data is missing
</verification>

<success_criteria>
- Image generation prompt incorporates three-dimensional intelligence (category + finish + collection) per CONTEXT.md locked decision
- Product fidelity is the non-negotiable first section
- All 28 finishes have lighting guidance
- Collection descriptions wired from existing `collection_descriptions.py` loader
- GOOG-05 complete
</success_criteria>

<output>
After completion, create `.planning/phases/20-targeted-fixes-intelligence-application/20-02-SUMMARY.md`
</output>
