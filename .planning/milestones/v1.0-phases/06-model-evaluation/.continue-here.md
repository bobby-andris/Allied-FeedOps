---
phase: 06-model-evaluation
task: 3
total_tasks: 3 (across 3 plans)
status: in_progress
last_updated: 2026-03-03T20:36:56.690Z
---

<current_state>
Plan 06-03 (Go-Live) partially complete. PR #57 created and ready to merge. Code deploys safely — defaults to GPT-5.2 until env vars are flipped. Post-merge steps remain: set Cloud Run env vars, verify health, generate test SKU, then dead code cleanup.
</current_state>

<completed_work>
- Plan 06-01: COMPLETE — eval scripts, SKU selection, blind eval, timeout fixes (committed 7b3e49bd)
- Plan 06-02: COMPLETE — all 4 model evaluations run, MODEL_COMPARISON_REPORT.md written
- Plan 06-03 Task 1: COMPLETE — recommendation.md written with data, rollback procedure, cost projection
- Plan 06-03 Task 2 (partial): Pre-deploy gates all passed (pytest 775/776, build, tsc, lint all clean)
- Plan 06-03 Task 2 (partial): PR #57 created — feat/claude-sonnet-v3-go-live branch pushed
- Production call chain fully traced and verified end-to-end
- Confirmed: ANTHROPIC_API_KEY secret exists and is bound to Cloud Run runtime SA
</completed_work>

<remaining_work>
1. **Merge PR #57** on GitHub
2. **Wait for auto-deploy** (Cloud Build for Cloud Run, Vercel for dashboard)
3. **Verify deployment healthy**: `curl $FEEDOPS_PIPELINE_URL/health`
4. **Set Cloud Run env vars to activate Claude**:
   ```bash
   gcloud run services update feedops-pipeline \
     --update-env-vars="FEEDOPS_PROVIDER=claude,FEEDOPS_GOOGLE_BRIEF_VERSION=v3" \
     --region=us-east1 \
     --project=bobbys-project-346400
   ```
5. **Generate 1 test SKU** via dashboard regeneration, verify quality
6. **Dead code cleanup** — remove legacy `generate_candidates`, `generate_variant_candidate`, `build_split_prompt`, `build_variant_prompt` from generator.py (confirmed dead — production uses executor.py path exclusively)
7. **Switch back to master branch**: `git checkout master && git pull`
</remaining_work>

<decisions_made>
- **Winner: Sonnet 4.6 + v3 skill prompt** — 84% cheaper, 2x faster, 8.85/10 blind score
- **Image support: Defer** — infrastructure exists (~15 lines to wire in executor.py), but ship the proven text-only win first
- **Rate limiting: No change needed** — Anthropic SDK auto-retries handle 429s
- **Dead code cleanup: After go-live** — diagnostic isolation (one variable at a time)
- **Deploy strategy: Safe default** — code deploys on GPT-5.2, Claude activates only when env vars are explicitly set
- **Rollback: Instant env var change** — no code deployment needed
</decisions_made>

<blockers>
None. All work is unblocked. PR #57 is ready to merge.
</blockers>

<context>
Master branch is protected — requires PR merge (not direct push). The branch feat/claude-sonnet-v3-go-live has the commit. After merge, Bobby needs to switch back to master and pull.

Key research finding this session: Image support infrastructure is already fully built (ImageInput, fetch_image, ClaudeProvider multimodal handling). Only executor.py needs ~15 lines to wire it into the production path. The legacy generator.py paths already use images but are dead code — production goes through executor.py which doesn't. This is a future enhancement, not a go-live blocker.

Rollback command if anything goes wrong:
```bash
gcloud run services update feedops-pipeline \
  --update-env-vars="FEEDOPS_PROVIDER=openai,FEEDOPS_GOOGLE_BRIEF_VERSION=v2" \
  --region=us-east1 \
  --project=bobbys-project-346400
```
</context>

<next_action>
1. Merge PR #57 on GitHub
2. Wait for Cloud Build deploy to complete: `gcloud builds list --project=bobbys-project-346400 --limit=3`
3. Verify health: `curl https://[PIPELINE_URL]/health`
4. Set env vars: `gcloud run services update feedops-pipeline --update-env-vars="FEEDOPS_PROVIDER=claude,FEEDOPS_GOOGLE_BRIEF_VERSION=v3" --region=us-east1 --project=bobbys-project-346400`
5. Test: Generate 1 SKU via dashboard, verify description > 500 chars
6. Clean up: Remove dead code from generator.py
</next_action>
