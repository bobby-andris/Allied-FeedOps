---
phase: 08-monitoring-automation
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - dashboard/package.json
  - dashboard/src/app/(dashboard)/backfill/page.tsx
  - dashboard/src/app/api/backfill/route.ts
  - dashboard/src/app/api/monitoring/backfill-health/route.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard /backfill page displays list of backfill jobs with status, progress bars, and ETA"
    - "Dashboard shows coverage metrics: X/2,784 SKUs with search terms, performance, and keyword data"
    - "Dashboard shows data freshness heatmap with green/yellow/red color coding by SKU age"
    - "Dashboard shows API health panel with latency p95, error counts, and rate limit stats"
    - "Job status auto-refreshes every 5 seconds for running jobs, stops polling on terminal state"
  artifacts:
    - path: "dashboard/src/app/(dashboard)/backfill/page.tsx"
      provides: "Backfill monitoring dashboard with job list, coverage, freshness, and health panels"
      min_lines: 150
    - path: "dashboard/src/app/api/backfill/route.ts"
      provides: "Next.js API proxy to Python /backfill/jobs endpoint"
      exports: ["GET"]
    - path: "dashboard/src/app/api/monitoring/backfill-health/route.ts"
      provides: "Next.js API proxy to Python /monitoring/* endpoints"
      exports: ["GET"]
  key_links:
    - from: "dashboard/src/app/(dashboard)/backfill/page.tsx"
      to: "dashboard/src/app/api/backfill/route.ts"
      via: "fetch('/api/backfill') for job list"
      pattern: "fetch.*api/backfill"
    - from: "dashboard/src/app/api/backfill/route.ts"
      to: "https://feedops-pipeline-623866089882.us-east1.run.app"
      via: "proxy to Cloud Run /backfill/jobs"
      pattern: "feedops-pipeline"
---

<objective>
Build the dashboard backfill monitoring page with real-time job status, coverage metrics, freshness heatmap, and API health panels.

Purpose: Give Bobby visual insight into backfill job progress, data coverage gaps, and system health — all in one dashboard page (MON-01, MON-02, MON-03, MON-04).

Output: New `/backfill` dashboard page with 4 panels + API proxy routes
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-monitoring-automation/08-01-SUMMARY.md
@dashboard/src/app/(dashboard)/monitoring/page.tsx
@src/feedops/api/backfill.py
@src/feedops/api/monitoring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js API proxy routes for backfill and monitoring endpoints</name>
  <files>dashboard/src/app/api/backfill/route.ts, dashboard/src/app/api/monitoring/backfill-health/route.ts</files>
  <action>
**Route 1: dashboard/src/app/api/backfill/route.ts**
- GET handler that proxies to Cloud Run `GET /backfill/jobs`
- Read PIPELINE_URL from env (or hardcode `https://feedops-pipeline-623866089882.us-east1.run.app`)
- Forward query params (status, limit) to upstream
- Return JSON response
- Follow existing proxy pattern from `dashboard/src/app/api/regenerate/route.ts`

**Route 2: dashboard/src/app/api/monitoring/backfill-health/route.ts**
- GET handler that aggregates 3 monitoring endpoints into one response
- Fetch from Cloud Run: `/monitoring/freshness`, `/monitoring/coverage`, `/monitoring/api-health`
- Use Promise.allSettled to fetch all 3 in parallel (don't fail if one is down)
- Return combined JSON: `{"freshness": {...}, "coverage": {...}, "apiHealth": {...}}`
- Any failed fetch returns null for that section

Both routes should:
- Use `NextResponse.json()` for responses
- Handle fetch errors with try/catch and return 502 on upstream failure
- Set `cache: 'no-store'` on fetches to avoid stale data
  </action>
  <verify>
    Run: `cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npx tsc --noEmit --pretty 2>&1 | tail -5` — should have zero errors related to the new route files.
  </verify>
  <done>Two API proxy routes exist, compile without TypeScript errors, and proxy to Cloud Run monitoring endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Install Tremor and build backfill monitoring dashboard page</name>
  <files>dashboard/package.json, dashboard/src/app/(dashboard)/backfill/page.tsx</files>
  <action>
**Step 1: Install Tremor**
- Run: `cd dashboard && npm install @tremor/react`
- This provides pre-built KPI cards, progress bars, data tables, and color utilities

**Step 2: Create dashboard/src/app/(dashboard)/backfill/page.tsx**

Build a server-renderable page with client-side polling. Structure:

```
"use client"

// Page layout: 4 panels in responsive grid
// Row 1: Job Status Panel (full width) + Coverage KPI Cards (sidebar)
// Row 2: Data Freshness Heatmap (left) + API Health Panel (right)
```

**Panel 1: Active Jobs (MON-01)**
- Fetch `/api/backfill` for job list
- Display table with columns: Job ID (truncated), Type, Status badge, Progress bar, Items (completed/total), ETA, Created
- Status badges: running=blue, complete=green, failed=red, partial=yellow
- Use Tremor `<Table>`, `<TableBody>`, `<TableRow>`, `<Badge>`, `<ProgressBar>`
- Auto-refresh every 5 seconds when ANY job has status "running" (use setInterval with cleanup)
- Stop polling when all jobs are terminal (complete/failed/partial)

**Panel 2: Coverage KPIs (MON-02)**
- Fetch `/api/monitoring/backfill-health` (coverage section)
- Display 3 KPI cards using Tremor `<Card>` + `<Metric>`:
  - Search Terms: {N}/2784 SKUs
  - Performance: {N}/2784 SKUs
  - Keywords: {N}/2784 SKUs
- Color code: >90% green, 50-90% yellow, <50% red
- Each card shows percentage as subtitle

**Panel 3: Data Freshness Heatmap (MON-03)**
- Fetch `/api/monitoring/backfill-health` (freshness section)
- Display grid of small colored squares (one per SKU)
- Color: green (<=7 days), yellow (8-30), orange (31-60), red (>60)
- Tooltip on hover shows SKU name and age
- Legend at bottom
- Use CSS grid with `grid-cols-[repeat(auto-fill,16px)]` for responsive layout

**Panel 4: API Health (MON-04)**
- Fetch `/api/monitoring/backfill-health` (apiHealth section)
- Display using Tremor cards:
  - Latency P95: {N}ms
  - Error Count: {N}
  - Rate Limit Hits: {N}
- Color thresholds: latency <500ms green, 500-2000ms yellow, >2000ms red

**Polling strategy:**
- Use useEffect + setInterval for job list (5s when running jobs exist)
- Use useEffect on mount only for monitoring data (coverage/freshness/health) — this data changes slowly, refresh on page load is sufficient
- Loading states with skeleton/spinner for each panel

Follow existing dashboard patterns from `dashboard/src/app/(dashboard)/monitoring/page.tsx` for data fetching and component structure. Use Tailwind for layout, Tremor for data visualization components.
  </action>
  <verify>
    Run: `cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npm run build 2>&1 | tail -10` — build should succeed with no errors.
  </verify>
  <done>Backfill monitoring page renders 4 panels (jobs, coverage, freshness, health), auto-polls running jobs, and builds without errors.</done>
</task>

</tasks>

<verification>
1. `cd dashboard && npm run build` passes
2. `/backfill` page exists and is accessible
3. API proxy routes compile and proxy to Cloud Run
4. Tremor is installed in package.json
5. Page has 4 distinct panels: jobs, coverage, freshness heatmap, API health
6. Polling stops when no running jobs
</verification>

<success_criteria>
- Dashboard /backfill page shows all 4 monitoring panels
- Job list auto-refreshes during active jobs
- Coverage KPIs show X/2784 with color coding
- Freshness heatmap displays per-SKU age with color legend
- API health shows latency p95, errors, rate limit hits
- Full dashboard build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-monitoring-automation/08-03-SUMMARY.md`
</output>
