---
phase: 08.1-data-model-gap-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/entity-relationships.md
autonomous: true
requirements: [ENTM-03]

must_haves:
  truths:
    - "Entity relationship document exists with Mermaid ER diagrams"
    - "variant_index is documented as the central hub for cross-platform joins"
    - "Multi-SKU product pattern (shared product_id) is documented with explicit warnings"
    - "All performance, search, publishing, and content table relationships are documented"
    - "FK constraints are recommended where missing (not applied -- deferred)"
  artifacts:
    - path: "docs/architecture/entity-relationships.md"
      provides: "Complete entity relationship documentation"
      contains: "erDiagram"
      min_lines: 100
  key_links:
    - from: "docs/architecture/entity-relationships.md"
      to: "docs/database/SCHEMA.md"
      via: "references"
      pattern: "SCHEMA.md"
---

<objective>
Document the complete entity relationship map showing variant_index as the hub linking Google Ads, Shopify, and GMC (ENTM-03).

Purpose: This is the reference document that prevents future developers from making incorrect joins. It explicitly documents the multi-SKU product pattern (DMF-2/2X through 2/5X sharing one product_id), the correct join keys for performance data, and where FK constraints should be added in future phases. Per locked decision: Mermaid ER diagrams, full data model scope, recommend but don't apply FKs.

Output: `docs/architecture/entity-relationships.md` with Mermaid diagrams.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.1-data-model-gap-audit/08.1-CONTEXT.md
@.planning/phases/08.1-data-model-gap-audit/08.1-RESEARCH.md
@docs/database/SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create entity relationship documentation with Mermaid ER diagrams</name>
  <files>docs/architecture/entity-relationships.md</files>
  <action>
    Create `docs/architecture/entity-relationships.md` covering the full data model per locked decision. Structure:

    **1. Overview section:**
    - State that `variant_index` (72K rows) is the central hub
    - Explain offer ID format: DB = lowercase `shopify_us_`, GMC = uppercase `shopify_US_`
    - Reference `feedops/utils/offer_id.py` for normalization

    **2. Core ER diagram (Mermaid):**
    - `variant_index` as hub entity with: gmc_offer_id (PK), master_sku, shopify_product_id, shopify_variant_id, finish, finish_code
    - Performance chain: `publish_events` -> `performance_snapshots` -> `performance_impact_scores`
    - Variant performance chain: `variant_index.gmc_offer_id` -> `performance_snapshots_variant`, `performance_baselines_variant`
    - Master performance: `performance_baselines` linked via master_sku
    - Content chain: `generated_content` -> `sku_approvals` -> `publish_events`
    - Publishing: `publish_batches` -> `batch_sku_assignments`
    - Search: `search_queries` (linked via gmc_offer_id), `keyword_metrics` (standalone by keyword)
    - Use the Mermaid ER diagram from RESEARCH.md Pattern 5 as starting point, expand to cover all relationships

    **3. Join key reference table:**
    - Table showing: Source -> Target -> Join Key -> Notes
    - Include all valid join paths
    - Flag INCORRECT joins (e.g., never join on shopify_product_id for performance data)

    **4. Multi-SKU product pattern section (DEDICATED):**
    - Example: DMF-2/2X, 2/3X, 2/4X, 2/5X all share `shopify_product_id=4539975336068`
    - Which APIs return product_id-level vs offer_id-level data
    - Where aggregation happens (performance_impact.py aggregates offer->master_sku)
    - Correctness assessment: Google Ads returns per-offer-ID (correct), but any join via product_id double-counts
    - WARNING box: "Never join on shopify_product_id for performance data"

    **5. Recommended FK constraints section:**
    - List tables and columns where FKs should exist but don't
    - For each: which tables, which columns, what the FK should reference
    - Note: "These are recommendations for a future migration phase -- not applied in Phase 8.1"
    - Candidates: performance_snapshots_variant.gmc_offer_id -> variant_index.gmc_offer_id, performance_baselines_variant.gmc_offer_id -> variant_index.gmc_offer_id, search_queries.gmc_offer_id -> variant_index.gmc_offer_id, batch_sku_assignments -> publish_batches, generated_content.master_sku -> some reference

    **6. Data granularity audit results:**
    - Table showing each data table, its granularity (master_sku vs offer_id vs keyword), and whether it has variant-level gaps
    - Include findings from RESEARCH.md Finding 5: search_queries already at offer_id granularity (no gap), keyword_metrics at keyword level (correct, no variant dimension), funnel_snapshots_daily at (date, custom_label_0, tier) level (correct)

    Keep the document practical and reference-oriented. Use Mermaid for visual diagrams, tables for quick lookups.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && grep -c "erDiagram" docs/architecture/entity-relationships.md && wc -l docs/architecture/entity-relationships.md</automated>
  </verify>
  <done>Document exists at `docs/architecture/entity-relationships.md` with at least one Mermaid ER diagram, multi-SKU pattern section, join key reference table, recommended FK list, and data granularity audit. At least 100 lines.</done>
</task>

</tasks>

<verification>
- `grep -q "erDiagram" docs/architecture/entity-relationships.md` -- Mermaid diagram present
- `grep -q "variant_index" docs/architecture/entity-relationships.md` -- hub entity documented
- `grep -q "DMF-2" docs/architecture/entity-relationships.md` -- multi-SKU pattern documented
- `grep -q "shopify_product_id" docs/architecture/entity-relationships.md` -- anti-pattern warning present
- Document is at least 100 lines: `wc -l docs/architecture/entity-relationships.md`
</verification>

<success_criteria>
- Entity relationship document exists with Mermaid ER diagram(s)
- variant_index documented as central hub
- Multi-SKU pattern documented with explicit warnings
- FK constraints recommended (not applied)
- Data granularity audit results included
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-data-model-gap-audit/08.1-03-SUMMARY.md`
</output>
