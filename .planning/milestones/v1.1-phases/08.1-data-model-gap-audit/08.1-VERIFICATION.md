---
phase: 08.1-data-model-gap-audit
verified: 2026-03-04T04:30:00Z
status: human_needed
score: 9/12 must-haves verified (3 deferred to post-deployment)
re_verification: false
human_verification:
  - test: "Run DATA-01 50-SKU test gate after Cloud Run deployment"
    expected: "Offer ID match rate >90%, rows inserted into performance_baselines_variant, time per batch acceptable"
    why_human: "Requires live Google Ads API access and deployed Cloud Run endpoint; variant tables are empty until deployment ships the wired code"
  - test: "Trigger daily snapshot collection (DATA-02) and verify variant dual-write"
    expected: "SELECT COUNT(*) FROM performance_snapshots_variant WHERE snapshot_date = CURRENT_DATE returns > 0 AND count > master_sku snapshot count"
    why_human: "Requires POST to $FEEDOPS_PIPELINE_URL/performance/collect-daily on a deployed Cloud Run instance with the new wiring; variant tables have 0 rows in current state"
  - test: "Trigger impact score computation (DATA-03) and verify population"
    expected: "SELECT COUNT(*) FROM performance_impact_scores returns > 0 after compute-impact endpoint is called"
    why_human: "Depends on DATA-02 snapshot data existing; requires deployed Cloud Run endpoint; scores cannot populate from empty snapshot tables"
---

# Phase 08.1: Data Model Gap Audit — Verification Report

**Phase Goal:** Audit and fix data model gaps — offer ID normalization (ENTM-01), apply normalization across codepaths (ENTM-02), entity relationship documentation (ENTM-03), bulk baseline backfill script (DATA-01), variant-level daily snapshots (DATA-02), impact score verification (DATA-03).
**Verified:** 2026-03-04
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | normalize_offer_id('shopify_US_123_456') returns 'shopify_us_123_456' | VERIFIED | `src/feedops/utils/offer_id.py` — `offer_id.lower()` implementation; 13 passing tests in `tests/test_offer_id_normalize.py` |
| 2 | to_gmc_format('shopify_us_123_456') returns 'shopify_US_123_456' | VERIFIED | `src/feedops/utils/offer_id.py` — `.replace("shopify_us_", "shopify_US_")` implementation; roundtrip test passing |
| 3 | Both functions handle None and empty string safely | VERIFIED | `if not offer_id: return offer_id` guard present; 4 tests confirm None/empty pass-through |
| 4 | All 4 Python codepaths use normalize_offer_id() instead of inline .lower() | VERIFIED | grep confirms 4 imports + usage at correct call sites; only remaining .lower() calls are on env var strings (correct) |
| 5 | collect_daily_performance_snapshots() writes to performance_snapshots_variant before aggregating | VERIFIED | Lines 370-416 in `performance_impact.py` — variant write block precedes `aggregated_by_sku` construction at line 435 |
| 6 | Only rows with impressions > 0 are written to performance_snapshots_variant | VERIFIED | Skip-zero logic confirmed in variant write block (`if v_impressions == 0: continue`) |
| 7 | Entity relationship document exists with Mermaid ER diagrams | VERIFIED | `docs/architecture/entity-relationships.md` — 585 lines, 5 `erDiagram` blocks |
| 8 | variant_index documented as central hub with multi-SKU warnings | VERIFIED | DMF-2/2X through 2/5X pattern documented; `shopify_product_id` flagged as incorrect join key for performance data |
| 9 | Bulk baseline backfill script exists and runs in dry-run mode | VERIFIED | `scripts/bulk_baseline_backfill.py` — 661 lines; all 4 CLI flags (--test-gate, --dry-run, --batch-size, --sleep) confirmed present |
| 10 | 50-SKU test gate validates data quality before full sweep | VERIFIED (code) | `TEST_GATE_SKU_LIMIT = 50`, 90% match rate threshold, `print_test_gate_summary()` present — live execution deferred |
| 11 | Daily snapshot job confirmed working with variant dual-write (DATA-02) | DEFERRED | Code wired and correct; variant tables have 0 rows — live verification requires Cloud Run deployment |
| 12 | Impact scores confirmed populating after snapshots collect (DATA-03) | DEFERRED | Code path exists; depends on DATA-02 data existing in variant tables — deferred to post-deployment |

**Score:** 9/12 automated truths verified; 3 deferred to human post-deployment verification

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/feedops/utils/__init__.py` | Package init for utils module | VERIFIED | Exists, 1 line docstring |
| `src/feedops/utils/offer_id.py` | normalize_offer_id + to_gmc_format | VERIFIED | 54 lines, both functions with correct logic and None/empty guards |
| `tests/test_offer_id_normalize.py` | Unit tests, min 30 lines | VERIFIED | 80 lines, 13 tests in 2 classes — all required behaviors covered |
| `supabase/migrations/043_variant_performance_tables.sql` | 2x CREATE TABLE with constraints | VERIFIED | 93 lines, 2 CREATE TABLE IF NOT EXISTS, uq_variant_snapshots_daily + pk_baselines_variant constraints confirmed |
| `src/feedops/monitoring/performance_impact.py` | Dual-write to variant table | VERIFIED | Contains `performance_snapshots_variant` upsert with correct on_conflict columns at lines 412-414 |
| `src/feedops/integrations/google_ads_performance.py` | normalize_offer_id at ingestion | VERIFIED | Import at line 19, usage at line 336 in _fetch_chunk_data() |
| `src/feedops/integrations/google_ads_search_terms.py` | normalize_offer_id replacing .lower() | VERIFIED | Import at line 20, usage at lines 401 + 428 |
| `src/feedops/api/performance_baseline.py` | normalize_offer_id in baseline capture | VERIFIED | Import at line 24, usage at line 631 |
| `docs/architecture/entity-relationships.md` | ER doc, min 100 lines, erDiagram | VERIFIED | 585 lines, 5 Mermaid erDiagram blocks, SCHEMA.md referenced |
| `scripts/bulk_baseline_backfill.py` | Backfill script, min 80 lines | VERIFIED | 661 lines, all CLI flags, imports _capture_google_baseline and normalize_offer_id |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/feedops/utils/offer_id.py` | `tests/test_offer_id_normalize.py` | import | WIRED | `from feedops.utils.offer_id import normalize_offer_id, to_gmc_format` at line 10 |
| `src/feedops/monitoring/performance_impact.py` | `performance_snapshots_variant` | supabase upsert | WIRED | `supabase.table("performance_snapshots_variant").upsert(...)` at line 412, on_conflict matches migration 043 constraint |
| `src/feedops/integrations/google_ads_performance.py` | `src/feedops/utils/offer_id.py` | import | WIRED | `from feedops.utils.offer_id import normalize_offer_id` at line 19 |
| `scripts/bulk_baseline_backfill.py` | `src/feedops/api/performance_baseline.py` | import | WIRED | `from feedops.api.performance_baseline import _capture_google_baseline` at line 55 |
| `scripts/bulk_baseline_backfill.py` | `src/feedops/utils/offer_id.py` | import | WIRED | `from feedops.utils.offer_id import normalize_offer_id` at line 57 |
| `docs/architecture/entity-relationships.md` | `docs/database/SCHEMA.md` | references | WIRED | `docs/database/SCHEMA.md` referenced at lines 30 and 581 |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| ENTM-01 | 08.1-01 | Shared offer ID normalization utility | SATISFIED | `offer_id.py` exists with both functions; 13 tests pass; importable as `from feedops.utils.offer_id import` |
| ENTM-02 | 08.1-02 | Apply normalization across all codepaths | SATISFIED | All 4 files confirmed importing and using normalize_offer_id; no remaining inline .lower() on offer IDs |
| ENTM-03 | 08.1-03 | Entity relationship map with variant_index as hub | SATISFIED | 585-line document, 5 Mermaid diagrams, variant_index hub, multi-SKU pattern, join key reference, recommended FKs |
| DATA-01 | 08.1-04 | Throttled bulk baseline capture with 50-SKU test gate | SATISFIED (code) / NEEDS HUMAN (live run) | Script exists with all behaviors; test gate execution deferred to post-deployment |
| DATA-02 | 08.1-04 | Verify daily snapshot dual-write working | NEEDS HUMAN | Dual-write code wired; live verification requires Cloud Run deployment + endpoint call |
| DATA-03 | 08.1-04 | Verify impact score population | NEEDS HUMAN | Impact computation code exists; depends on DATA-02 data; deferred to post-deployment |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | No stubs, TODOs, or incomplete implementations found in phase artifacts | — | — |

Notes:
- Pre-existing test failure in `tests/test_cli.py` (`test_optimize_pipeline_integration`) is a race condition that exists independently of this phase's changes. 789/790 tests pass.
- Plan 08.1-04 Task 2 is a checkpoint task (type: checkpoint:human-verify, gate: blocking) — the plan design explicitly requires human approval before full execution. The checkpoint was approved by the user with the understanding that live verification is deferred.

---

## Human Verification Required

### 1. DATA-01: 50-SKU Test Gate

**Test:** After Cloud Run deployment, run:
```bash
cd /Users/bobby/Documents/GitHub/Allied-FeedOps
set -a && source .env.vercel && set +a
GOOGLE_ADS_API_ENABLED=1 PYTHONPATH=./src python scripts/bulk_baseline_backfill.py --test-gate
```
**Expected:** Offer ID match rate >=90%, rows appear in `performance_baselines_variant`, time per batch is acceptable (< 5 min per 50 SKUs).
**Why human:** Requires live Google Ads API access. Variant tables are currently empty (expected). Cannot execute until Cloud Run ships the new normalization + dual-write code. If match rate is >=90%, authorize full sweep with `python scripts/bulk_baseline_backfill.py`.

---

### 2. DATA-02: Daily Snapshot Variant Dual-Write

**Test:** After Cloud Run deployment, trigger daily collection and verify both tables receive rows:
```bash
curl -X POST "$FEEDOPS_PIPELINE_URL/performance/collect-daily"
# Then in Supabase:
SELECT COUNT(*) FROM performance_snapshots WHERE snapshot_date = CURRENT_DATE;
SELECT COUNT(*) FROM performance_snapshots_variant WHERE snapshot_date = CURRENT_DATE;
```
**Expected:** variant_snapshots count > 0 AND variant count > master count (multiple offer IDs per SKU).
**Why human:** Requires a deployed Cloud Run service with the new `performance_impact.py` code. The dual-write block (lines 370-416) is correct in source, but the currently-deployed service does not yet contain it.

---

### 3. DATA-03: Impact Score Population

**Test:** After DATA-02 succeeds, trigger impact computation:
```bash
curl -X POST "$FEEDOPS_PIPELINE_URL/performance/compute-impact"
SELECT COUNT(*) FROM performance_impact_scores;
```
**Expected:** rows > 0 in `performance_impact_scores` table.
**Why human:** Depends on DATA-02 snapshot data being present. Scores are computed from snapshot data — empty tables produce 0 impact scores. Must run after DATA-02 is confirmed working.

---

## Gaps Summary

No blocking code gaps found. All six phase artifacts are substantive and wired. The three deferred items (DATA-01 test gate live run, DATA-02 snapshot verification, DATA-03 impact score verification) are not code gaps — they are live execution checkpoints that require Cloud Run deployment as a prerequisite.

The phase plan explicitly designated Plan 04 Task 2 as `type: checkpoint:human-verify` with `gate: blocking`. The checkpoint was approved by the user with deferred verification. This is expected behavior, not a gap.

**Deployment sequence required:**
1. Push current branch to master (triggers Cloud Run auto-deploy)
2. Verify Cloud Run build succeeded: `gcloud builds list --project=bobbys-project-346400 --limit=3`
3. Run DATA-01 test gate
4. Run DATA-02 daily snapshot trigger + verify
5. Run DATA-03 impact score trigger + verify

---

## Commit Verification

All 7 phase commits verified in git history:

| Commit | Plan | Description |
|--------|------|-------------|
| `a8ce4f9e` | 08.1-01 | feat: create offer ID normalization utility with TDD |
| `55f60a24` | 08.1-01 | feat: create and apply migration 043 for variant performance tables |
| `d70cafe1` | 08.1-02 | feat: apply offer ID normalization to all 4 Python codepaths |
| `0fb5d788` | 08.1-02 | feat: add variant-level dual-write to collect_daily_performance_snapshots |
| `408e92e6` | 08.1-03 | docs: create entity relationship documentation with Mermaid ER diagrams |
| `fe085837` | 08.1-04 | feat: create bulk baseline backfill script with test gate |
| `f6a26a7d` | 08.1-04 | docs: checkpoint at Task 2 — bulk backfill script complete, pipeline verification pending |

---

_Verified: 2026-03-04_
_Verifier: Claude (gsd-verifier)_
