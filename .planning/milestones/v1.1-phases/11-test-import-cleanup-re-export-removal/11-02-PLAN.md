---
phase: 11-test-import-cleanup-re-export-removal
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/feedops/pipeline/generator.py
  - tests/test_prompt_sanitization_contract.py
autonomous: true
requirements: [DEAD-04]

must_haves:
  truths:
    - "generator.py contains no copies of functions that exist in executor.py — zero duplicates remain"
    - "test_prompt_sanitization_contract.py imports _platform_reasoning_effort and _platform_completion_cap from executor.py, not generator.py"
    - "All tests pass after duplicate removal"
  artifacts:
    - path: "src/feedops/pipeline/generator.py"
      provides: "generator.py with duplicate functions removed"
    - path: "tests/test_prompt_sanitization_contract.py"
      provides: "Test importing from canonical executor.py location"
      contains: "from feedops.generation.executor import"
  key_links:
    - from: "tests/test_prompt_sanitization_contract.py"
      to: "feedops.generation.executor"
      via: "direct import of _platform_completion_cap and _platform_reasoning_effort"
      pattern: "from feedops\\.generation\\.executor import"
---

<objective>
Remove 2 duplicate helper functions from generator.py that already exist in executor.py (DEAD-04), and update the one test file that imports them from generator.py.

Purpose: Eliminate the last source-of-truth ambiguity between generator.py and executor.py — executor.py is the single canonical location for per-platform generation utilities.
Output: generator.py shrunk by ~40 lines, test_prompt_sanitization_contract.py updated.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-test-import-cleanup-re-export-removal/11-RESEARCH.md

<interfaces>
<!-- Duplicate functions confirmed at these exact locations -->

generator.py duplicates (TO DELETE):
- `_platform_reasoning_effort` at line 75
- `_platform_completion_cap` at line 82

executor.py canonical copies (KEEP):
- `_platform_reasoning_effort` at line 74
- `_platform_completion_cap` at line 80

NOT duplicates (DO NOT DELETE from generator.py):
- `generate_per_platform` — canonical, imported by routes.py, generation.py, job_runner.py, optimize.py
- `GenerationBudgetExceededError` — canonical, imported by routes.py

test_prompt_sanitization_contract.py current imports:
```python
from feedops.pipeline.generator import (
    _platform_completion_cap,
    _platform_reasoning_effort,
    generate_per_platform,
)
```

After update:
```python
from feedops.generation.executor import (
    _platform_completion_cap,
    _platform_reasoning_effort,
)
from feedops.pipeline.generator import generate_per_platform
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate functions from generator.py and update test import (DEAD-04)</name>
  <files>
    src/feedops/pipeline/generator.py
    tests/test_prompt_sanitization_contract.py
  </files>
  <action>
1. **Verify current duplicates exist** — Run:
   ```
   grep -n "_platform_reasoning_effort\|_platform_completion_cap" src/feedops/pipeline/generator.py src/feedops/generation/executor.py
   ```
   Confirm both functions exist in both files with identical implementations.

2. **Search for any additional duplicates** between generator.py and executor.py (per user decision: "Do a verification search for any remaining duplicates between the two files before declaring DEAD-04 complete"). Compare function definitions in both files. If any additional duplicates are found beyond the known 2, include them in the removal.

3. **Delete from generator.py:**
   - Remove the `_platform_reasoning_effort` function definition (~7 lines starting at line 75)
   - Remove the `_platform_completion_cap` function definition (~7 lines starting at line 82)
   - Check if any imports become orphaned after deletion — remove those too (ruff will catch them)

4. **Verify no internal callers in generator.py:**
   ```
   grep -n "_platform_reasoning_effort\|_platform_completion_cap" src/feedops/pipeline/generator.py
   ```
   Should return zero results. If any internal calls exist (unexpected), they must be updated to import from executor.py.

5. **Update test_prompt_sanitization_contract.py:**
   - Change the import from:
     ```python
     from feedops.pipeline.generator import (
         _platform_completion_cap,
         _platform_reasoning_effort,
         generate_per_platform,
     )
     ```
   - To:
     ```python
     from feedops.generation.executor import (
         _platform_completion_cap,
         _platform_reasoning_effort,
     )
     from feedops.pipeline.generator import generate_per_platform
     ```

6. **Run verification:**
   - `PYTHONPATH=./src .venv/bin/python -m pytest tests/test_prompt_sanitization_contract.py -q`
   - `.venv/bin/ruff check src/feedops/pipeline/generator.py tests/test_prompt_sanitization_contract.py`
   - `PYTHONPATH=./src .venv/bin/python -m pytest tests/ -q --tb=short` (full suite)

7. **Commit:** `refactor(dead-code): remove duplicate _platform_reasoning_effort and _platform_completion_cap from generator.py (DEAD-04)`

Note: If DEAD-04's original "6 functions" count is outdated (Phase 9 already removed `_generate_with_provider_compat` and others), update the commit message to reflect actual count removed. Per user decision: "update the requirement note to match current reality rather than chasing the original count."
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps && grep -c "_platform_reasoning_effort\|_platform_completion_cap" src/feedops/pipeline/generator.py | xargs test 0 -eq && PYTHONPATH=./src .venv/bin/python -m pytest tests/test_prompt_sanitization_contract.py -q && .venv/bin/ruff check src/feedops/pipeline/generator.py tests/test_prompt_sanitization_contract.py && PYTHONPATH=./src .venv/bin/python -m pytest tests/ -q --tb=short</automated>
  </verify>
  <done>Both duplicate functions removed from generator.py. test_prompt_sanitization_contract.py imports from executor.py. Zero duplicates between generator.py and executor.py. All tests pass. ruff clean.</done>
</task>

</tasks>

<verification>
- `grep -n "_platform_reasoning_effort\|_platform_completion_cap" src/feedops/pipeline/generator.py` returns zero lines
- `grep -n "_platform_reasoning_effort\|_platform_completion_cap" src/feedops/generation/executor.py` returns the canonical definitions
- `PYTHONPATH=./src .venv/bin/python -m pytest tests/ -q --tb=short` full suite green
- `.venv/bin/ruff check src/feedops/pipeline/generator.py` clean
</verification>

<success_criteria>
- Zero duplicate functions between generator.py and executor.py
- test_prompt_sanitization_contract.py imports from feedops.generation.executor
- Full test suite green (790+ tests passing)
- ruff clean on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-import-cleanup-re-export-removal/11-02-SUMMARY.md`
</output>
