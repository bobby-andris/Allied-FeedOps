# Milestone v1.1: Dead Code Cleanup + Data Infrastructure

**Status:** SHIPPED 2026-03-04
**Phases:** 8-12 (6 phases including 8.1 inserted)
**Total Plans:** 11

## Overview

Remove dead code from the v1.0 pipeline decomposition, fix the broken daily performance snapshot job, harden data table schemas, normalize entity relationships, and scale baseline coverage from 274 to all ~2,500 master SKUs.

## Phases

### Phase 8: Schema Hardening

**Goal**: Data table schemas enforce correctness at the database level — the daily performance snapshot job succeeds instead of failing silently
**Depends on**: Phase 7 (v1.0 complete)
**Requirements**: SCHM-01, SCHM-02, SCHM-03, SCHM-04
**Plans**: 1 plan

Plans:
- [x] 08-01-PLAN.md — Write and apply migration 042 (dedup + unique constraint + CHECK constraints + FK)

### Phase 8.1: Data Model Gap Audit (INSERTED)

**Goal**: Audit all data flows for granularity mismatches, create variant-level performance tables, normalize offer IDs across all codepaths, document entity relationships, and scale baseline coverage to all ~2,500 master SKUs.
**Depends on**: Phase 8
**Requirements**: ENTM-01, ENTM-02, ENTM-03, DATA-01, DATA-02, DATA-03
**Plans**: 4 plans

Plans:
- [x] 08.1-01-PLAN.md — Create offer ID normalization utility (ENTM-01) + migration 043 variant tables (DATA-02)
- [x] 08.1-02-PLAN.md — Apply normalization across 4 codepaths (ENTM-02) + variant dual-write
- [x] 08.1-03-PLAN.md — Entity relationship documentation with Mermaid ER diagrams (ENTM-03)
- [x] 08.1-04-PLAN.md — Bulk baseline backfill script (DATA-01) + pipeline verification (DATA-02, DATA-03)

### Phase 9: Trivial Dead Code Removal

**Goal**: All zero-caller orphan functions are deleted from the codebase — no test changes required, ruff and pytest stay green after each deletion
**Depends on**: Phase 8
**Requirements**: DEAD-01, DEAD-05
**Plans**: 2 plans

Plans:
- [x] 09-01-PLAN.md — Remove 8 orphan functions and re-exports (DEAD-01)
- [x] 09-02-PLAN.md — Remove FEEDOPS_VARIANT_AT_LLM_TIME feature flag block (DEAD-05)

### Phase 10: Image Wiring

**Goal**: All modern generation endpoints send product images to Claude during generation — SKUs with a main_image_url get richer context
**Depends on**: Phase 8 (independent of Phase 9)
**Requirements**: IMG-01
**Plans**: 1 plan

Plans:
- [x] 10-01-PLAN.md — Wire image fetch + forwarding through executor.py to provider (IMG-01)

### Phase 11: Test-Import Cleanup and Re-export Removal

**Goal**: Test files import from canonical module locations; main.py backward-compat re-export block is deleted; executor.py is the single source of truth for per-platform generation utilities
**Depends on**: Phase 9
**Requirements**: DEAD-02, DEAD-03, DEAD-04
**Plans**: 2 plans

Plans:
- [x] 11-01-PLAN.md — Migrate 4 test files to canonical imports (DEAD-02) + delete re-export block (DEAD-03)
- [x] 11-02-PLAN.md — Remove duplicate functions from generator.py (DEAD-04)

### Phase 12: Shared Utils Extraction

**Goal**: The duplicated _require_request_id() and GenerationBudgetExceededError exist in exactly one location — circular import between persistence.py and job_management.py is resolved cleanly
**Depends on**: Phase 11
**Requirements**: DEAD-06
**Plans**: 1 plan

Plans:
- [x] 12-01-PLAN.md — Create utils.py, consolidate _require_request_id and GenerationBudgetExceededError (DEAD-06)

---

## Milestone Summary

**Decimal Phases:**
- Phase 8.1: Data Model Gap Audit (inserted after Phase 8 — variant-level performance data was discarded during aggregation)

**Key Decisions:**
- Dead code before data infra: Low-risk cleanup reduces noise before schema changes
- variant_index as entity hub: 72K rows, central to all cross-platform mapping
- Upsert semantics: ignore_duplicates=True for snapshots (first-write-wins)
- normalize_offer_id() uses .lower() — simple, safe, idempotent
- executor.py is canonical location for per-platform generation utilities
- feedops/api/utils.py is canonical location for shared helpers

**Issues Resolved:**
- Fixed performance_snapshots 42P10 upsert failure (daily Slack errors)
- Removed ~1,200 lines of dead code (feature flags, orphan functions, duplicate re-exports)
- Normalized offer ID handling across all 4 Python data codepaths
- Wired product images through to Claude provider for richer content generation

**Issues Deferred:**
- Bing {FINISH_NAME} regeneration (96 SKUs) — deferred to v2
- Dashboard join migration (master_sku → gmc_offer_id) — out of pipeline scope
- optimize.py CLI retirement — needs explicit decision

**Technical Debt Incurred:**
- Phase 8.1 variant tables created but awaiting first daily run to populate
- Dashboard still uses master_sku joins (pipeline-only milestone)

---

_For current project status, see .planning/ROADMAP.md_
