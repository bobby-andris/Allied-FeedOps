---
phase: 30-historical-funnel-persistence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - dashboard/src/app/api/funnel-snapshots/__tests__/capture.test.ts
  - dashboard/src/app/api/funnel-snapshots/__tests__/trends.test.ts
  - dashboard/src/app/(dashboard)/shopping-funnel/__tests__/FunnelTrendCards.test.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "capture.test.ts exists with failing tests for auth, upsert, retention cleanup"
    - "trends.test.ts exists with failing tests for 7d vs prev-7d aggregation and edge cases"
    - "FunnelTrendCards.test.tsx exists with failing tests for rendering, trend arrows, and missing data"
  artifacts:
    - path: "dashboard/src/app/api/funnel-snapshots/__tests__/capture.test.ts"
      provides: "Test scaffold for capture endpoint (auth, upsert, retention)"
      min_lines: 40
    - path: "dashboard/src/app/api/funnel-snapshots/__tests__/trends.test.ts"
      provides: "Test scaffold for trends endpoint (aggregation, edge cases)"
      min_lines: 40
    - path: "dashboard/src/app/(dashboard)/shopping-funnel/__tests__/FunnelTrendCards.test.tsx"
      provides: "Test scaffold for trend cards (rendering, arrows, missing data)"
      min_lines: 40
  key_links: []
---

<objective>
Scaffold test files for all Phase 30 behavioral requirements before implementation begins.

Purpose: Establish automated verification for capture endpoint (auth, upsert, retention), trends endpoint (aggregation logic), and FunnelTrendCards (rendering, trend arrows, missing data handling). Tests will initially fail (RED phase) and pass as Plans 01 and 02 implement the production code.

Output: Three test files covering HIST-01 (retention), HIST-02 (capture + auth), and HIST-03 (trends + UI).
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-historical-funnel-persistence/30-CONTEXT.md
@.planning/phases/30-historical-funnel-persistence/30-RESEARCH.md
@dashboard/vitest.config.ts
@dashboard/src/lib/shopping-funnel/types.ts (LabelTierPerformance type - lines 200-218)
@dashboard/src/lib/supabase/admin.ts (createAdminClient pattern to mock)
@dashboard/src/app/api/performance/capture-snapshot/route.ts (existing capture endpoint test pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffolds for capture, trends, and FunnelTrendCards</name>
  <files>
    dashboard/src/app/api/funnel-snapshots/__tests__/capture.test.ts
    dashboard/src/app/api/funnel-snapshots/__tests__/trends.test.ts
    dashboard/src/app/(dashboard)/shopping-funnel/__tests__/FunnelTrendCards.test.tsx
  </files>
  <action>
Create three test files that define expected behavior for Phase 30. All tests should import from the production paths (which don't exist yet) and mock external dependencies (Supabase, Google Ads). Tests will fail until Plans 01/02 implement the code.

**1. `dashboard/src/app/api/funnel-snapshots/__tests__/capture.test.ts`:**

Mock `@/lib/shopping-funnel/service` (getLabelTierPerformance) and `@/lib/supabase/admin` (createAdminClient).

Test cases:
- `rejects requests without valid Authorization header (returns 401)`
- `rejects requests with wrong Bearer token (returns 401)`
- `calls getLabelTierPerformance with yesterday's date and upserts rows to Supabase`
- `returns snapshot_date, rows_captured in response body`
- `handles re-run for same day via upsert (no duplicate error)`
- `deletes rows older than 90 days during capture` -- verify that `.delete().lt('snapshot_date', cutoffDate).select('id')` is called
- `returns 500 with error message when getLabelTierPerformance throws`

Mock pattern:
```typescript
vi.mock('@/lib/shopping-funnel/service', () => ({
  getLabelTierPerformance: vi.fn(),
}))
vi.mock('@/lib/supabase/admin', () => ({
  createAdminClient: vi.fn(() => ({
    from: vi.fn(() => mockSupabaseChain),
  })),
}))
```

Set `process.env.CRON_SECRET = 'test-secret'` in beforeEach. Build NextRequest objects with appropriate headers.

**2. `dashboard/src/app/api/funnel-snapshots/__tests__/trends.test.ts`:**

Mock `@/lib/supabase/admin` (createAdminClient).

Test cases:
- `returns aggregated 7d current and previous period sums`
- `computes CTR as clicks/impressions (guards division by zero)`
- `computes ROAS as conversions_value / (cost_micros / 1e6) (guards division by zero)`
- `returns has_data: false when no snapshot rows exist`
- `returns has_previous: false when only current period has data`
- `applies Cache-Control header to response`

Mock the Supabase `.from().select().gte().lte()` chain to return configurable row sets for different test scenarios.

**3. `dashboard/src/app/(dashboard)/shopping-funnel/__tests__/FunnelTrendCards.test.tsx`:**

Use `@testing-library/react` (already in project). Mock fetch globally.

Test cases:
- `renders nothing when API returns has_data: false`
- `renders 6 cards with correct metric names: Impressions, Clicks, CTR, Ad Spend, Conversions, ROAS`
- `shows "No prior data" when has_previous is false`
- `shows green up arrow when metric increases more than 5%`
- `shows red down arrow when metric decreases more than 5%`
- `shows flat indicator when change is within 5% threshold`
- `inverts color for Ad Spend (down = green, up = red)`
- `formats numbers correctly: impressions with commas, CTR as percentage, Ad Spend as currency, ROAS with x suffix`

Mock fetch to return different response shapes for each test scenario.

IMPORTANT: These tests define the behavioral contract. They should be specific enough that the implementation in Plans 01/02 must satisfy them, but not so coupled to implementation details that minor refactors break them. Focus on inputs/outputs and observable behavior.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npx vitest run src/app/api/funnel-snapshots/__tests__/ src/app/\\(dashboard\\)/shopping-funnel/__tests__/ 2>&1 | tail -20</automated>
    <manual>Verify all three test files exist and contain the specified test cases. Tests should fail (RED) since production code does not exist yet -- that is expected and correct.</manual>
  </verify>
  <done>Three test files exist covering all HIST-01/02/03 behavioral requirements. Tests fail because production code is not yet implemented (RED phase). Test count: ~20 test cases across 3 files.</done>
</task>

</tasks>

<verification>
- All three test files exist at expected paths
- `npx vitest run` finds and attempts to run all tests
- Tests fail due to missing production code (expected RED state)
- No existing files modified
</verification>

<success_criteria>
- capture.test.ts covers auth rejection, upsert behavior, retention cleanup, error handling
- trends.test.ts covers aggregation, derived metrics (CTR/ROAS), edge cases (no data, no previous)
- FunnelTrendCards.test.tsx covers rendering, trend arrows, threshold logic, number formatting
- All tests are runnable (vitest finds them) even though they fail
</success_criteria>

<output>
After completion, create `.planning/phases/30-historical-funnel-persistence/30-00-SUMMARY.md`
</output>
