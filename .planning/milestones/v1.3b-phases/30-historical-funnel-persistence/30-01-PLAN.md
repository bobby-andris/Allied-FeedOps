---
phase: 30-historical-funnel-persistence
plan: 01
type: execute
wave: 1
depends_on:
  - 30-00
files_modified:
  - dashboard/src/app/api/funnel-snapshots/capture/route.ts
  - scripts/setup-funnel-scheduler.sh
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "funnel_snapshots_daily table exists in Supabase with correct schema and unique constraint"
    - "Calling POST /api/funnel-snapshots/capture with valid auth writes yesterday's tier data to Supabase"
    - "Calling the endpoint without valid auth returns 401"
    - "Re-running capture for the same day upserts (no duplicates)"
    - "Rows older than 90 days are deleted during capture and rows_deleted count is returned accurately"
    - "A Cloud Scheduler setup script exists that targets the capture endpoint at 5 AM ET daily per user decision"
  artifacts:
    - path: "dashboard/src/app/api/funnel-snapshots/capture/route.ts"
      provides: "POST endpoint that captures yesterday's funnel data to Supabase"
      min_lines: 40
    - path: "scripts/setup-funnel-scheduler.sh"
      provides: "Cloud Scheduler job setup script"
      min_lines: 15
  key_links:
    - from: "dashboard/src/app/api/funnel-snapshots/capture/route.ts"
      to: "dashboard/src/lib/shopping-funnel/service.ts"
      via: "import getLabelTierPerformance"
      pattern: "getLabelTierPerformance"
    - from: "dashboard/src/app/api/funnel-snapshots/capture/route.ts"
      to: "funnel_snapshots_daily table"
      via: "supabase.from('funnel_snapshots_daily').upsert()"
      pattern: "funnel_snapshots_daily"

user_setup:
  - service: gcp-cloud-scheduler
    why: "Daily automated funnel data capture"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random UUID, add to Vercel env vars AND pass to setup script"
    dashboard_config:
      - task: "Run setup-funnel-scheduler.sh after adding CRON_SECRET to Vercel"
        location: "Terminal: bash scripts/setup-funnel-scheduler.sh <CRON_SECRET>"
---

<objective>
Create the funnel_snapshots_daily table and capture endpoint that persists daily Google Ads shopping funnel tier data to Supabase.

Purpose: Replace ephemeral 2-minute cache with durable daily snapshots, enabling historical trend analysis. The capture endpoint reuses the existing proven `getLabelTierPerformance()` function from service.ts with zero modifications to the live query path.

Output: Supabase table, capture API route, Cloud Scheduler setup script.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-historical-funnel-persistence/30-CONTEXT.md
@.planning/phases/30-historical-funnel-persistence/30-RESEARCH.md
@.planning/phases/30-historical-funnel-persistence/30-00-SUMMARY.md
@dashboard/src/lib/shopping-funnel/service.ts (getLabelTierPerformance function - lines 1042-1101, DO NOT MODIFY)
@dashboard/src/lib/shopping-funnel/types.ts (LabelTierPerformance type - lines 200-218)
@dashboard/src/lib/supabase/admin.ts (createAdminClient pattern)
@dashboard/src/app/api/performance/capture-snapshot/route.ts (existing capture endpoint pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create funnel_snapshots_daily table and capture endpoint</name>
  <files>dashboard/src/app/api/funnel-snapshots/capture/route.ts</files>
  <action>
1. Create the funnel_snapshots_daily table via Supabase MCP (`mcp__supabase__execute_sql`):
```sql
CREATE TABLE IF NOT EXISTS funnel_snapshots_daily (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  snapshot_date DATE NOT NULL,
  custom_label_0 TEXT NOT NULL,
  tier TEXT NOT NULL CHECK (tier IN ('HIGH', 'MEDIUM', 'LOW')),
  impressions BIGINT NOT NULL DEFAULT 0,
  clicks BIGINT NOT NULL DEFAULT 0,
  cost_micros BIGINT NOT NULL DEFAULT 0,
  conversions DOUBLE PRECISION NOT NULL DEFAULT 0,
  conversions_value DOUBLE PRECISION NOT NULL DEFAULT 0,
  roas DOUBLE PRECISION NOT NULL DEFAULT 0,
  captured_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (snapshot_date, custom_label_0, tier)
);
CREATE INDEX IF NOT EXISTS idx_funnel_snapshots_date ON funnel_snapshots_daily (snapshot_date DESC);
ALTER TABLE funnel_snapshots_daily ENABLE ROW LEVEL SECURITY;
```

2. Create `dashboard/src/app/api/funnel-snapshots/capture/route.ts`:
   - POST handler with shared-secret auth: check `Authorization: Bearer {CRON_SECRET}` header against `process.env.CRON_SECRET`
   - If no CRON_SECRET env var set or header doesn't match, return 401
   - Compute yesterday's date in UTC: `const d = new Date(); d.setUTCDate(d.getUTCDate() - 1); const yesterday = d.toISOString().split('T')[0]`
   - Call `getLabelTierPerformance({ startDate: yesterday, endDate: yesterday })` -- import from `@/lib/shopping-funnel/service` (DO NOT modify service.ts)
   - Map result.rows to Supabase rows with snapshot_date, custom_label_0, tier, impressions, clicks, cost_micros, conversions, conversions_value, roas
   - Upsert to `funnel_snapshots_daily` with onConflict: `'snapshot_date,custom_label_0,tier'`
   - After upsert, run 90-day retention cleanup: Chain `.select('id')` on the delete call to get the count:
     ```typescript
     const { data: deletedRows } = await supabase
       .from('funnel_snapshots_daily')
       .delete()
       .lt('snapshot_date', cutoff.toISOString().split('T')[0])
       .select('id')
     const rowsDeleted = deletedRows?.length ?? 0
     ```
   - Return JSON: `{ snapshot_date, rows_captured: rows.length, rows_deleted: rowsDeleted }` on success
   - Wrap in try/catch, return 500 with error message on failure
   - Add console.log for key steps (start, rows fetched, upsert complete, cleanup complete) for Cloud Scheduler debugging

CRITICAL: Do NOT import from or modify any existing shopping-funnel page files. The capture route is a completely new, independent API endpoint.
  </action>
  <verify>
    <automated>cd /Users/bobby/Documents/GitHub/Allied-FeedOps/dashboard && npx vitest run src/app/api/funnel-snapshots/__tests__/capture.test.ts 2>&1 | tail -20</automated>
    <manual>Verify table exists: run SELECT count(*) FROM funnel_snapshots_daily via Supabase MCP. Verify route.ts compiles with no type errors.</manual>
  </verify>
  <done>funnel_snapshots_daily table exists in Supabase with unique constraint and index. Capture route passes all capture.test.ts tests: auth rejection, upsert, retention cleanup with accurate rows_deleted count, error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Create Cloud Scheduler setup script</name>
  <files>scripts/setup-funnel-scheduler.sh</files>
  <action>
Create `scripts/setup-funnel-scheduler.sh` that sets up a Cloud Scheduler job:

- Accept CRON_SECRET as first argument (required, fail with usage message if missing)
- Job name: `feedops-funnel-snapshot-daily`
- Project: `bobbys-project-346400`
- Location: `us-east1`
- Schedule: `0 5 * * *` (5 AM ET per user decision)
- Timezone: `America/New_York`
- URI: `https://allied-feed-ops.vercel.app/api/funnel-snapshots/capture`
- HTTP method: POST
- Headers: `Content-Type=application/json,Authorization=Bearer ${CRON_SECRET}`
- Message body: `{}`
- Attempt deadline: 120s
- Max retry attempts: 3
- Min backoff: 60s, Max backoff: 300s
- Add a comment in the script: `# NOTE: 5 AM ET = 2 AM PT. Google Ads data for yesterday may be ~95% settled at this time`
  `# (full settlement typically by 3-4 AM PT). If snapshot data appears incomplete, consider`
  `# changing schedule to "0 8 * * *" (8 AM ET / 5 AM PT) for full data settlement.`
- Make script executable: add `#!/bin/bash` and `set -euo pipefail`
- Include a `--delete` option to remove the job: `if [ "${1:-}" = "--delete" ]; then gcloud scheduler jobs delete ...`

Note: This script is run manually by the user after setting CRON_SECRET in Vercel env vars. It is a checkpoint:human-action item handled via user_setup frontmatter.
  </action>
  <verify>
    <automated>bash -n /Users/bobby/Documents/GitHub/Allied-FeedOps/scripts/setup-funnel-scheduler.sh 2>&1 && echo "Script syntax OK"</automated>
    <manual>Review script for correct project ID, URI, schedule (5 AM ET), and auth header format. Verify the data settlement comment is present.</manual>
  </verify>
  <done>Cloud Scheduler setup script exists at scripts/setup-funnel-scheduler.sh, is syntactically valid bash, creates a job targeting the capture endpoint at 5 AM ET daily (per user decision) with retry and backoff configuration, and includes a comment about data settlement timing.</done>
</task>

</tasks>

<verification>
- Capture tests pass: `cd dashboard && npx vitest run src/app/api/funnel-snapshots/__tests__/capture.test.ts`
- TypeScript compilation passes: `cd dashboard && npx tsc --noEmit`
- funnel_snapshots_daily table exists: `SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'funnel_snapshots_daily'`
- Unique constraint exists on (snapshot_date, custom_label_0, tier)
- Setup script is syntactically valid bash
- No existing shopping-funnel files were modified (verify with `git diff dashboard/src/app/(dashboard)/shopping-funnel/ dashboard/src/lib/shopping-funnel/`)
</verification>

<success_criteria>
- funnel_snapshots_daily table created in production Supabase with correct schema
- POST /api/funnel-snapshots/capture endpoint passes all capture.test.ts tests
- rows_deleted accurately reflects deleted row count (uses .select('id') chain)
- Cloud Scheduler setup script uses 5 AM ET with data settlement advisory comment
- Zero modifications to any existing shopping-funnel files
</success_criteria>

<output>
After completion, create `.planning/phases/30-historical-funnel-persistence/30-01-SUMMARY.md`
</output>
