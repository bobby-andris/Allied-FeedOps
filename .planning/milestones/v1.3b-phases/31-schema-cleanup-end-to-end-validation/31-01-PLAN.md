---
phase: 31-schema-cleanup-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/database/SCHEMA.md
autonomous: true
requirements:
  - MIGR-01
  - MIGR-04

must_haves:
  truths:
    - "All 14 KEEP'd tables exist in production with schemas matching their migration SQL"
    - "All 4 DEFER'd tables exist in production and are documented with [DEFER] tags"
    - "SCHEMA.md reflects every table in the public schema — no missing tables, no phantom tables"
    - "Phase 29-30 tables (content_performance_summary, funnel_snapshots_daily, search_query_snapshots, performance_impact_scores) are documented"
  artifacts:
    - path: "docs/database/SCHEMA.md"
      provides: "Complete production schema reference with [KEEP] and [DEFER] tags"
      min_lines: 500
  key_links:
    - from: "docs/database/SCHEMA.md"
      to: "production Supabase"
      via: "information_schema.columns query"
      pattern: "table_name.*column_name.*data_type"
---

<objective>
Verify all 18 deferred tables (14 KEEP + 4 DEFER) exist in production with correct schemas, then rebuild SCHEMA.md from scratch using information_schema queries to create a single source of truth for all database work.

Purpose: Establishes ground truth for the production schema so all subsequent Phase 31 work (component wiring, page validation) operates on verified foundations. Prevents the "schema drift" problem where SCHEMA.md diverges from reality.

Output: Verified schema state + rebuilt SCHEMA.md with complete table documentation.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-architecture-audit-migration-triage/28-migration-triage.md
@docs/database/SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 18 deferred table schemas against migration SQL</name>
  <files>docs/database/SCHEMA.md</files>
  <action>
    Use Supabase MCP `execute_sql` to run schema verification queries against production:

    1. Get complete table inventory:
    ```sql
    SELECT table_name,
           (SELECT count(*) FROM information_schema.columns c WHERE c.table_name = t.table_name AND c.table_schema = 'public') as column_count
    FROM information_schema.tables t
    WHERE table_schema = 'public' AND table_type = 'BASE TABLE'
    ORDER BY table_name;
    ```

    2. For each of the 18 deferred tables, query columns and compare to migration SQL:
    ```sql
    SELECT column_name, data_type, is_nullable, column_default
    FROM information_schema.columns
    WHERE table_schema = 'public' AND table_name = '{table}'
    ORDER BY ordinal_position;
    ```

    Tables to verify (14 KEEP):
    - ga4_source_medium_daily, ga4_landing_page_quality_daily, ga4_attribution_root_cause_daily, ga4_shopify_reconciliation_daily
    - term_intent_state, policy_decision_log, policy_action_execution_log, policy_snapshots
    - experiment_registry, experiment_assignments, experiment_outcomes
    - negative_registry, search_buildout_recommendations, operator_review_audit

    Tables to verify (4 DEFER):
    - intent_taxonomy_versions, sku_margin_daily, order_line_returns_daily, attribution_confidence_daily

    Also verify Phase 29-30 tables exist:
    - content_performance_summary, performance_impact_scores, search_query_snapshots, funnel_snapshots_daily

    3. Document any schema mismatches. If a column mismatch is found between production and migration SQL, note it but do NOT alter the production schema — document the discrepancy.

    4. Check row counts for each table to document data state:
    ```sql
    SELECT 'table_name' as tbl, count(*) as row_count FROM table_name
    UNION ALL ...
    ```
  </action>
  <verify>
    <automated>echo "Schema verification is manual via Supabase MCP queries — verify by checking that all 22 tables (18 deferred + 4 Phase 29-30) are accounted for in the output"</automated>
  </verify>
  <done>All 18 deferred tables confirmed to exist in production. Column schemas documented. Any mismatches between migration SQL and production noted. Phase 29-30 tables confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild SCHEMA.md from production information_schema</name>
  <files>docs/database/SCHEMA.md</files>
  <action>
    Full refresh of SCHEMA.md from production. This is a complete rewrite, not incremental patching.

    1. Query ALL tables and their full column definitions from production:
    ```sql
    SELECT table_name, column_name, data_type, is_nullable, column_default, ordinal_position
    FROM information_schema.columns
    WHERE table_schema = 'public'
    ORDER BY table_name, ordinal_position;
    ```

    2. Rebuild SCHEMA.md with this structure for each table:
    ```markdown
    ## table_name [STATUS]

    **Purpose:** Brief description
    **Status:** PRODUCTION / KEEP / DEFER

    | Column | Type | Nullable | Default | Notes |
    |--------|------|----------|---------|-------|
    | column_name | data_type | YES/NO | default | ... |
    ```

    3. Status tags:
    - Tables that existed before Phase 28: No tag (they are core PRODUCTION tables)
    - 14 KEEP'd tables from Phase 28 triage: `[KEEP]`
    - 4 DEFER'd tables from Phase 28 triage: `[DEFER]` with note about which version they activate in
    - Phase 29-30 tables: No tag (they are core PRODUCTION tables, recently added)

    4. Add a summary section at the top with:
    - Total table count
    - Table categories (Core, Phase 29-30 additions, KEEP'd deferred, DEFER'd deferred)
    - Last refresh date
    - Quick reference for JSONB parsing patterns and common column conventions (from existing SCHEMA.md)

    5. Preserve the existing SCHEMA.md's useful documentation patterns:
    - JSONB parsing: `(column#>>'{}')::jsonb` pattern
    - LATERAL joins for array expansion
    - Case sensitivity notes
    - Column naming conventions (approval_status not status, etc.)

    IMPORTANT: Do NOT fabricate column information. Every column must come from the information_schema query result. If a query fails or returns unexpected results, document the discrepancy.
  </action>
  <verify>
    <automated>test -f docs/database/SCHEMA.md && wc -l docs/database/SCHEMA.md | awk '{print ($1 > 500) ? "PASS: " $1 " lines" : "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>SCHEMA.md fully rebuilt from production information_schema. Every production table documented. All 18 deferred tables tagged with [KEEP] or [DEFER]. Phase 29-30 tables included. JSONB conventions and query patterns preserved.</done>
</task>

</tasks>

<verification>
1. `grep -c '## ' docs/database/SCHEMA.md` returns a count matching or exceeding the total production table count
2. `grep -c '\[KEEP\]' docs/database/SCHEMA.md` returns 14
3. `grep -c '\[DEFER\]' docs/database/SCHEMA.md` returns 4
4. `grep 'funnel_snapshots_daily' docs/database/SCHEMA.md` finds the Phase 30 table
5. `grep 'content_performance_summary' docs/database/SCHEMA.md` finds the Phase 29 table
</verification>

<success_criteria>
- SCHEMA.md is a complete, accurate representation of the production database
- All 14 KEEP'd tables are documented and verified against migration SQL
- All 4 DEFER'd tables are documented with clear status tags
- Phase 29-30 tables (content_performance_summary, funnel_snapshots_daily, etc.) are documented
- No phantom tables (tables in SCHEMA.md that don't exist in production)
- No missing tables (tables in production that aren't in SCHEMA.md)
</success_criteria>

<output>
After completion, create `.planning/phases/31-schema-cleanup-end-to-end-validation/31-01-SUMMARY.md`
</output>
