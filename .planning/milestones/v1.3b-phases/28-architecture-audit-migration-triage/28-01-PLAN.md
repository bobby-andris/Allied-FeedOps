---
phase: 28-architecture-audit-migration-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-architecture-audit-migration-triage/28-data-flow-map.md
  - docs/architecture/data-flow-map.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-05

must_haves:
  truths:
    - "A data flow document exists mapping every table from Google Ads API through to dashboard actions and back"
    - "Dead ends are explicitly marked (e.g., service.ts no-persist, empty dashboard pages)"
    - "Both TypeScript and Python Google Ads integration paths are mapped as separate flows"
    - "The circular feedback loop (capture -> monitor -> analyze -> optimize -> repeat) is validated with gap analysis"
  artifacts:
    - path: ".planning/phases/28-architecture-audit-migration-triage/28-data-flow-map.md"
      provides: "Complete data flow map with Mermaid diagrams and annotated prose"
      contains: "mermaid"
    - path: "docs/architecture/data-flow-map.md"
      provides: "Long-term reference copy of data flow map"
      contains: "mermaid"
  key_links:
    - from: "Google Ads API"
      to: "performance_snapshots"
      via: "Python pipeline (google_ads_performance.py)"
      pattern: "shopping_performance_view"
    - from: "Google Ads API"
      to: "Dashboard (ephemeral)"
      via: "TypeScript service.ts (2-min cache, no persist)"
      pattern: "search_term_view"
    - from: "publish_events"
      to: "Google Sheets -> GMC -> Google Ads"
      via: "Publishing chain (expand-variants -> google-sheets)"
      pattern: "publish_events.*Google Sheets"
---

<objective>
Map the complete production data flow from Google Ads API through service layers, database tables, dashboard pages, and publishing actions back to Google Ads. Validate the circular feedback loop schema.

Purpose: Establish verified ground truth for all subsequent phases (29-31) to build on. Without this map, we risk building on assumptions about data flow that don't match production reality.
Output: Data flow document with Mermaid diagrams in both phase directory and docs/architecture/ for long-term reference.
</objective>

<execution_context>
@/Users/bobby/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bobby/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-architecture-audit-migration-triage/28-RESEARCH.md
@docs/database/SCHEMA.md
@dashboard/src/lib/shopping-funnel/service.ts
@dashboard/src/lib/google-ads.ts
@src/feedops/integrations/google_ads_performance.py
@src/feedops/integrations/google_ads_search_terms.py
@dashboard/src/lib/publishing/expand-variants.ts
@dashboard/src/lib/publishing/google-sheets.ts
@dashboard/src/app/api/performance/capture-snapshot/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query production schema and build data flow map</name>
  <files>
    .planning/phases/28-architecture-audit-migration-triage/28-data-flow-map.md
  </files>
  <action>
    1. Query production Supabase for actual table list:
       ```sql
       SELECT tablename FROM pg_tables WHERE schemaname = 'public' ORDER BY tablename;
       ```
    2. Compare result against SCHEMA.md — document any surprise tables or missing tables.
    3. Build a comprehensive data flow document with these sections:

    **Section 1: Data Ingestion (Google Ads -> Database)**
    - Map TypeScript path: `google-ads.ts` -> `shopping_performance_view` GAQL -> `performance_baselines` table
    - Map TypeScript ephemeral path: `service.ts` -> 7 GAQL queries -> 2-min in-memory cache -> Dashboard pages (NO database persistence) — mark as DEAD END for persistence
    - Map Python pipeline path: `google_ads_performance.py` -> `shopping_performance_view` GAQL -> `performance_snapshots` + `performance_impact_scores`
    - Map Python search terms path: `google_ads_search_terms.py` -> `search_term_view` GAQL -> `search_queries` + `search_queries_by_master_sku`
    - Flag redundancy: Both TS google-ads.ts and Python pipeline query `shopping_performance_view`

    **Section 2: Content Generation & Publishing (Database -> External)**
    - Map: `generated_content` -> approval -> `publish_events` (with prompt_hash) -> `expand-variants.ts` -> `google-sheets.ts` -> Google Sheets -> GMC -> Google Ads
    - Map: `variant_finish_sentences` -> integrated during variant expansion
    - Map: Lifestyle images: `product_lifestyle_images` / `variant_lifestyle_images` -> Shopify CDN -> Google Sheets

    **Section 3: Performance Monitoring (External -> Database)**
    - Map: Google Ads -> Python pipeline -> `performance_snapshots` (with publish_event_id FK)
    - Map: `performance_snapshots` + `publish_events` -> `performance_impact_scores` (diff-in-diff)
    - Map: `search_query_snapshots` (monitoring endpoint)

    **Section 4: Dashboard Consumption**
    - Map each dashboard page to its data source tables
    - Mark empty pages (Shopping Funnel, Optimization Control, Intent Control, Search Governance, Experiment Lab) as depending on 035b tables
    - Mark service.ts-dependent pages as ephemeral-only

    **Section 5: Dead Ends & Gaps**
    - service.ts: 7 GAQL queries with no persistence (Phase 30 gap)
    - funnel_snapshots_daily: Does not exist yet (Phase 30 creates it)
    - 034b/035b tables: Empty or unpopulated (defer to Task in Plan 02)
    - Orphaned components: GmcDisapprovalBadge, PromptLineagePanel

    Use Mermaid `graph TD` diagrams for each section. Include a master overview diagram at top.
    Include annotated prose explaining each flow.

    4. Read the actual source files listed in context to verify integration points are accurately mapped. Do NOT guess — confirm from code.
  </action>
  <verify>
    <automated>grep -c "mermaid" .planning/phases/28-architecture-audit-migration-triage/28-data-flow-map.md</automated>
    <manual>Document has Mermaid diagrams for all 4 data flow sections plus master overview. Dead ends are marked. Both TS and Python paths shown.</manual>
  </verify>
  <done>Data flow document exists with Mermaid diagrams covering all integration points. Dead ends explicitly marked. Both TS and Python Google Ads paths mapped. Schema comparison against production complete.</done>
</task>

<task type="auto">
  <name>Task 2: Validate circular feedback loop and create long-term reference</name>
  <files>
    .planning/phases/28-architecture-audit-migration-triage/28-data-flow-map.md
    docs/architecture/data-flow-map.md
  </files>
  <action>
    1. Add a "Circular Flow Validation" section to the data flow map document (AUDIT-05).
    2. Trace the complete loop and validate each link exists in production schema:
       - **Capture**: Google Ads API -> performance_baselines / performance_snapshots (EXISTS)
       - **Monitor & Evaluate**: performance_snapshots -> dashboard performance pages (EXISTS)
       - **Analyze & Learn**: performance_impact_scores (diff-in-diff from snapshots + publish_events) (EXISTS)
       - **Optimize**: generated_content -> approval workflow -> publish_events (EXISTS)
       - **Repeat**: publish_events -> Google Sheets -> GMC -> Google Ads (EXISTS)
    3. For each link, note:
       - Whether the table/join EXISTS in production
       - Whether it contains data (row count from production query)
       - Whether it has working code consumers
       - Gap assessment (what's missing for the loop to close)
    4. Query production for row counts on key tables in the loop:
       ```sql
       SELECT 'performance_baselines' as tbl, COUNT(*) as rows FROM performance_baselines
       UNION ALL SELECT 'performance_snapshots', COUNT(*) FROM performance_snapshots
       UNION ALL SELECT 'performance_impact_scores', COUNT(*) FROM performance_impact_scores
       UNION ALL SELECT 'publish_events', COUNT(*) FROM publish_events
       UNION ALL SELECT 'generated_content', COUNT(*) FROM generated_content;
       ```
    5. Create a Mermaid diagram showing the loop with status annotations (green=working, yellow=exists-but-gaps, red=missing).
    6. Summarize: Can the loop close today? What's blocking? What does Phase 29/30/31 need to fix?
    7. Copy the complete document to `docs/architecture/data-flow-map.md` for long-term reference.
  </action>
  <verify>
    <automated>test -f docs/architecture/data-flow-map.md && grep -c "Circular" docs/architecture/data-flow-map.md</automated>
    <manual>Circular flow validation section exists with per-link status. Row counts from production included. Gap summary for Phases 29-31 present.</manual>
  </verify>
  <done>Circular feedback loop validated with production data. Each link has EXISTS/GAP status. Long-term reference copy in docs/architecture/. Gap summary informs Phase 29-31 planning.</done>
</task>

</tasks>

<verification>
1. Data flow document exists at `.planning/phases/28-architecture-audit-migration-triage/28-data-flow-map.md`
2. Long-term reference exists at `docs/architecture/data-flow-map.md`
3. Mermaid diagrams render correctly (valid syntax)
4. Both TypeScript and Python Google Ads paths are mapped
5. Dead ends are explicitly marked
6. Circular flow validation includes production row counts
7. Schema comparison (pg_tables vs SCHEMA.md) is documented
</verification>

<success_criteria>
- Data flow map covers all 4 sections (ingestion, publishing, monitoring, dashboard) with Mermaid diagrams
- Dead ends explicitly identified (service.ts no-persist, empty pages, missing tables)
- Circular loop validation shows per-link status with production evidence
- Both TS and Python integration paths mapped accurately from source code review
- Copy exists in docs/architecture/ for long-term reference
</success_criteria>

<output>
After completion, create `.planning/phases/28-architecture-audit-migration-triage/28-01-SUMMARY.md`
</output>
