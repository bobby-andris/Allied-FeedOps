---
phase: 25.4-production-impact-audit
task: 0
total_tasks: TBD (phase not yet planned)
status: not_started
last_updated: 2026-02-24T17:16:23Z
---

<current_state>
Phase 25.4 was just inserted. It needs to be planned and executed BEFORE merging
branch `v1.3a/prompt-rewrite-validation` to master. The branch has 31 commits and
touches 10 source files — many of which are used by BOTH the v1 and v2 prompt paths.
The `FEEDOPS_PROMPT_VERSION` feature flag only gates prompt SELECTION in main.py,
but shared modules (evidence.py, openai_provider.py, generator.py, etc.) affect
ALL production traffic regardless of flag setting.
</current_state>

<completed_work>
## This session accomplished:
1. Full open-phase audit (phases 25 through 25.3) — all statuses documented
2. Root cause identified: committed ab_prompt_test.py imported build_core_prompt (v1 legacy)
3. Working copy already had the fix (v2 imports) — committed it properly in 2 logical commits
4. Added 2 static guardrail tests (build_core_prompt never imported in v2 paths)
5. Fresh canonical run: 10 SKUs, Google+Bing, 120/120 checks PASS
6. Fresh unseen run: 5 SKUs (seed=52), Google+Bing, 63/63 checks PASS
7. Zero legacy "Product Evidence Table:" in any v2 user_prompt
8. Wrote 6 phase closeout SUMMARYs (25-07, 25.1-03, 25.2-03, 25.3-01, 25.3-02, 25.3-03)
9. Wrote v1.3a closeout decision memo
10. Created branch v1.3a/prompt-rewrite-validation and pushed all work
11. Inserted Phase 25.4 for production impact audit
</completed_work>

<remaining_work>
## Phase 25.4: Production Impact Audit (NOT YET PLANNED)

Must research and verify before merging to master:

### Files changed that could affect v1 production path:
1. `src/feedops/pipeline/evidence.py` — format_evidence_markdown() changes (for_customer_copy filtering). Both v1 and v2 call this.
2. `src/feedops/providers/openai_provider.py` — reasoning_effort/temperature mutual exclusion fix. Affects ALL API calls.
3. `src/feedops/pipeline/generator.py` — completion cap changes, generate_per_platform signature. v1 fallback path is inside this file.
4. `src/feedops/pipeline/prompts.py` — added GOOGLE_BRIEF/BING_BRIEF/etc constants + schema changes. SYSTEM_PROMPT and legacy schemas must be unchanged.
5. `src/feedops/pipeline/skill_loader.py` — get_platform_system_prompt() rewrite. Does the old get_system_prompt() still work for v1?
6. `src/feedops/pipeline/keyword_placement.py` — keyword distillation changes.
7. `src/feedops/pipeline/optimize.py` — changes unknown, need to diff.
8. `src/feedops/api/prompt_builder.py` — added v2 builders + helpers. Does build_core_prompt() still work identically for v1?

### Key questions to answer:
- Does `git diff origin/master..HEAD -- <file>` show ANY change to code paths exercised by v1?
- Are the evidence.py changes additive (new parameter) or breaking (changed default behavior)?
- Does openai_provider.py reasoning fix change behavior when FEEDOPS_REASONING_EFFORT is unset (current prod)?
- Do prompts.py schema changes (minLength, maxLength) affect the v1 CANDIDATE_SCHEMA?
- Is the v1 path in generator.py (build_split_prompt + generate_candidates) unchanged?

### Acceptance gate:
- Prove each file's changes are either (a) additive/new-code-only or (b) behind v2 feature flag
- OR identify breaking changes and decide: fix them, revert them, or accept the risk
- Document findings in a PLAN + SUMMARY
</remaining_work>

<decisions_made>
- Bobby correctly flagged that "merging changes zero production behavior" was wrong — shared modules affect both paths
- Created Phase 25.4 as an INSERTED urgent phase to properly audit before merge
- Branch approach: all work on v1.3a/prompt-rewrite-validation, master untouched
</decisions_made>

<blockers>
- Cannot merge to master until Phase 25.4 audit confirms safety
- Human review gate (Bobby/Robert scorecard) still pending for v1.3a deploy decision
- 25.3-04 (deploy v2 default) blocked on both audit + human gate
</blockers>

<context>
The branch has 31 commits touching prompt architecture, test harness, and pipeline source.
The v2 prompt path works correctly (proven by 183/183 test checks). The concern is whether
changes to SHARED modules (evidence, openai_provider, prompts, skill_loader, etc.) break
the v1 production path that's currently serving live traffic on Cloud Run.

The right approach for Phase 25.4:
1. Diff each changed file against origin/master
2. For each diff hunk, determine if it touches v1 code path
3. Write tests that exercise the v1 path with the new code
4. Run those tests
5. Document findings

Key artifacts:
- Branch: v1.3a/prompt-rewrite-validation (pushed to origin)
- Canonical results: /tmp/ab_test_outputs_25.3_final/run-canonical-20260224-164151/
- Unseen results: /tmp/ab_test_outputs_25.3_final/run-unseen-20260224-164200/
- Closeout memo: .planning/phases/25.3-prompt-rewrite/25.3a-closeout-decision-20260224.md
</context>

<next_action>
1. Run `/gsd:plan-phase 25.4` to create the production impact audit plan
2. The plan should diff each of the 10 changed source files against origin/master
3. Classify each change as: additive-only, v2-gated, or v1-affecting
4. Write targeted tests for any v1-affecting changes
5. Make go/no-go decision on merge safety
</next_action>
