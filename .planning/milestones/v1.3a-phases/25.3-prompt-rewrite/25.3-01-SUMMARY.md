# Phase 25.3-01: Cherry-Pick Useful Codex Changes

## Status: COMPLETE

**Commit:** `5933c906`

## What Was Done
Cherry-picked 4 useful changes from the Codex working tree:
1. **JSON parser** (`openai_provider.py`) — content-wrapper unwrapper for GPT-5.2's occasional `{"content": {...}}` wrapping
2. **Skill sanitizer** (`skill_loader.py`) — competitor brand and banned word stripping from injected skill text
3. **Enrichment fix** — evidence enrichment field filtering
4. **Reasoning/cap tuning** (`generator.py`) — platform-specific completion token caps with finish floor at 10K

## What Was Discarded
- ~200 lines of post-processing regex layer — masks prompt problems instead of fixing them
- `max_completion_tokens` reduction to 4000 — causes truncation in strict JSON mode
- Schema relaxation changes — weakens output contract
